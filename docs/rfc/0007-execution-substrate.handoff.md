# RFC-0007 — Execution substrate — handoff

- **Stage:** 4 code review — WAVES 1+2 COMPLETE AND PUSHED (through 41ce5fe, 2026-09-19): all 8 Highs + all behavioral/liveness Mediums (r1-r6, r9-r22, r26) + r55 fixed. Wave-1 CI green all 5 legs (run 35412693578). TIMING TRIO VERDICT (headofline/max_jobs_overlap/a2b_shared_grace): debug agent proved ENVIRONMENTAL, not wave-2 code — instrumented A/B showed pure wave-1 baseline fails 5/5 with the identical zero-compiles-in-6s signature under an unrelated proptest batch pegging the host at load 21-34 (bare term_ignores compile = 13.8s vs 6s budget); no code changed. Wave-2 CI GREEN ALL 5 LEGS incl. timing (run 35420882162, 41ce5fe) — regression question CLOSED as environmental. WAVE 3 PROGRESS (2026-09-19): batch A COMMITTED+PUSHED — r27 posixcore split (2fcf0d5), r7 ExecuteReport + r33 (06e0237), CI watch in flight. Batch A CI GREEN all 5 legs (run 35427609100). Batch B COMMITTED+PUSHED (06e0237..9e04564): r28 ChildSpec.claimOrphans (b003f57, restores RFC-pinned reap(sv,id); RFC ChildSpec pin + D2a-5 changelog amended), r8 RunDocument (9e04564); local boundary suite SUITE-EXIT:0, timing trio (headofline/max_jobs_overlap/a2b) failed ENVIRONMENTAL again (host load 10.7-13, same signature as the adjudicated wave-2 precedent) — batch-B CI watch in flight on sha 9e04564 adjudicates. Batch-B CI GREEN all 5 legs incl. timing (run 35431209825) — environmental verdict re-confirmed. Batch C COMMITTED (ALL MANDATED FINDINGS NOW CLOSED): r29/r30 (7e79b6a — verify-cache-pct in planImpl, RlimitOverrides bundle carrier), r23/r24/r25 (afe45c2 — pure decideExit + 10-arm table, ExecCtx, claimSlot/enterLive), ledger rows updated (93abe28). Local verification: timing FULLY GREEN (40 files, TIMING-EXIT:0, incl. the previously-environmental trio); ./dev test re-run IN FLIGHT (first run's SUITE-EXIT marker lost to a tail truncation — re-running for the honest marker, quiescent tree). Batch C commits UNPUSHED: 84c1e4a, 7e79b6a, afe45c2, 93abe28 (+ this handoff). On SUITE-EXIT:0: push, CI watch (full sha, poll for run creation), then re-review round 2 to the 0 C/H/M floor.
- **Resume:** read ./dev test re-run marker (expect SUITE-EXIT:0; quiescent tree); push origin main (84c1e4a..93abe28+) and arm CI watch (full sha, poll for run creation); on CI green launch re-review round 2 — standing security/design/liveness fable lenses + touched dims (runner/executor, result model/wire, CLI/config), re-verify new findings adversarially, loop to 0 C/H/M; on floor: quipu set 0007/review floor (round count + closing sha in commit msg), sweep [[item]] rows (w4 stays open deliberately), report remaining Lows (r31-r54 minus r33/r55), stage 5 close-out.
- **Build history:** BUILD COMPLETE 2026-09-10 (Stages A–C prior; Stage D grind: D1a/D1b/D2a); D2b closed via RFC-0009 B4b (CI 35170572732, 2026-09-16); D1c closed 2026-09-18 (05a90cb, CI 35353539907). RFC checklist: 32 checked, 0 unchecked. Both 2026-09-10 follow-ups closed: cbLimit attribution = D1c; RFC-0009 sequencing = done. Sub-slice briefs: scratchpad/D2a-*-brief.md, scratchpad/D1b-design-notes.md.
- **Sequencing (decided 2026-08-22):** #18 landed first → **0007 Stage A** (A0 CI baseline → result model + contract + seam, Linux, folds #1/#17) → **RFC-0005 build** (its `StoredEntry` wire freezes on A1's shape; the A7-gate re-baselines the 0005 doc+handoff first; FORK-2 still owed by Corey) → 0007 Stages B / C / D → RFC-0008 / RFC-0009. RFC-0006 parked (negative benchmark).

## Slices (re-cut in round 2; order is normative: A0 → A1a…A1f → A2a-i/ii/iii → A2d → A2b → A2c → A3…A7+gate)

| slice | what | status |
|-------|------|--------|
| A0 | Linux CI baseline + meta-test proving nonzero exit propagates (the `./dev test` grep trap) + serial `CRISOL_TIMING_TESTS=1` job — BEFORE any surgery | ✅ 4b16d32+d836fb0; CI run 33659482378 green (meta step + full leg + serial timing leg). Root causes found: nimble never propagates task failure to exit code (ci/run-tests.sh marker-file wrapper, fail-closed); gitignored milpa-generated nim.cfg absent in CI (fetch-deps.sh regenerates). Timing tests moved to tests/timing/. |
| A1a | types + `OutcomePolicy` + `deriveOutcome` derivation table + `classifyCause` table + `hasFailRecords` + `resultjson` (unknown-enum ⇒ structural failure for crisol's own readers); Outcome carries BOTH value sets until A1e-i | ✅ 3fb7b2d; CI 33663693135 green; 82 tests in 5 files; §2-shaped EntrypointResult lives in process/types.nim distinct from legacy; LimitKind family born here (needed structurally by Cause/Evidence) |
| A1b | honest producer — killAndReap/reapBlocking + teardownLiveSlots capture wstatus, wait4 (importc), classifyCause stamping, sink read on every end, dual-write + COHERENCE check, advisory `exit`/`cause` into run/v1 (rev bump) — **load-bearing**, E2E through `crisol run --json` (`hang_forever` ⇒ SIGTERM/`escalated:false`; `term_ignores` ⇒ SIGKILL/`escalated:true`). supervise deletion NOT here (moved to A2a-i) | ✅ fd99d45+a23b757; CI 33669237828 green. wait4 IS in std/posix 2.2.10 (handoff's 'absent' note was stale — no FFI struct needed; WCOREDUMP absent, bit 0x80 replicated). Dual-write carried as Rfc7Window parallel seq via execute(windowOut: ptr) — A1c should invert (see resume note). Wire enum strings = RFC plain names ('runner'/'timeout'/'signaled'), fixed post-A1a |
| A1c | consumers I: render, crisol.nim, order, junit, **protocol.nim** (reconcile → records-only predicate), api (re-export set ENUMERATED + `runResult`/`failureLine` digests), isQuarantined, retry mapping, Summary → derived `counts` array (legacy counters dual-counted until A1e-i) | ✅ d65b64c; CI 33675878742 green. Dependency inversion done: Outcome+HermeticLevel live in process/types, legacy EntrypointResult carries compile/run Phase pair, windowOut ptr + Rfc7Window + A1a scaffold EntrypointResult deleted; deriveOutcome/hasFailRecords operate on the production type (crisol/types). Bonus fix: cache-hit synthesize() now dual-writes honest pkCached (was deriving oSpawnError) |
| A1d-i | wire: run/v2 with COMPLETE v1→v2 field table (`cacheDecision` not `cacheMode`; `compileStats` rename; `interrupted`; `notStarted`; NO substrate key until A7), planview cold-start, 7 run/v1-pinning tests migrate | ✅ 7a89f7e; CI 33682085174 green. run/v2 (rev continues at 16), complete v1→v2 table in jsonout.nim header; compileStats rename; interrupted:false + summary.notStarted producers live; v1 lastrun = cold-start; durationUs per-phase (aggregate durationMs dropped); inputHash kept; fixture bug fixed suite-wide (hand-built results left Phase pair at pkSkipped — 3 tests were passing for the wrong reason) |
| A1d-ii | cache replay: resultcache stores ProcessResult+records; hit-path E2E pins `cause`/`evidence.tree`/`rusage` BYTE-EQUAL (not just outcome string); recomputed-not-pass ⇒ miss+rerun; pass-only store carried; cachedispatch, outcomestrings (ledger legacy-string compat), shard | in progress | ✅ a3ccf20; CI 33688311952 green. resultCacheFormatVersion 1→2 (CachedResult stores real ProcessResult via resultjson; v1/structural-bad entry = miss); replay = pkCached verbatim + deriveOutcome recompute at boundary (cdmRecomputeMiss, wire 'recomputeMiss', RunSchemaRevision 17); byte-equal E2E pins cause/evidence.tree/rusage cold-vs-warm |
| A1e-i | removal: fields + legacy enum values + legacy counters + coherence check; `deriveOutcome` → `outcome` rename; ~52 test files via SANCTIONED scripted sweep | in progress | ✅ b832ace; CI 33695334162 green. Legacy fields+oTimeout/oSignal+Summary counters+coherence machinery deleted; deriveOutcome→outcome (12 modules); cached(r)/flaky(r,policy) derived accessors; sweep = bracket-aware script (21 files) + hand-fix (9); test_outcome_window/test_c5_rss_jsonout deleted (subject gone); grep-test test_rfc7_legacy_names_gone.nim (outcomestrings legacy persisted strings = sole allowance); peakRssBytes + zero counts keys dropped off wire, no rev bump (no external v2 reader yet) |
| A1e-ii | interrupt partial results: emission set, `interrupted` marker, NO ledger rows, NO lastrun persist; `CrisolInterrupted` retired, `RunReport.interrupted`, onResult on killed finals, crisol.nim early-return dies; SIGINT E2E fully specified (marker-file sync, 5 assertions, serial) + SIGTERM ⇒ 143 case | in progress | ✅ db5ee39; CI 33701282507 green. CrisolInterrupted retired (both discard points dead); teardownLiveSlots(attribute=true) builds honest partials, krInterrupt stamped, onResult fires; emission = finalized-only + notStarted count; no ledger rows, no lastrun persist; SIGINT E2E 5 assertions + SIGTERM 143 in tests/timing/. Two real bugs found: poll-loop signal check ran before draining ready exits (misattribution race — reordered per §1) and Nim quit() saturates to int8 so 130/143 collapsed to 127 (exitnow bypass) |
| A1f | authorship breadth: crash/external/limit-with-precondition (SIGXCPU **and** fsize_overrun/lkFileSize, both ± requested), `term_cooperative` ⇒ killed (exit 0!), compile-interrupt ⇒ killed, coreDumped pinned false on default path | ✅ 43ddfe1 pushed; CI 33705238209 green. Producer fix: cbLimit was UNREACHABLE (empty Limits passed to classifyCause) — interimLimits() aggregate approximation wired (per-limit readback stays A2a-iii). All cases pinned incl. term_cooperative soundness (exit 0 ⇒ oKilled) and compile-interrupt. coreDumped NOT hardcodeable false: pipe-based core_pattern (systemd-coredump) sets WCOREDUMP despite RLIMIT_CORE=0 — test asserts against live /proc/sys/kernel/core_pattern |
| A2a-i | extraction: process/{types,posixcore,posix}.nim + process.nim ladder + shell process/linux.nim; spawn folded, `supervise` DELETED here; 5 spawn-importing tests migrate once; test_pgroup → conformance rewrite (test_signal/test_m6_teardown wait for A2b) | ✅ 60adb90; CI 33709442688 green. process/{posixcore(529),posix(75),linux(10)}.nim + process.nim ladder(26); §1 surface complete incl. groupRssBytes + shutdown wakeup; supervise deleted; 5 tests migrated via tests/support/spawnhelpers.nim; test_pgroup → requestStop/forceKill conformance (+ fixture sleep-loop bug fixed); spawn.nim stays as thin runner surface until A2b (forkExec/GracePeriodMs); ambiguities: direct forceKill records krTimeout (documented fallback), rlimit-pipe EOF ⇒ lsFailed, cooperativeUnavailable=false on POSIX |
| A2a-ii | conformance suite (9 items incl. **shutdown wakeup**), imports `process.nim` never a backend; honest note: runner not on Supervisor until A2b | ✅ 4593c74; CI 33711499981 green. tests/conformance/ 9 items (imports process.nim only, import-purity self-test); timing-gated cases in serial leg (shared grace: 3 children ~330ms vs 300ms window; shutdown wakeup ~210ms vs 10s deadline; kill 25ms after requestStop); seed file shrunk to double-reap Defect only; nimble/dev/ci.yml wired |
| A2a-iii | Limits re-home: enum-indexed Limits/LimitsAchieved, SandboxSpec sheds rlimits, **SandboxAchieved deleted**, `keys.nim` named (SoundnessKey fold-input change deliberate, free under format bump), config/api plumbing | ✅ e56da9c; CI 33714820056 green. SandboxSpec.limits = ptypes.Limits (RlimitConfig deleted); SandboxAchieved deleted (env/tmpdir by-construction; netIso ⇒ isFullyAchieved false; rlimits ⇒ per-limit LimitsAchieved via per-kind status-pipe bytes in spawn.nim); interimLimits deleted (no callers); SoundnessKey fold loop-driven, resultCacheFormatVersion 2→3 |
| A2d | cross-platform contract spike (N=3 **before A2b consumes the contract**): windows-latest skeleton compiles+smoke; macos-latest posix conformance subset | ✅ a8dfe5c+2 fixes; CI 33718516001 ALL FOUR legs green. windows-latest: skeleton compiles vs real Win32 (CreateProcessW+Job Objects) + smoke passes; macos-latest: full poll-fallback conformance subset green; NO §1 signature changes forced (contract froze clean). Findings: escalated-vs-cooperativeUnavailable wiring validated (first backend where reachable); BSD grep \s portability in fetch-deps; windows NTSTATUS deaths vary by console teardown (pin = partition invariant >= 0xC0000000, not one status) |
| A2b | runner on the contract: SlotState (#1), executor deadlines, ONE stop/escalate machinery (exception teardown shares machinery, records NOTHING); second-interrupt ⇒ skip-grace; memprobe/admission → `groupRssBytes(sv,id)`; test_signal/test_m6_teardown migrate; std/posix leaves runner | ✅ 96a0598; CI 33724921934 all four legs green. SlotState(ssIdle/ssLive)+ChildId replace pepIdx==-1; armExpiredTimeouts = sole krTimeout author, escalateExpired escalates any armed grace identically, finalizeSlot derives Cause purely from ReapReport.stop (never branches on why); exception teardown shares machinery, discards every report; second interrupt = weShutdown edge ⇒ immediate force; NEW race found+fixed: first weShutdown drains already-ready exits through normal completion before committing teardown (next() checks pendingShutdown before sweeping exits — child finishing just before SIGINT would be misattributed as killed); compile children on sv.spawn (no seam); spawn.nim DELETED (497 lines, zero callers; GracePeriodMs → runner); memprobe/admission → groupRssBytes(sv,id) at 25ms via next() deadline; forced scope: execute() gains installSignals/shutdownSignalOut (Supervisor owns sigaction; crisol/signals global-flag would race it), api.nim threads opts.installSignals, installSignalHandlers/clearSignal/pendingSignal dropped; shared-grace acceptance tests/timing/test_rfc0007_a2b_shared_grace.nim: 3 term_ignores interrupted together tear down in ~408ms (one window, not 3×) |
| A2c | cwd = projectRoot both compile substrates (#17) + 3-way acceptance + README | ✅ 4fb6652; CI 33730831175 all four legs green. Compile ChildSpec.cwd + run-child default = config.projectRoot (scratch tmpdir still overrides); second substrate aligned via realCompileOnlyIn/newMeasureDriver(workingDir) + MeasurePlan.projectRoot (authored by buildCompileWorkerPlan, consumed by measure worker); closure/depgraph cc -M header replay resolves against projectRoot via ccprobe.realRunIn; 3-way acceptance green (lib API unrelated cwd, CLI at root, CLI subdir via --config ../crisol.kdl) with genuine RED before fix (cannot open file: helper); README documents root-relative flags. Issue #17 closed |
| A3 | ioutils sole owner; grep test: zero std/posix outside process/ioutils/lock/signals + `# process-contract-exempt` markers | ✅ 38650b3; CI 33736788089 all four legs green. 5 posix offenders migrated (crisol.nim, depgraph, jsonout, ledger, shardedledger) + 4 osproc sites marked exempt with rationale; grep-test tests/unit/test_rfc7_a3_ioutils_ownership.nim RED-first; primitive set = 4 RFC-pinned (atomicPublish = renamed atomicPutFile) + closeFd/lastErrorString/createOverwrite/readRandomBytes/writeGuardedFile (real usage that fit none of the four — fifth-primitive rule, not exemptions); 3 fault-injection tests re-aimed at atomicPublish's PID-suffixed tmp naming (EISDIR occupation trick, PID-independent) so they stay non-vacuous; bonus: isatty → std/terminal, exitnow kept as raw libc importc (process-exit, not file I/O) |
| A4 | flock; `shutdownRequested(): Option[ShutdownSignal]` (signum only); handler↔Supervisor wiring (`initSupervisor(installSignals)`) | ✅ 25b39d2; CI 33740407097 all four legs green. lock.nim fcntl F_SETLK → flock(2) importc (not in std/posix; same pattern as RLIMIT_*), public API unchanged; RED-first regression proves the close-any-fd hazard on the OLD code (in-process helper open+close of the lock path silently dropped the fcntl lock — forked contender acquired; flock survives, contender blocked) + acquire/contend/auto-release-on-exit conformance suite stays green. Handler↔Supervisor seam was already A2b's (initSupervisor(installSignals), self-pipe → weShutdown, 128+n threading); A4's remainder = unification: shutdownSigHandler stamps a sticky volatile gShutdownSignum alongside the self-pipe write (one handler, two consumption models: edge-triggered weShutdown, level-triggered global), posix.nim exposes globalShutdownSignal(), signals.nim collapses to shutdownRequested(): Option[ShutdownSignal] delegating onto it; integration test proves ONE real SIGTERM observed identically by next()'s weShutdown and shutdownRequested(); stale-global regression in test_run_tests (fresh runTests() unaffected); A3 grep-test allow-list TIGHTENED (signals.nim no longer imports std/posix). Windows signals conversion explicitly D2 per RFC |
| A5 | rusage surfacing + per-limit `evidence.limits` CLI E2E (sampled group-sum REMAINS admission quantity) | ✅ 5371220; CI 33743713445 all four legs green. rusage was ALREADY on the wire (A1b producer reached resultjson/jsonout) — CLI pin passed immediately; the limits leg went RED and exposed the real gap: runner.toProcessResult built evidence from default(Evidence), silently discarding ReapReport.limits — fixed to Evidence(limits: report.limits) (killDomain/tree/escapees stay interim zero = A6a's job). Ledger: new maxRssBytes + rssMechanism ("wait4"; "" = unrecorded) columns via wait4MaxRss in appendAttemptRow; additive-NDJSON compat (old rows parse with honest defaults, no rowVersion bump); sampled group-sum rssBytes untouched as admission quantity. Per-limit statuses declared in rev 16 shape but never live — wired, documented as no-rev-bump wire-content-change in jsonout schema history. Tests: test_rfc0007_a5_rusage_limits_wire.nim (CLI E2E both legs), test_rfc0007_a5_ledger_maxrss.nim |
| A6a | escapee observation: killSnapshot at FIRST stop act (rssBytes populated), `escapees.len > 0` is the gate fact; fixture **spawn_grandchild** (same-pgroup, visible) drives it; spawn_grandchild_setsid pins honest `toUnobservable`-cacheable | ✅ eba223c; CI 33750230464 all four legs green. RED at CLI level proved the real regression: leaked same-pgroup grandchild was invisible to isFullyAchieved and got CACHED like a clean pass. killSnapshot was already captured at first stop act (posixcore requestStopCore/forceKillCore, rssBytes from /proc/pid/status) but never reached Evidence — toProcessResult now copies killDomain/tree/escapees/killSnapshot/cooperativeUnavailable verbatim from ReapReport; reapCore runs scanProcessGroup on EVERY reap (spawn_grandchild leaks while exiting 0, no kill involved); treeObservationFor(killDomain) total function (kdsProcessGroup → toUnobservable — honest label ties to MECHANISM not scan outcome, flips at B1 automatically). evidenceSatisfies(spec, ev) replaces isFullyAchieved in shouldStore (escapees>0 refuses; lsFailed refuses; lsUnsupported/lsNotRequested/lsApplied satisfy; tree deliberately not consulted — separate axis); shouldStore dropped redundant achieved param, reads runEvidence(res); [ESCAPEE] render warning + not-cached second-run E2E; setsid variant cached-with-label. Fixtures use marker files (crisol threads no argv). Flagged: Evidence.hermetic still unwired (no Stage A bullet owns it) — producer folded into A6b brief per no-deferral |
| A6b | `--strict-hygiene` as OutcomePolicy + RunOptions field + KDL key; CLI exit-1 on spawn_grandchild | ✅ addf324; CI 33755142663 all four legs green. outcome(r, policy)/flaky(r, policy) called with resolved policy at every REPORTING boundary (summarize→exit code, render, jsonout toJson + persistLastRun so --failed selection agrees with stdout, junit); shouldStore + live scheduling (retry eligibility, quarantine, ledger authorship) deliberately stay DefaultPolicy=unstrict per "cache stores/derives unstrict". Strict×cache: A6a already made escapee results uncacheable, so the only reachable interaction is same-observation-different-verdict — pinned (cacheDecision unchanged by flag while outcome flips). Config parity: Config.strictHygiene / KDL strict-hygiene / RunOptions / --strict-hygiene, opt-in-only-strengthen shape (config-true survives CLI-false, pinned); ResolvedSettings.strictHygiene = one policy for all sinks. CLI tracer both directions RED-first (flag didn't exist → usage dump). FOLDED-IN hermetic producer: toProcessResult gains hermetic param — run phase stamps spec.level, compile phases default hlNone (ord-0 weakest claim, compile has no hermeticity concept); wire already declared evidence.hermetic (A5-era) so purely a missing producer — pinned E2E for default (isolated) and --hermetic none. Both timing flakes (mem_throttle, max_jobs_overlap) green on isolated rerun, untouched subsystems |
| A7 | capabilities() per-file probes with EXPECTED VALUES PINNED per tier (an always-false probe fails here, not at B2); substrate in run/v2+plan/v1; killDomain per-spawn | ✅ 030f328; CI 33760087338 all four legs green. EVERY capabilitiesCore field was a hardcoded literal (incl. flock:true, wait4Rusage:true) — all now real probes: pidfd_open via raw syscall(2); subreaper PR_SET+PR_GET readback (left set, per §4 present-tense tier); cgroup delegation = live mkdir-leaf + cgroup.procs write (self in/out, rmdir; correctly EROFS in rootless dev container), cgroup.kill/memory.peak checked only inside a proved-writable leaf; flock LOCK_EX|LOCK_NB on tempfile; wait4Rusage = throwaway fork+wait4 checking the actual return. Memoised module-level (ccprobe pattern), parameterless capabilities() overload for Supervisor-free callers (plan path). run/v2 rev 16→18 + plan/v1 rev 3→4 gain platform-shaped substrate node (Linux keys only, no greyed-out placeholders). Tier pins: CRISOL_TIER=ci-linux env in ci.yml docker invocation (CRISOL_TIMING_TESTS convention) → pins pidfd/wait4Rusage/flock true; unset (rootless dev) → subreaper true, cgroupDelegation false — both arms verified live. Mutation sanity-check: hardcoding probePidfd()=false made the acceptance fail (pins are non-vacuous). killDomain audited: flows verbatim ReapReport→wire, honestly kdsProcessGroup (scanProcessGroup is pgid-only; subreaper elevation would be dishonest before B1's orphan adoption — reapCore comment updated). macOS/windows nim check cross-verified |
| A7-gate | (Corey-owned, non-TDD; blocks 0005 build): 0005 doc+handoff re-baseline (incl. `--verify-cache` over observations), amoxtli run/v2 green, CHANGELOG/notice (JSON + library break incl. `CrisolInterrupted` retirement + `RunV1Schema`→`RunV2Schema`), README sweep | ✅ executed 2026-09-03 on Corey's instruction, three parallel agents. (1) 0005 re-baseline 7c15e5c: StoredEntry = real CachedResult observation payload, outcome advisory + recomputed at every trust boundary, trust filters on named Evidence guarantees never ordinals, --verify-cache compares Exit+records never outcome strings, #1 SlotState prereq struck (landed A2b/96a0598), FORK-2 untouched — now the SOLE blocker before B3a. (2) amoxtli run/v2 green, amoxtli commit cbf9a76b pushed: crisol_json.nim v1 rev6 → v2 rev18 (real captured rev-18 doc embedded verbatim; v1 docs now a parse error), milpa pin + Containerfile CRISOL_REF → 58ad9db, full pure suite 1649/1649 via the NEW crisol binary; bump surfaced 4 genuinely misplaced pure tests (osproc closures the old incomplete closure tracking masked) — re-homed to tests/subsystem/; follow-up: rebuild runtime image at next refresh. (3) CHANGELOG/notice + README sweep b2945a7: verified corrections — RunV1Schema→RunSchema (unversioned, not RunV2Schema), resultCacheFormatVersion 1→3, oTimeout/oSignal deleted not renamed; README gap flagged (a dozen CLI flags never documented, pre-existing, separate sweep). **Stage A done + gate done ⇒ RFC-0005 build may start once Corey answers FORK-2** |
| B1 | subreaper (weOrphanReaped delivers `ownedBy`; late orphans logged at run level, never retro-fitted) | ✅ 5728246 + 7194e21; CI 34327304618 green all four legs. initPosixCore sets PR_SET_CHILD_SUBREAPER deliberately; reapCore's discoverAndReapEscapees finds same-pgroup + reparented (setsid) descendants, kills via pidfd_open+starttime-identity (pid-reuse-safe), bounded WNOHANG reap (never blocks the single-threaded loop); async waitid(P_ALL,WNOWAIT) sweep in nextEvent delivers weOrphanReaped, routed to a still-live owner's pending escapees or counted run-level (lateOrphansReaped, run/v2 rev 24). RunSchemaRevision 23→24. Three regressions found+fixed pre-merge (bisect+controlled A/B on test_rfc0007_a2b_shared_grace): unbounded blocking wait4 starving the loop; ppid==ownPid discovery scoped to RUN-phase reaps only (compile toolchain is contract-exempt); waitid sweep vs pollSweepChildren reap race for a registered slot's own exit (livelock) → sweep now reports a still-live registered pid as normal weChildExited. macOS conformance assertion made capability-driven (kdsProcessGroupSubreaper only when caps.subreaper). Fixtures: spawn_grandchild_setsid now tree=toComplete/escapees=1/uncacheable; new spawn_late_orphan + pass_after_delay (B1b, both weOrphanReaped destinations); test_rfc0007_b1_stat_parsing (starttime field 22). |
| B2 | pidfd + epoll-driven `next` with timerfd deadlines; 25ms tick retained only for memory sampling; poll fallback when pidfd absent (probe); conformance green under both (env-knob forced) | ✅ 0bb5f99; CI 34339945448 green all four legs. initPosixCore creates epoll+timerfd (registered EPOLLIN beside self-pipe) when probePidfd && CRISOL_FORCE_POLL unset, raises OSError on failure (no half-loop); spawnChild opens a pidfd per child (EPOLLIN), reapCore does EPOLL_CTL_DEL+close; nextEvent preamble (self-pipe drain, level-triggered re-report, pollSweepChildren, sweepAdoptedOrphan, deadline) untouched — only the final blocking step dispatches epollBlock vs pollBlock, and epollBlock NEVER inspects returned events so pollSweepChildren stays the single wait4-reap path (B1 race guarantee carries over, no double-reap). timerfd re-armed to caller deadline each pass; epoll_wait timeout capped 25ms for orphan-sweep/RSS cadence. CRISOL_FORCE_POLL forces poll path; conformance CI runs BOTH modes (new forced-poll pass in the test leg + ./dev wiring). Verified: a2b 10/10 both modes (~6.4s), b1b both modes, fd-leak test (RED-verified), latency 202ms epoll vs 227ms poll, macOS cross-compile clean. |
| B3 | delegated cgroup-v2, LEG-FIRST (canary CI job fails if delegation probe absent, BEFORE backend merges); `lkMemory` only when child's own Exit = SIGKILL ∧ oom_kill>0 (cgroup memory.events oom_kill); green-probe/failed-leaf fault-injection test | ✅ leg-first, GitHub `ubuntu-latest` `docker run --privileged` delegation (fork resolved: "try GitHub runners first"). Phase-1 (cgroup CI job establishing real cgroup-v2 delegation + fail-on-absent-`memory.peak`/`cgroup.kill` canary + A7 ci-cgroup tier pin) 05815ca/a60898a, CI 34374079270 green all five legs. Phase-2 backend 5254ea4 + fixes e8a30d0/cd24727/62fe2a2/**c208b62**; CI run **34404878358 green ALL FIVE legs** incl. `cgroup` (2026-09-09). Per-slot leaf: `createCgroupLeaf` (sibling of the resident cgroup — no-internal-process topology) then the CHILD self-joins `cgroup.procs` FIRST in its async-signal-safe window (parent-side write raced a fast child forking grandchildren pre-placement); a join-result byte flows back so a failed self-join sets `cgroupLeaf=""` → PER-SPAWN honest degrade to kdsProcessGroupSubreaper. reapCore reads leaf `cgroup.procs` survivors into `escapees` and `memory.events` oom_kill BEFORE `cgroup.kill`, then reapBounded each survivor (subreaper reparenting reaps them) and removeCgroupLeafBounded. `lkMemory` Cause only when child-own Exit=SIGKILL ∧ oom_kill>0; memory.swap.max=0 alongside memory.max so RSS pressure OOMs instead of swapping. killDomain=kdsCgroup⇒tree=toComplete. Fault-injection sabotage: THREE empirically-disproven forms first (regular file — cgroupfs rejects O_CREAT; pids.max=0 — the pids controller gates FORKS not MIGRATIONS so a self-join is admitted; +cpu-only on an empty leaf — the constraint is enforced at ENABLE time, not migrate-in, and cpu didn't reject). WORKING form: turn the leaf INTERNAL (a child cgroup + `+memory` in its own subtree_control) so the no-internal-process rule rejects the child's `cgroup.procs` self-join with EBUSY — structural, root-unbypassable, name-specific (a clean sibling leaf still works → per-spawn). Verified end-to-end against a --privileged cgroup-v2 container (kernel 6.12) before the final push. |
| C1a | posix green on macos leg (full unit+conformance via poll fallback) | ✅ 6df6d26+fa958d6+f5cb84f+24257c1; CI 34423297420 green all 5 legs, macos OK:115 (2026-09-09) |
| C1b | process/darwin (kqueue EVFILT_PROC next + libproc forensics; ladder macosx→darwin; kqueue cap) | ✅ dee7370+80f18dd+0b72a3f; CI 34431503261 green all 5 legs (2026-09-09) |
| D1a | windows core: CreateProcess+Job spawn, inherited sink handles, IOCP next, uniform sweep reap, ekNtStatus symbol table, snapshotTree forensics + **access-violation fixture (ekNtStatus producer proof)** | ✅ 6b78a87+4e30a68+1ba4dc2 (+702c40d/34c6727 control-loop fixes); CI 34437972869 green all 5 legs (2026-09-10) |
| D1b | kill domain + cooperative stop (3 sub-slices): D1b-i CTRL_BREAK deliverability probe + coop/detach fixtures; D1b-ii Job cpu/AS limits + parent-computed LimitsAchieved; D1b-iii ppid via Toolhelp32Snapshot + breakaway-containment proof | ✅ c2b7c7c + 9c5ffad/50b55dc + f208fbc (+ forensics-flake fix b229dc2); CI 34440588672/34444943333/34447537041 green all 5 legs (2026-09-10). Known limitation tracked: no cbLimit attribution on windows (needs IOCP job-msg decode + classifyCause param — awaits sign-off) |
| D2a | Windows production de-POSIXing (6 sub-slices): signals-getter+groupRssBytes / lock LockFileEx / memprobe GlobalMemoryStatusEx / ioutils CRT int-fd backend / httpraw winsock timeouts (+reap runPhase parity) / CLI-liveness proof — `crisol run --json` compiles+runs on windows | ✅ 3e9760f+a519664+07d6696+f8c0ba4(+08dbc89)+db6e723+a7325e9; CI 34450315559…34471882898 each green all 5 legs (2026-09-10) |
| D2b | test-suite de-POSIX (~73 tests/** files, RFC's "35" stale) + windows-latest FULL conformance leg + full-suite windows green | ✅ **CLOSED via RFC-0009 B4b** (CI 35170572732, 2026-09-16) — "full-suite green remains RFC-0009's acceptance" satisfied; no longer the escalation boundary. |
| D1c | cbLimit attribution on windows (signed off 2026-09-18): IOCP `JOB_OBJECT_MSG_END_OF_PROCESS_TIME` decoded as ANNOTATION only (`ReapReport.limitKilled: Option[LimitKind]`; sweep still solely decides liveness); additive `classifyCause limitKilled` param mirroring `memoryOomKill` (join with requested+achieved; no-join ⇒ cbExternal; stop-act still wins); `reap` drains the port (timeout 0) pre-report to close the sweep-beats-message race; per-pid guard (same-Job grandchild never annotates the child); `lkAddressSpace` deliberately NOT attributed (PROCESS_MEMORY_LIMIT = failed alloc, not a kill — attribution would fabricate authorship); scope-narrowing from the sign-off note recorded in the RFC bullet | done (05a90cb): 5 unit table rows GREEN, cpu_spin conformance asserts `limitKilled==some(lkCpu)` + `cbLimit(lkCpu)` — proven on the windows CI leg, all 5 legs green (run 35353539907) |

## Wiring audit (stage 3.5, 2026-09-18)

Three read-only fable lenses (result-model pipeline; process substrate; claims-vs-code), consolidated and control-loop-verified against the code.

**Contract verdict:** the load-bearing property IS live end-to-end — `tests/integration/test_rfc0007_a1b_kill_path.nim:52-101` drives the real entry (`runMain(@["run",…,"--json"])`) on a hanging fixture and asserts `outcome=="killed"`, `cause {by:"runner",reason:"timeout"}`, honest SIGTERM-in-grace and SIGKILL/`escalated:true`; the cooperative-exit-0-still-killed soundness case pinned at `test_rfc0007_a1f_authorship.nim:310-316`. Outcome recomputation at every trust boundary holds (cache stores observation only, `resultcache.nim:26-29`; serve/store recompute `cachedispatch.nim:412-437`/`:639`); the executor-never-sees-Pid seam holds; `evidenceSatisfies` live on serve+store; interrupt contract (i1/i3) and D1c verified in code. The audit found dark mechanisms (w1–w4 below), so `wiring` stayed unproven until the gaps closed. **W1–W3 landed 2026-09-18** (w1: spawn gate + lsUnsupported degradation on `cgroupTierUsable`, unit truth-table + cgroup-leg tracer; w2: `--rlimit-cpu/as/fsize/core` + `--limit-memory` CLI/config → `resolveSandbox`, SIGXCPU-on-the-wire tracer; w3: stdout gains compileStats/reuseAlerts, lastrun.json gains real substrate/trackedRoots, symmetry E2E); fast-confirm re-audit verified both sides of each wire in the landed code; `wiring = proven` set with CI runs 35386987729 + 35392078295 as warrant. w4 remains open (L, deliberate).

| id | sev | kind | finding | evidence (both sides) |
|----|-----|------|---------|-----------------------|
| w1 | H | 2 | `Capabilities.cgroupKill` probed, never consulted: delegated host w/o `cgroup.kill` (kernel <5.14 — RHEL8, 5.4/5.10 LTS) ⇒ forced kill is a silent no-op (`killCgroupLeaf` swallows the write failure, `killpg` deliberately skipped) while reap still stamps `kdsCgroup`/`toComplete` | probe `posixcore.nim:1669`; gate consults only delegation `posixcore.nim:696`; kill arm `posixcore.nim:1373-1381`; swallow `posixcore.nim:273` |
| w2 | H | 6/5 | cbLimit attribution chain (posix SIGXCPU, B3 `memoryOomKill`→`lkMemory`, windows D1b-ii/D1c `lkCpu`) unreachable from every entry point: only `rlimit-nofile` is plumbed (`rlimitOverridesFrom` sets `limitNofile` alone; RunOptions carries no bundle; no `lkMemory` surface exists) — deferrals buried in comments `api.nim:845`, `process/types.nim:96` | overrides honored `sandbox.nim:161-168`; sole production construction `api.nim:847`; config `config.nim:856` |
| w3 | M | 2/7 | run/v2 surfaces diverge complementarily: stdout emission never passes `compileBlock`/`reuseAlerts` (schema doc `jsonout.nim:74` says ALWAYS PRESENT; zero refs in crisol.nim), `persistLastRun` never receives `substrate`/`trackedRoots` yet emits zero-value defaults as probed facts (contradicts `jsonout.nim:1066` "matches stdout exactly") | stdout call `crisol.nim:1241-1250`; persist signature `jsonout.nim:1053-1060`; defaults `jsonout.nim:776,781` |
| w4 | L | 5 | `memory.peak` probed+serialized, no consumer: §7's tagged-successor ledger accounting never landed; `ledger.nim:96` awaits "a future producer" | probe `posixcore.nim:1670`; deferral `posixcore.nim:1543-1547` |

**Fixed inline this session (stale-claim batch, doc-only):** §3 table + §5 + B3 bullet corrected to shipped design (child self-join not clone3; `memory.max` = separate `lkMemory`, `RLIMIT_AS` stays; `memory.peak` probe-only); A3 allow-list wording matched to the enforced list (`httpraw`/`paths` additions, `signals` dropped); `linux.nim` shell doc; `jsonout.nim` rev-history hermetic note marked superseded (A6b landed the producer); `types.nim` `notStarted` "always 0" note updated. Checked-and-true (not re-litigated): backend tier selection default-on with no dark config knobs; cooperative-stop symmetry; escapee/killSnapshot/tree production producers; signals wiring; A1b/A1e/B3/C1b/D1b test bullets each assert what they claim.

**Out-of-scope note:** w3's stdout half predates RFC-0007 (RFC-0006 M-report era) but is a live run/v2 trust-boundary divergence, homed here with the audit that found it.

## Open forks (awaiting Corey)
- **Un-defer the review? DECIDED 2026-09-18:** Corey proceeded on the recommended path (post-compact "continue") — review un-deferred, stage 4 round 1 launched. Historical context: `review = deferred` reason "pending RFC-0009" was spent once RFC-0009 closed; Corey had deferred the decision until after the wiring audit. **2026-09-18 decisions (Corey):** (1) ledger rows i1/i2/i3 (the three round-2 amendments below) RATIFIED — resolved in the fence with implementing commits cited; (2) cbLimit attribution on windows SIGNED OFF — build now as slice D1c (IOCP `JOB_OBJECT_MSG_END_OF_PROCESS_TIME` decoded as an annotation + additive `classifyCause limitKilled` param, mirroring `memoryOomKill`); (3) `review` stays `deferred` until after the stage-3.5 wiring audit — un-defer question re-raised then.
- Historical record: **A1f spec-vs-reality note (flag, not fork):** the RFC bullet says "coreDumped pinned false on default path" — empirically false on hosts with a pipe-based core_pattern (systemd-coredump): WCOREDUMP reads true despite RLIMIT_CORE=0 (core(5): pipe handlers bypass the limit). Shipped behavior: honest observation, test pins against live /proc/sys/kernel/core_pattern. The RFC's A1f bullet text should be amended to match at the next doc pass. **Three round-2 amendments flagged for veto, not forks** (recommendations applied per no-fake-forks):
  1. **Interrupt never persists `lastrun.json`** (stdout run/v2 with `interrupted:true` only). Invariant: a never-observed entrypoint must not silently leave the `--failed` selection; merging two runs' entries into one document fabricates a report no run produced. Veto ⇒ the merged-persist variant (this run's observed + prior entries for unobserved) is the fallback, and the A1d-i field table gains per-entry provenance.
  2. **`lsUnsupported` ⇒ cacheable-with-label; `lsFailed` ⇒ uncacheable** (§6). Extends the locked `toUnobservable` posture to limits; without it, any Windows config requesting `openFiles` permanently loses caching. Veto ⇒ strict RFC-0004 reading (requested-not-achieved always refuses).
  3. **Round-1's interrupt partial-results amendment stands** (unchanged from round 1, still veto-able as documented there; now fully specified — emission set, facade contract, no-ledger, no-persist).

## Key decisions (round 2, 2026-08-23 — applied to the RFC)
- **Authorship gets ONE owner: the Supervisor's act ledger.** `ReapReport.stopRequested`/`escalated` → `stop: Option[(reason, escalated)]`; `requestStop`/`forceKill` record atomically against exit observation (already-observed ⇒ no-op recording nothing; first act wins); `cbRunner` ⇔ `stop.isSome`; `next` drains ready exits before `weDeadline` (spec-level race narrowing). `escalated` defined observably (forceKill recorded before exit observed; SIGTERM+escalated:true corner accepted; vacuously false under cooperativeUnavailable).
- **reap's one-report promise made true:** `killDomain`, `LimitsAchieved`, `cooperativeUnavailable` ride in ReapReport (they had NO carrier); SpawnResult slims to id-or-error; Evidence copies backend fields verbatim; `Evidence.sandbox` → `hermetic: HermeticLevel` (SandboxAchieved deleted — object of constants).
- **Weakest-claim-at-ord-0 house rule:** TreeObservation reordered (`toUnobservable` first), LimitStatus gains `lsNotRequested` first — a default-initialized evidence value can never encode a vouch; plus an interim-population table so no dual-write-window serialization fabricates evidence.
- **TreeObservation is TWO values (observability only); `escapees.len > 0` is the separate cache-gate fact** — resolves the §6/B1 contradiction (B1: toComplete WITH reaped escapees, still uncacheable) and the A6a fixture blindness: new `spawn_grandchild` (same-pgroup, visible) drives escapee tests; setsid fixture pins honest `toUnobservable`; pgid-only tiers may never claim toComplete.
- **Limits unified:** one `LimitKind` enum (5 kinds + B3 lkMemory), `Limits.req`/`LimitsAchieved` enum-indexed arrays (no parallel-field transposition, loop-driven readback); `DeterministicLimits` constant; `lkMemory` asserted only when the child's own exit is the OOM SIGKILL.
- **Two pure functions, both named:** `classifyCause` joins `outcome` in A1a with its own pinned table. `OutcomePolicy` object replaces the bare bool. Dual-write window naming: born `deriveOutcome`, renamed at A1e-i (the `r.outcome` field/proc silent-bind trap).
- **Sampler/forensics split:** ninth proc `groupRssBytes(sv, id)` is the 25 ms sampler; `snapshotTree` is kill/reap forensics only (0005-signed) — the wire type never feels sampler pressure; memprobe's pgid scan becomes the posix backend's impl (A2b); RFC-0002 bullet added (quantity/cadence unchanged).
- **Supervisor lifecycle contract written:** non-copyable, =destroy kills nothing, init can fail, `installSignals` ownership seam, ChildId monotonic never-reused, misuse ⇒ Defect, unkillable D-state child ⇒ slot stays live, NO fabricated Exit. weShutdown = edge per delivery; second interrupt ⇒ skip grace. posixcore sharing mechanism specified (Nim has no partial module override — C1b is a thin delegation module).
- **Interrupt fully specified:** emission set (last-started phase rule + `summary.notStarted`), `interrupted:true` wire marker, never-ledgered, never-persisted, facade contract (`CrisolInterrupted` retired, `RunReport.interrupted`, onResult fires), SIGINT E2E pinned non-vacuous + SIGTERM 143.
- **Cache honesty tightened:** pass-only store stated; recomputed-not-pass hit ⇒ miss+rerun; the "different verdict under different policy" claim demoted to future-mechanism (vacuous today, said so); unknown-enum ⇒ structural failure for crisol's own readers; compile pkCached documented unreachable.
- **Slices re-cut for blast radius (measured):** A0 CI-first (meta-test + serial timing job); A1d → i/ii; A1e → i/ii (scripted sweep sanctioned); A2a → i/ii/iii (keys.nim coupling named); **A2d moved before A2b** (spike validates the contract against N=3 before the runner rewrite consumes it); supervise deletion A1b → A2a-i (five test harnesses call it — one migration, not two); test list corrected (5 spawn importers; test_signal/test_m6_teardown → A2b); A7 code vs A7-gate (Corey-owned) split; D2a/D2b noted.
- **Liveness pins added:** A1b keeps `--json` surface via additive run/v1 `exit`/`cause` (load-bearing property on the real wire from slice one); dual-write coherence check; A1d-ii byte-equal replay pins; A7 probe expected-values; B3 fault-injection (green-probe/failed-leaf); D1a owns the ekNtStatus fixture, D1b the DETACHED_PROCESS fixture; run/v2 ships with NO substrate key until A7.
- **Smaller calls:** StdioSink collapsed to one combined path + `combinedSink` ctor (split arm had zero producers); `ShutdownSignal.reason` dropped (one-value enum); `cached`/`flaky` fields struck (derived; `flaky(r, policy)`); Summary = derived counts array; `protocol.nim` named in A1c (`hasFailRecords` defined, truncated-stream conservative); Windows 0xC0000000 partition documented as heuristic; hlNetwork uncacheable until 0008 stated; records read on every run end; retry compile/run external-kill asymmetry acknowledged-not-changed; RFC-0001 5s-grace + interrupt-sink-deletion prose amendments listed; exception teardown records nothing (no fake KillReason).

## Key decisions (round 1, 2026-08-23 — applied to the RFC)
- **Contract reshaped: `Supervisor` object owns the event loop** (epoll/kqueue/IOCP need registration state a free `wait()` has nowhere to keep — N=1 freeze); `ChildId` tokens; `requestStop`/`forceKill` **non-blocking** (grace = executor deadline; N slots tear down in ONE shared window); `reap` returns one `ReapReport` of backend observations; runner authors `Cause` from recorded acts (round 2 moved the ledger into the Supervisor). `WaitEvent` defined (level-triggered; `weShutdown` in the wait set; `weOrphanReaped` for B1). Real modules + per-platform `nim check`, not `include`.
- **Authorship rule made precise:** `cbRunner` iff stop requested before an exit was observed — `term_cooperative` (traps SIGTERM, exits 0) is `oKilled`, closing the trap-SIGTERM-to-fake-a-pass soundness hole. Signal classification for unsent signals = documented heuristic with named misattribution windows; `cbLimit` only when the limit was requested+achieved.
- **Load-bearing E2E corrected:** `hang_forever` dies on SIGTERM in grace — the old pinned `SIGKILL` was today's *synthesized* value (killAndReap discards wstatus; pollSlot fabricates SIGKILL — verified). Producer = killAndReap/reapBlocking rework; `spawn.supervise` is dead code (zero src callers — verified) and is deleted (round 2: at A2a-i).
- **Result encoding: `Phase` variant** (pkSkipped/pkSpawnFailed/pkRan/pkCached share `res`) replaces Option×Option×spawnError-string — fixes the cache-hit-derives-to-oSpawnError contradiction and the unrecoverable-phase spawn error. `outcome(r, policy)` total case expr; compile-interrupt ⇒ oKilled; policy explicit; JSON outcome advisory, recomputed at trust boundaries.
- **Dark values cut:** `krMemoryPressure`, `krBudget`, `Limits.processCount`, `KillDomainRequest`, `Evidence.outputComplete`, `nearAddressSpaceLimit` (B3 adds honest `lkMemory` instead). Enums serialize as strings, readers unknown-tolerant ⇒ additive growth without dead freeze.
- **`Exit` purified:** `symbol` and `rusage` out (symbol = proc over table; rusage = `Option[Rusage]` on ProcessResult via wait4 — never zero-filled).
- **Evidence honesty:** tree observation honest about "cannot see" — no fabricated `treeReaped:true` on blind tiers; unobservable = cacheable-with-label; `killDomain` = per-spawn achieved, a label not a ladder; `evidenceSatisfies` = named guarantees, never enum ord; `cooperativeUnavailable` keeps Windows `escalated` meaningful.
- **ChildSpec cleanup:** Limits single home (+`core`); SandboxSpec sheds rlimits; runner resolves env/tmpdir/cwd (achieved-by-construction); StdioSink structural + stdin invariant; one cwd mechanism.
- **A5 rusage swap rejected:** wait4 maxRss ≠ memprobe group-sum — sampled sum stays the admission/ledger quantity; wait4 value is a new tagged column; cgroup `memory.peak` (B3) is the tagged successor.
- **osproc third spawn path scoped out explicitly** with `# process-contract-exempt` markers grep-asserted in A3; A2c aligns realCompileOnly's workingDir deliberately.
- **B3 leg-first** (delegated-cgroup CI job with fail-on-absent-delegation canary — the #12 lesson); missing consumers added in A1c/A1d (api, Summary, outcomestrings, shard, ledger compat, retry mapping).
- Prior locked calls unchanged: guarantees-as-identity, no network enforcement, escapee verdict untouched, flock, event wait for uniformity, #18 shape, RFC-0008/0009 charters.

## Verified-in-source claims (don't re-derive in later rounds)
- `spawn.supervise`: zero callers in src/ but FIVE integration-test harnesses call it (test_scratch_tmpdir ×6 sites, test_env_scrub_integration ×2, test_rlimits_safe, test_rlimits_timing, test_sandbox_achieved) — hence deletion at A2a-i. `killAndReap`/`reapBlocking` discard wstatus on all paths (runner.nim:218-241); `teardownLiveSlots` discards at runner.nim:265/279/282; `pollSlot` synthesizes SIGKILL. std/posix importers in src/ = ten; in tests/ = 35 files (incl. 2 fixtures). 11 src/ modules read `.outcome`; ~52 test files pin old fields/strings; 13 `EntrypointResult(` construction sites in runner.nim; 7 test files pin `"crisol/run/v1"`; wire field is `cacheDecision` (jsonout.nim:304), not `cacheMode`. Today's cache stores PASSES ONLY (cachedispatch.nim:270-283) and CachedResult stores outcome/exitCode/signal/durationMs/records only (resultcache.nim:88-98). Interrupt today: `CrisolInterrupted` raised (runner.nim:1128-1130), api.nim:698-706 returns empty report, crisol.nim:995-996 returns before JSON write. Signaled path skips sink reconciliation (runner.nim:894-899). `wait4` absent from std/posix (importc needed). `hasFailRecords` does not exist yet. runner.nim 1637 lines / spawn.nim 580.

## Review ledger (stage 4)

Round 1, 2026-09-18. Seven fable lenses (posix substrate, windows substrate, runner/executor, result model/wire/cache, security, design, liveness) + six sonnet adversarial verifiers on the Highs. All six behavioral Highs CONFIRMED; flock-symlink downgraded High->Medium (protected_symlinks blocks default Linux; real on macOS shared-/tmp). Verifier evidence in the finding rows. Status legend: open | fixed | deferred | wontfix | refuted.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| r1 | H | macOS ru_maxrss unit: decodeRusage *1024 unconditional but Darwin reports BYTES -> 1024x-inflated maxRssBytes vouched on wire+ledger (posixcore.nim:1149; universal wait4 reap path, kqueue included; no downstream correction; no test pin) | fixed | maxRssBytesFrom(raw, darwin) pure helper, darwin arm unscaled; test_rfc0007_r1_maxrss_units.nim pins both arms on linux (RED: helper absent pre-fix) |
| r2 | H | Escapee filter cross-slot kill/misattribution: livePids = other slots' leader pids only; ppid==ownPid arm admits a still-running test's daemonized descendant -> another slot's reap SIGKILLs it and stamps it into the WRONG slot's escapees (posixcore.nim:1512-1522; no jobs>1 escapee test exists) | fixed | pgrp-in-livePids exclusion (slot pgid == leader pid by setpgid); residual both-setsid window documented as accepted; test_rfc0007_r2_cross_slot_escapee.nim jobs=2 deterministic marker-sync, RED 3x/GREEN 3x; linux-gated (subreaper tier) |
| r3 | H | Library embedding: unconditional PR_SET_CHILD_SUBREAPER (never cleared on destroy) + waitid P_ALL sweep consumes host's own children's exits (host waitpid -> ECHILD) + ppid arm SIGKILLs host's unrelated workers (posixcore.nim:574-582,1181-1269,1509-1560; amoxtli = live consumer; no carve-out documented) | fixed | preExisting /proc snapshot before subreaper set, excluded from sweep (skip-no-consume) + escapee kill; subreaper cleared on destroy (subreaperSet); host wait(-1) inverse limitation documented; test_rfc0007_r3_library_embedding.nim (conformance, linux-gated) RED both halves independently |
| r4 | H | Windows escapees always @[]: contract says "survivors observed at kill/reap time" (types.nim:425, RFC "even where escapees were then reaped"); leaky test caches as CLEAN on windows, uncacheable on linux; KILL_ON_JOB_CLOSE silently kills survivors post-report; queryJobPids(hJob) available pre-close (windows.nim:1095) | fixed | jobSurvivors(queryJobPids minus own pid, WaitForSingleObject liveness check) before hJob close; test_windows_escapees.nim + leaky_child.nim; RED unverifiable locally -- windows CI leg is the proof |
| r5 | H | Interrupt drain spawns run child post-SIGINT: drain runs with shuttingDown=false -> allowTransition=true -> transitionToRun spawns then kill-arms krInterrupt; RFC names compile-done-run-unstarted as honestly-OMITTED (fkOmitted unreachable from this window); post-compile consult lacks the shutdownRequested() guard the plan-time loop has; PLUS second Ctrl-C during drain downgraded to first-interrupt grace (runner.nim:2342-2359,541-762) | fixed | drain calls handleChildExited(blockTransition=true) -> compile successes take fkOmitted, no post-SIGINT spawn; second weShutdown during drain -> immediate forceKill (426ms->** <250ms measured); test_rfc0007_r5_drain_interrupt.nim RED both halves |
| r6 | H | run.durationUs includes compile time for compiled-this-run entries: slot.t0 set at compile spawn, transitionToRun never resets -> run Phase stamps compile+run (edRunFresh correct -> same field means different things); contradicts jsonout.nim:193-197 sum claim; contaminates cache stores, hit-replay durationMs, wallSavedMs telemetry, shard-balance history (runner.nim:596,794,1200,1357-1388) | fixed | transitionToRun resets slot.t0; test_rfc0007_r6_run_duration.nim RED (runUs 7.35s > compileUs) -> GREEN (runUs < 1s); compile Phase durationUs unaffected (stamped pre-transition) |
| r7 | H | Design: execute() reports run-level facts through five raw ptr out-params (memThrottled/interrupted/notStarted/shutdownSignal/lateOrphansReaped); forgotten addr = silently lost fact; already bit failFast path (r33) -> return one ExecuteReport object (runner.nim:1481-1531, api.nim:1534-1554) | fixed | ExecuteReport object, five ptr out-params deleted; single epilogue construction (n==0 guard = honest zero object); failFast break falls through the same epilogue; documented narrowing: memThrottled exception-path value retired (grep: no consumer) |
| r8 | H | Design: toJson/toJsonString/persistLastRun triplicate a 15-param defaulted signature; forgotten param = wrong-but-valid document; ALREADY caused the shipped W3 defect -> single RunDocument assembled once (jsonout.nim:801-815,1054-1068,1087-1096) | fixed | RunDocument record assembled once in runTestsWith, both sinks consume it (toJson stays the flat pure primitive); sink differences now code (persistLastRun self-guards interrupted, hardcodes no-knobs); stdout+lastrun byte-identical pre/post (normalized rusage/duration only); RunReport.doc additive |
| r9 | M | flock probe symlink-follow truncation: open(fmWrite) O_CREAT|O_TRUNC no O_EXCL/O_NOFOLLOW on predictable /tmp/crisol-flock-probe-<pid>; blocked by fs.protected_symlinks on default Linux; REAL on macOS shared-/tmp and hardened-off Linux; violates repo's own exclusiveCreate posture -> route through ioutils (posixcore.nim:1750-1763) | fixed | probe via ioutils.exclusiveCreate (O_EXCL|O_NOFOLLOW) + 16 random bytes suffix, defer-unlinked; test_rfc0007_r9 RED-genuine (pre-planted file at old fixed name survives untouched, flock still true) |
| r10 | M | cgroup forced-kill escapable + no killpg backstop: same-uid child can leave leaf (delegation common-ancestor rule) or runtime write can fail; forceKill cgroup arm skips killpg entirely, swallows failure, reap still vouches kdsCgroup/toComplete -> add killpg backstop + downgrade vouch on non-resident pid (posixcore.nim:753-761,1427-1435) | fixed | killCgroupLeaf returns bool; killpg(SIGKILL) backstop always follows cgroup.kill; cgroupKillWriteFailed degrades killDomain via pure killDomainFor (truth-table pinned); KNOWN GAP: kill-time write-failure fault injection not reproducible on the CI cgroup leg (only leaf-creation sabotage exists) -- decision logic + backstop reachability proven, combination under real write failure unpinned |
| r11 | M | Unbounded blocking read in spawnChild readback; SA_RESTART makes it SIGINT-immune -> stalled pre-exec child wedges the Supervisor unrecoverably (posixcore.nim:879-903,571) | fixed | readPipeBounded poll(2) helper (SA_RESTART-immune), 10s deadline, timeout -> SIGKILL pgid + bounded reap + spawn error; test_rfc0007_r11 pins fast path + injected-deadline timeout |
| r12 | M | cgroup-tier killSnapshot uses pgid-only scanProcessGroup though cgroupLeafSurvivors exists -> setsid escapee visible to the tier is absent from killSnapshot while tree=toComplete (posixcore.nim:1416,1426) | fixed | killSnapshotFor prefers cgroupLeafSurvivors when leaf exists, pgid-scan fallback; both arms pinned via fake cgroup.procs file (no delegation needed) |
| r13 | M | queryJobPids treats ERROR_MORE_DATA as @[] -> Job >256 procs loses ALL forensics (snapshotTree/killSnapshot empty) and groupRssBytes=none, blinding admission on the largest trees; contradicts types doc 205-209 (windows.nim:922-931) | fixed | ERROR_MORE_DATA retry sized from numberOfAssignedProcesses (+64, cap 65536), partial never regressed; compile-checked (257-proc case impractical in CI) |
| r14 | M | Per-slot temp dir leaked on every NORMAL completion (cleanupSlotTmp removes files only; removeDir exists only on shuttingDown path); one dir per entrypoint per run (runner.nim:1389-1402,1913) | fixed | cleanupSlotTmp (single choke point) now removeDirs the dir; dead shuttingDown duplicate removed; test_r14 RED (leaked 2/4 dirs) -> GREEN zero |
| r15 | M | Event loop busy-spins from forceKill until exit observed (past stopDeadline stays in nextDeadline; weDeadline returns immediately) -> unbounded 100% CPU vs D-state child (runner.nim:420-454) | fixed | pure slotWakeDeadline(SlotWakeInfo, floor) excludes forceKilled slots from the min (both main loop + teardownDiscard); test_r15 4-case truth table RED->GREEN |
| r16 | M | No try/finally around post-execute tail (drainPending, perf-check scans, persistLastRun, aggregateCacheStats) -> expected env failure (disk full) propagates AND leaks advisory lock in library embedding (api.nim:~1568-1759) | fixed | whole post-execute tail in try/finally releaseLock (idempotent); persist failure = best-effort warning returning the report (facade contract api.nim:14 cited -- persist failure is not a structural run problem); root cause: unguarded stderr.write inside persistLastRun except-branches; test_rfc0007_r16 RED both signatures (raise + leaked-lock rsStructural on 2nd run) |
| r17 | M | cdmRecomputeMiss never reaches final wire: failing rerun stamps verdict.decision=keyMiss alongside cacheLookup:"ok" -- internally contradictory pair; rev-17 distinction dead in production (runner.nim:2092-2105, cachedispatch.nim:639-643) | fixed | store-gate not-stored branch preserves plan-time cdmRecomputeMiss (narrowly -- genuine keyMiss untouched); test_r17 real-cache E2E (evidence-injected escapee forces recompute-invalidation, failing rerun) RED keyMiss -> GREEN recomputeMiss+cvOk |
| r18 | M | MinSafeRlimitAs warning declared-not-implemented: doc promises spec-resolution warning; none exists; --rlimit-as small => every child SIGSEGVs pre-main reported as test crash with zero guidance (sandbox.nim:117, api.nim:1448) | fixed | stderr warning at the single resolveSandbox production call site (api.nim runTestsWith), resolved-value gated (hlNone never warns); test_rfc0007_r18 RED via stash |
| r19 | M | --hermetic network: live CLI arm for a never-goal -- no isolation applied, wire vouches "network", evidenceSatisfies silently disables ALL caching, no warning (crisol.nim:908, types.nim:93,146) | fixed | planImpl raises cekConfig for hlNetwork (only producer -- no KDL hermetic key exists); RFC sanctions cache-refusal not acceptance (checked, no blocker); enum kept for RFC-0008; test_rfc0007_r19 6/6 incl. library path |
| r20 | M | SandboxSpec.chdirIntoScratch: load-bearing consumer (runner.nim:1263), ZERO product producers -- no RunOptions field, no config key, no flag (sandbox.nim:145) | fixed | full producer chain (RunOptions/Config/KDL chdir-into-scratch/CLI flag/merge/resolveSandbox); A2c projectRoot default pinned intact; test_rfc0007_r20 RED via case-arm rename |
| r21 | M | resolveSandbox(passthroughs) env-allowlist extension unreachable from any entry point; --env-pin abused as stand-in with different soundness-key semantics (sandbox.nim:27,143) | fixed | producer chain + dedup union (envPassthroughsFrom); NO keys.nim fold needed -- passthroughs already hash via envAllowlist->hermeticEnvHash (empty-case byte-identical + value-change-changes-hash both unit-pinned); test_rfc0007_r21 7/7 |
| r22 | M | Capabilities.jobObjectNesting: expensive real probe (spawns a process), zero consult sites; nesting-incapable host => N x oSpawnError instead of one structural degrade -- same shape as closed w1 (windows.nim:397,487,628) | fixed | initSupervisor consults cachedCapabilities().jobObjectNesting, raises structural OSError (no-half-loop posture) |
| r23 | M | handleChildExited = ~300-line template inlining retry/ledger/promotion/store-gate policy into the event loop; pure decision logic untestable except E2E (runner.nim:1817-2112) | fixed | ExitDecision + pure decideExit(facts) extracted; dispatch loop applies decision fields as plain I/O; cacheDecisionIfNotStored hardened (explicit not-closureRecorded guard); 10-arm table test_rfc0007_r23_exit_decision 10/10 (afe45c2) |
| r24 | M | Param-list growth: finalizeSlot 14 params, spawnCompileStable 10; run-lifetime invariants re-threaded per call -> ExecCtx object (runner.nim:541-556,996-1007) | fixed | ExecCtx (config/cache/plan/recordClosureFn/budgets/projectRoot/toolchainFp/dupSlugs) built once in execute(); finalizeSlot 14->9, spawnCompileStable 10->7; mutable per-call state stays explicit (afe45c2) |
| r25 | M | Slot-claim reset manual+triplicated (~20 assignments x3 sites); invariant holds today (runner lens verified) but by hand-sync -> whole-object claim constructor (runner.nim:1193,1323,1381) | fixed | claimSlot value-returning constructor + enterLive shared bookkeeping; dispatch-loop attempt/peakRssBytes pre-set folded in (4th hand-sync site); spawnRunDirect explicitly clears binCompiled/cacheDir (stale-occupant gap, readers phase-gated); transitionToRun deliberately enterLive-only (in-place handoff preserves compile state) (afe45c2) |
| r26 | M | Windows capabilities() not memoised: parameterless overload builds throwaway Supervisor + real cmd.exe probe PER CALL; >=2 calls per run; posix memoises -- same name, opposite contract (windows.nim:495-504 vs posix) | fixed | windows Capabilities memoised process-globally (posixcore cachedCapabilities idiom); probe runs once per process |
| r27 | M | posixcore.nim serializes 5 concerns (forward-decls + "moved ahead" comments = accretion signal) -> split cgroup/procscan/caps modules (posixcore.nim:40-49,213-229,652) | fixed | split into procscan(192)/cgroup(188)/caps(267) + core(1579); all forward decls gone (grep-verified); named re-exports keep every direct importer compiling untouched; macOS cross-check clean; A3 allow-list already directory-scoped |
| r28 | M | reap(id, runPhase) leaks executor concept into SS1 contract -> name the mechanism (claimOrphans) or make it a ChildSpec property (posix.nim:569-582) | fixed | ChildSpec.claimOrphans (default true; compile spawns false) declared at spawn; reap(sv,id) param dropped all backends -- RESTORES the RFC-pinned signature (runPhase was an undocumented B1 deviation); RFC ChildSpec pin + D2a-5 changelog amended; teardownDiscard delta noted (run-slot exception teardown now claims orphans -- cleanup improvement, report discarded either way) |
| r29 | M | verify-cache-pct: second full loadConfig peek in CLI with duplicated error mapping; library callers excluded from config fallback -> resolve in planImpl like siblings (crisol.nim:1143-1158) | fixed | CLI second loadConfig peek deleted; verifySample pct default -1 = unset sentinel (0 stays explicit disable); planImpl merge chain is the single resolution point; library fallback + explicit-override pinned by test_r29_verify_cache_pct_library (7e79b6a) |
| r30 | M | rlimit family five-way parallel structure repeated across RunOptions/Config/CLI/KDL/merge; RlimitOverrides bundle exists but unused as the carrier -> next limit kind = six-file sweep (api.nim:312-321 etc.) | fixed | RunOptions/Config carry rlimits: RlimitOverrides; KDL parses into the bundle; planImpl merges via fieldPairs-generic mergeRlimitOverrides (new kind = no new merge line); limitMemory deliberately outside (cgroup ceiling, not an rlimit); CLI/KDL names byte-identical, w2/r18 tests green (7e79b6a) |
| r31 | L | wait4Rusage cap probed+reported but loop hard-depends on wait4 (seccomp env => permanent weDeadline); flock cap not consulted by lock error path (posixcore.nim:1765-1779, lock/posix.nim:83) | open | posix + liveness lenses |
| r32 | L | Unchecked wait4 results decode uninitialized wstatus on failure path (posixcore.nim:1236-1241,1266-1267) | open | posix lens |
| r33 | L | failFast early return skips out-param epilogue: lateOrphansReaped/shutdownSignal/notStarted/interrupted dropped (runner.nim:2395-2403 vs 2438-2445) | fixed | closed structurally by r7 (failFast -> shared epilogue); test_failfast_phantom r33 test RED (notStarted==0 old) -> GREEN (==1) |
| r34 | L | rlimit/limit-memory flags silently no-op under --hermetic none (sandbox.nim:159-160) | open | runner lens |
| r35 | L | Interrupted finals: cache stamp bypassed (notEligible/inputHash "" despite real consult), attempts=0 on drain-raced finals, quarantine overlay skipped (runner.nim:1904-1916,779-793; result lens concurs) | open | runner + result lenses |
| r36 | L | RunOptions.strictHygiene doc contradicts SO1 (serve-side recompute IS strict) (api.nim:268-275) | open | runner lens; doc-only |
| r37 | L | resultcache TestRecord status via $enum stringify vs house explicit-case rule; rename silently invalidates cache with no format bump (resultcache.nim:198,215-220) | open | result lens |
| r38 | L | jsonout header schema sketch lists counts keys timedOut/signaled the emitter cannot produce; loadLastRun doc same drift (jsonout.nim:27-29,50-52) | open | result lens; doc-only |
| r39 | L | types.nim keyDiff doc overstates (cdmRecomputeMiss can never carry keyDiff) (types.nim:798-809) | open | result lens; doc-only |
| r40 | L | lookupAtPlan stale comment: says cdmPolicyDisabled where code returns cdmGroupOptOut (cachedispatch.nim:494-495) | open | result lens; comment-only |
| r41 | L | Retried compiled entry emits bare cacheLookup:"ok" with no backing consult -- the exact ambiguity the presence gate documents preventing (runner.nim:710, jsonout.nim:940-949) | open | result lens |
| r42 | L | crisol.nim:1377-1381 stale invariant comment (interrupted early-return retired) | open | runner lens; comment-only |
| r43 | L | Stale IOCP completion key = raw hJob value; handle+pid double-recycle can fabricate limitKilled -> use ChildId as key (windows.nim:674-675,771-778) | open | windows lens |
| r44 | L | consoleHasPid re-query: n2 > big.len scans documented-undefined buffer -> retry-until-fit or fail closed (windows.nim:1005-1010) | open | windows lens |
| r45 | L | D1c drain-closes-race claim strength: kernel ordering of END_OF_PROCESS_TIME vs handle signal undocumented; failure direction honest (missing attribution never fabrication) (windows.nim:1077) | open | windows lens; claim-strength nit |
| r46 | L | buildCommandLine not cmd.exe-safe if argv[0] is .bat/.cmd -> one-line contract note (windows.nim:511-514) | open | windows lens; doc |
| r47 | L | bInheritHandles=TRUE without PROC_THREAD_ATTRIBUTE_HANDLE_LIST whitelist; crisol's own fds verified clean; consumer-opened inheritable handles leak (windows.nim:600) | open | windows lens; library-grade caveat |
| r48 | L | windows forceKill no-prior-stop branch sets escalated=deliverable contradicting module's own formula; path executor never takes (windows.nim:1057-1058) | open | windows lens; doc-honesty |
| r49 | L | Raw child output rendered unsanitized (protocol records ARE sanitized on adjacent lines) -- ANSI spoofing of CI logs; possibly intentional (test colors) (render.nim:622-643,668-680) | open | security lens |
| r50 | L | memprobe.cgroupBudget reads cgroupfs root only; systemd-slice MemoryMax invisible; ownCgroupV2Path exists unused (memprobe.nim:88-91,144-180) | open | posix lens |
| r51 | L | cgroup reap aggregate stall bounded per-item not total: N stuck survivors = N x 300ms + 300ms loop starvation (posixcore.nim:1618-1620) | open | posix lens |
| r52 | L | runTestsWith ~500-line god-proc; perf-check annotation phase extractable to stats.nim (api.nim:1273-1781) | open | design lens |
| r53 | L | CLI parser duplication: ~10 parse-int-with-floor copies + short-flag bodies duplicate long-flag verbatim (crisol.nim:803-978,1050-1072) | open | design lens |
| r54 | L | Fabricated krTimeout in act ledger for direct forceKill; honesty is caller-discipline not structure (posixcore.nim:1441-1446, windows.nim:1056-1058, runner.nim:813-817) | open | design lens |
| r55 | L | Stale ELF executables in src/crisol/ shadowing module basenames -- lens claimed tracked; verified UNTRACKED local artifacts (June) | fixed | deleted + .gitignore entries; "tracked" claim refuted by git ls-files |
| r56 | L | Capabilities.kqueue: consulted value and reported value take different paths (same probe fn, cannot diverge today) (posixcore.nim:542,1792) | open | liveness lens; hypothesis-grade |
