# RFC-0005 — Distributed result cache + cryptographic trust & cache observability — handoff

- **Stage:** 2 — **architect round 3 APPLIED 2026-08-21** (fable lenses: depth/breadth/design/feasibility/liveness; ~85 raw findings, heavy convergence). RFC doc rewritten wholesale (every round-1/2 decision carried forward). **RFC-0007 Stage A COMPLETE (2026-09-03) — A7-gate re-baseline APPLIED to the RFC doc + this handoff (see §A7-gate re-baseline below).** **FORK-2 RESOLVED (a) 2026-09-03 (Corey, on design merit: the consult lives where the key becomes fully known; key-completion is staged, so the post-compile consult is the general mechanism and lookupAtPlan its memoized fast path) — A2c ships.** Everything is ready for `/tdd`. Previously: rounds 1 & 2 APPLIED 2026-08-21; FORK-1 (crypto deps) RESOLVED (sello for ed25519, nimcrypto for HMAC-SHA256, S3 unsigned-first).
- **INTERLUDE DONE (2026-08-21): GitHub issue #5 fixed + pushed (HEAD d4f75b2)** — closure from nimcache `link` array; `updateEntry` refuses empty closures + `invalidateEntry`; runner invalidates on recording failure; `DepGraphFormatVersion` 2→3 (v2 graphs discarded once); cold==warm acceptance; `./dev` dual mount (host path for milpa relative symlinks + /workspace for golden cc cmds). Suite green 80u/43i. amoxtli session notified with sha. **Next candidates (Corey's call): `/code-review` on 17c12e8..d4f75b2; then issues #8 (extra search-path modules invisible to closures) and #9 (`crisol closure --json`) — both wanted by amoxtli RFC-0064 S0c and staying crisol-side.** Then back to the RFC-0005 line below.
- **Issue #5 /code-review: round 1 ALL FIXED (commits f5e8d95 58bcfaf c5acf09 + recordClosure commit); re-review round 2 launching; then full suite, push, amoxtli notice.** Ledger (round 1, 2026-08-21; scope 17c12e8..d4f75b2; 5 lenses: soundness+liveness on fable, security/design/test on sonnet):**
  - R1 HIGH fixed (f5e8d95; `.nim.c.o` filter + moduleCPathOf; synthetic+real-compile tests) — `{.compile.}`d C sources appear in `link` as `@m<name>.c.o`; `.c.o` filter accepts them → phantom `<epDir>/<name>` path (R5 no-existence rule) → `closureContentHash` IOError → invalidate+warn EVERY run (sound but a functional regression: perpetual recompile + force-include). Fix: filter `.nim.c.o`. Verified against golden ep_a.json.
  - R2 HIGH fixed (58bcfaf; persistence test staged under root, warm closure == cold via execute()) — `test_nimcache_persistence_real` uses out-of-root eps → every execute() now hits the failure branch; no test asserts warm closure == cold through execute(). Fix: stage fixture under root; assert persisted closure after run 2.
  - R3 HIGH fixed (f5e8d95; empty/missing link raise pinned) — `extractClosure` empty-`link` → cekEnvironment raise has no pinning test.
  - R4 MED fixed — dead `try/except` around second `saveDepGraph` (never raises); R5 MED fixed (recordClosure in depgraph) — recovery policy inline in runner (extract+hash+update+invalidate+save) → `recordClosure` in depgraph/closure; R6 MED fixed (c5acf09 CHANGELOG) — no CHANGELOG entry for DepGraphFormatVersion 3 (one-time full recompile); R7 LOW fixed (f5e8d95) — duplicate `[0..^3]` ext strips, one helper; R8 LOW fixed (invariant NONEMPTY-CLOSURE on DepGraphEntry.closure) — invariant rationale restated ~6 places, state once on `DepGraphEntry.closure`.
  - R9 LOW fixed (store gated on closureRecorded; cdmKeyMiss) — result-cache store still runs after invalidation (key closureHash ""), dead write, never served (pre-existing).
  - OUT-OF-RANGE (pre-existing) — FILED: X1→#11, X2→#12, X3/X4/X5→#13, X6→comment on #8 (Corey: separate issues are NOT fixed in this review loop): X1 HIGH — `include`d files / `staticRead` / `{.compile.}` C sources in NO closure (not in `link` or `compile`) → false-fresh + under-selection; remediation `-d:nimBetterRun` `depfiles`. X2 MED — `crisol clean` depgraph GC dark (`loadDepGraph(config, "")` mismatches stored nimVersion → empty → never GCs). X3 LOW — M10 load guard checks only absolute paths; relative `..` in a tampered depgraph escapes root in `closureContentHash`. X4 LOW — no symlink check before `closureContentHash`. X5 LOW — stable binary persists even if `saveDepGraph` fails (stale entry + new binary). X6 LOW — `@n` (nimblePath) mangle prefix dropped; in-project `--path:tests` helpers excluded (= issue #8).
  - Security: no exploitable issue in range. Verified: migration (single reader/writer, no parallel closure copy), `link: []` never legitimate, no Defect escape, JSON purity.
- **Issue #5 /code-review round 2 (scope d4f75b2..0cd958d):** security clean; liveness: all mechanisms live, L-low fixed (persistence test now asserts warm manifest compile.len==0); round-2 ALL FIXED in d922aa9 6e49ae5 166b628 (flaky 4 pass in isolation; suite green on 0cd958d). Round 3 (0cd958d..166b628): security+soundness clean; liveness LOW-MED fixed (RunV1Revision 13 + ledger line, RFC-0005 §telemetry list); design MED fixed (rename moduleMangledNameOf) + cosmetic — commit 7943b5e... (see git log). Round 3 surfaced nothing above Low after fixes ⇒ FLOOR REACHED. Final full suite GREEN (80u/43i; timing tests pass in isolation). PUSHED: HEAD 1088e7f; amoxtli notified; #5 commented. **REVIEW LOOP COMPLETE.**
- **NOW: `/tdd` issue #8 (2026-08-22) — APPROVED 2026-08-22; slice 1 (tracer) DONE b1931dc; pins DONE f21ef54 (tests/unit/test_source_index.nim, all 6 pins green, no prod change) + CHANGELOG. Suite GREEN (81u/43i; headofline wall-clock test passes in isolation). PUSHED HEAD f21ef54; #8 CLOSED; amoxtli notified. **#8 DONE.**
- **NOW: `/tdd` issue #9 (approved "go on 9", 2026-08-22).** Design decided: `api.closureReport(opts): ClosureReport` on `planImpl` (no re-implemented loader/discovery); entries {path, group, flagHash, recorded, closure sorted, closureHash}; CLI `crisol closure <entrypoint> [--json]` / `--all [--json]` (neither/both → exit 3); JSON `crisol/closure/v1` rev 1 via pure `closureToJsonString` (jsonout); human `renderClosure` (render); read-only, no lock. Slice A DONE + committed 01446f0 (types in types.nim, re-exported by api; 7/7 e2e tests; CHANGELOG). Suite GREEN (81u/45i). PUSHED HEAD 01446f0; #9 CLOSED; amoxtli notified. **#9 DONE — S0c set (#5 #8 #9) complete.** COMPACT-SAFE NOW. Next: RFC-0005 build per the Resume line (FORK-2 answer first), or any other open issue (#1 SlotState, #4, #7, #10, #11, #12, #13). Then RFC-0005 build per Resume line.
- PREVIOUSLY-NEXT: `/tdd` #9 (`crisol closure [<ep>|--all] --json`, must reuse depgraph loader incl. nim-version probe; emit path+flagHash+group). COMPACT-SAFE POINT after push.** Design (decided, no architect round): replace `trackedRoots` root-guessing with a once-per-run `SourceIndex` (walk projectRoot minus .git/stateDir/nimcache/symlinked dirs + each dep-root; `.nim` files by basename); `@p`/`@n` body resolves to every indexed file whose path == body or endsWith(`/`&body) (R7 over-selection kept); `extractClosure(..., index)` + convenience overload; `recordClosure(..., index)`; `execute()` builds index once. Symlinked dirs NOT followed by default (dep content stays opt-in via dep-roots). Slices: (1) TRACER e2e — temp project with tests/config.nims `switch("path", thisDir())`, tests/support/helper.nim, tests/sub/test_x.nim importing support/helper → execute() → closure contains helper AND selectByDiff(changed={helper}) selects test_x; (2) `--path:lib/foo/src` lib visible; (3) ambiguity → all candidates (adapt r7 test); (4) `@n`; (5) index excludes stateDir/.git/symlinked dirs; (6) existing behaviours green. Commit per slice, push direct to main, notify amoxtli (uds:/run/user/1000/cc-socks/1882215.sock) with sha. Then #9.
- After #8/#9: RFC-0005 build per the Resume line below. Details — soundness S-Q1 MED fixed (`{.importcpp.}`/`{.importobjc.}` modules link as `.nim.cpp.o`/`.nim.m.o` → silently dropped; accept `.nim.{c,cpp,m}.o`, resolveMangledAll takes pre-stripped name); design D1 MED in-progress (drop redundant `epAbs` param from recordClosure), D4/D5 MED in-progress (new `cdmClosureUnrecorded` CacheDecision variant, json "closureUnrecorded"; fixes stale M8 doc + dead conditional), D6 LOW in-progress (CHANGELOG result-cache sentence). Round-2 full-suite: 4 timing tests failed under 8-way load (compile_not_run_budget, max_jobs_overlap, mem_throttle, per_group_timeout) — re-run in isolation pending. Then round 3 re-review (standing lenses) on the round-2 fix diff.
- **#8/#9 /code-review ledger (round 1, 2026-08-22; scope 1088e7f..01446f0; soundness+liveness fable, security/design/test sonnet):**
  - S1 HIGH **fixed** c01d1e1 (pins: test_source_index trigger-A/decoy/symlinked-dep-root realistic body; test_closure_searchpath trigger A + B real compiles) — was: (empirically verified, real compile) — `@p` bodies with `..` never match the suffix index: Nim mangles with the SHORTEST relative path (modulepaths.nim) and canonicalizes the found file to realpath (options.nim rawFindFile). Trigger A (REGRESSION vs pre-#8): in-root relative import shorter from a --path root, e.g. tests/unit/deep/t.nim → ../../../lib/x with --path:src → `@p..@slib@sx.nim` → dropped. Trigger B (pre-existing but now mis-documented/mis-tested as supported): symlinked dep-root (milpa _deps→CAS) yields realpath-relative `@p..@s..@shome@s…@ssrc@sdep.nim` (seen in our own .crisol/cache nkdl manifests) → dropped; test_source_index "symlinked dep-root" pins a body Nim never emits. Fix: strip leading `..` components before suffix match (widening only) AND match the suffix against expandFilename(p) of indexed entries too (index lexical+real, report lexical).
  - S2 LOW **fixed** c01d1e1 (pin: test_source_index @c/@h) — decodeBody handles only @s/@@; Nim also emits @h (#) and @c (:).
  - S3 MED **fixed** 2e5e154 (DepGraph.loadDiscard enum + loadDiagnostic → pipeline ConfigWarning; pins test_depgraph_guard S3 suite + test_cli_closure stale-depgraph) — loadDepGraph nimVersion mismatch → empty graph silently; `crisol closure` then shows recorded:false everywhere, exit 0 (indistinguishable from never-ran). Add stderr warning on header mismatch; fix api doc "no subprocess" (nim probe IS a subprocess).
  - S4 MED **fixed** aaed138 (CLI exit 3 no-match; pin test_cli_closure) — `closure <path>` matching no entrypoint → {"entries":[]} exit 0 (run exits 3 "no entrypoints matched").
  - D1 MED **fixed** aaed138 (ClosureReport.adHocPaths/ambiguousPaths; CLI pathFlagsWarnings; JSON unchanged since plan/v1 never carried them) — closure drops adHocPaths/ambiguousPaths diagnostics (pathFlagsWarnings) that run/list print.
  - L1/T1 MED **fixed** aaed138 (--config/--config=; pins) — `crisol closure` rejects --config (run/list/clean accept it).
  - D2/D3/D4 LOW **fixed** aaed138 (usage grammar/gates note/path convention in type+schema+usage; api doc subprocess claim); T2 LOW **fixed** 6106233; **round 2 re-review** (01446f0..aaed138): design → R2-D1 MED --config parse dup clean/closure (helper), R2-D2 MED load-provenance on DepGraph persistence type (→ out-param overload + DepGraphDiscard{kind,stored,current} with key/message as single authority), R2-D3 LOW enum/string twins + dead arm (same fix), R2-D4 LOW AmbiguousPath alias, R2-D5 LOW review-ids in shipped comments/test names; security → R2-S1 LOW sanitize stored header strings in discard message; liveness → R2-L1 MED clean nimVersion "" = issue #12 (annotated, separate), R2-L2 LOW closure gated-out positional → exit 0 like run, R2-L3 LOW pcLinkToFile (covered by 6106233), R2-L4 LOW @c/@h test shape dir-component, R2-L5 LOW run structural no-match drops plan warnings; soundness → R2-SO1 HIGH `@m..` realpath body via symlinked dep-root (entrypoint as shallow as every --path root) normalizes to realpath → filtered out (verified; fix: SourceIndex.roots + gated lookup fallback when @m candidate escapes all roots; agent in flight), R2-SO2 MED = R2-L2 (gated-out positional exit), R2-SO3 MED = issue #12, R2-SO4 LOW malformed header → dgdNone silent (→ dgdMalformed, agent addendum), R2-SO5 LOW multi-line fingerprint in message + wrong tail for list/closure (agent addendum), R2-SO6 LOW Windows drive-letter absolute body (wontfix: Linux target; noted). Round-2 fixes committed: 1488315 (R2-D2/D3/S1/SO4/SO5: DepGraphDiscard out-param, key/message authority, sanitize, dgdMalformed, single-line msg), 38e094e (R2-SO1 @m realpath fallback; R2-L4 @c/@h dir-component pin), b597924 (R2-D1 takeConfigFlag, R2-D4 AmbiguousPath, R2-D5 ids stripped, R2-L2/SO2 gatedOut exit 0 + closure/v1 rev 2, R2-L5 no-match run carries plan). 6106233 = T2 + R2-L3. **Round 3 re-review** (01446f0..b597924) findings: soundness R3-SO1 MED @m fallback suffix over-selects untracked out-of-root imports (→ exact realpath match via byReal; agent), R3-SO2 LOW epDir-behind-symlink lexical candidate bogus (same agent), R3-SO3 LOW sanitizer hides fingerprint hash (→ first line + |hash tail; agent), R3-SO4 LOW readFile raises IOError not OSError (agent), R3-SO5 = R3-L1 closure human output lacks gate-skip lines (agent), R3-SO6 LOW nimVersion non-string → dgdMalformed (agent), R3-SO7 INFO 2-arg overload silent for clean (= #12); liveness R3-L1 MED gate lines, R3-L2 LOW malformed/stale depgraph never driven via runMain for list/run (agent), R3-L3 LOW CHANGELOG overclaims JSON on run no-match (agent), R3-L4 LOW clean --config= forms untested (agent); security R3-S1 LOW OSError branch raw stderr (FIXED inline, uncommitted); design R3-D1 = R3-S1, R3-D2 MED sanitize at the stderr sink for all ConfigWarning/pathFlags lines (agent), R3-D3 LOW extractClosure should use index.roots (agent), R3-D4 LOW takeConfigFlag own whole branch (agent), R3-D5 LOW T2 tokens from 6106233 (agent). Round-3 fixes committed: 82ef635 (R3-D2 sink sanitizer, R3-L1/SO5 gate lines, R3-D4 takeConfigFlag outcome, R3-L3 changelog, R3-L4 clean --config= tests, R3-D5 tokens), 3e23dcf (R3-SO1 exact-realpath @m recovery via byReal, R3-SO2 epDir symlink, R3-D3 index.roots authority), b24df4c (R3-SO4 IOError, R3-SO3 hash tail, R3-SO6 nimVersion non-string, R3-L2 runMain list/run depgraph tests, R3-S1). Full suite at b597924: 81/81 unit, 43/45 integ (headofline+mem_throttle = known timing flakes; headofline passed isolated; mem_throttle rerun pending, log8_flaky). **Round 4 re-review** (b597924..b24df4c): R4-1 MED symlinked entrypoint FILE → @m base must be parentDir(realpath(entrypoint)) (perpetual invalidate; agent), R4-2/SEC1 MED sanitization coverage partial (CrisolError msgs incl. KDL snippet, rr.error, gate lines, junit) → per-line sanitizer in ioutils shared by render+depgraph, all writes routed (agent), R4-3 LOW gate lines sanitize + closure gate lines stdout like run (agent), R4-4 LOW unreadable test skipped as root → /proc/self/mem symlink (agent), R4-5 LOW '|' heuristic only for fingerprints (agent), R4-6 LOW execute()-driven tests for symlinked ep dir (agent), SEC2 LOW byReal collision in case 2 (doc; wontfix-by-design), SEC3 INFO TOCTOU FIFO (comment), DES LOW addUnique helper + F1/F2/L2 tokens (agents). Round-4 fixes committed: 8ed0426 (SEC1/R4-2/R4-3 full sanitization via ioutils.sanitizeControlBytes, closure gate lines stdout, R4-4 /proc/self/mem test, R4-5 pipeAware only nimVersion, TOCTOU comment, L2 tokens), 45d9480 (R4-1 realpath of entrypoint FILE, case-2 exists-rule, addUnique, R4-6 execute tests, F1/F2 tokens). **Round 5 re-review** (b24df4c..45d9480): FLOOR REACHED — 0 Critical/High/Medium. Lows: design (TOCTOU comment placement, r4_1 tag, ioutils doc, alias dup, DEL assertion) **fixed** 3783234; soundness/liveness R5-F1/F3/F4/F5 **fixed** 7121135; R5-F2 INFO report bodies raw = pre-existing → **issue #14**. Full suite at 45d9480: 81/81 unit, 45/45 integ (headofline passed isolated). Light re-verify of 3783234/7121135: one Low (stale CHANGELOG alias ref) fixed cf458f8. **amoxtli cross-session report (2026-08-22, at 01446f0):** gap 2 (`..` bodies) = S1, fixed c01d1e1; gap 1 (`_deps/chronos -> ../.docker-home/milpa-cache/...`, symlink into a PRUNED dot-dir under projectRoot; @p-shaped bodies miss because nothing under the dot-dir is indexed) → fix in flight: on index miss, join stripped suffix onto each tracked root + existence check (sound by construction; prune = walk-cost, not tracking). Told amoxtli: fresco is outside projectRoot → needs `dep-roots "_deps/fresco"`. Gap-1 fix **committed a1f8cec** (roots-existence fallback; real-compile @p deep + @m shallow through `_deps/dep -> ../.hidden/cas/dep`). test_headofline was failing isolated at ~388-400ms vs 380 threshold (buildSourceIndex ~21ms per execute on the repo root + host load ~2 from the amoxtli session) → **22953c0** runner builds the source index lazily on first closure recording (all-fresh runs pay nothing); headofline passes again. **DONE: #8/#9 review loop closed at the floor; 01446f0..2cf7366 (18 commits) PUSHED to main (2cf7366 = drop an unused `ioutils` import in render.nim that was fatal under amoxtli's --warningAsError:UnusedImport; consider adopting that flag in crisol's own build so consumers never hit it); full suite green at 22953c0 (81/81 unit, 45/45 integ incl. the 4 timing tests run serially); amoxtli pinged with pin target 2cf7366 (they will bump pin + add dep-roots _deps/fresco + delete depgraph to re-record). Memory updated (dev-test-verification-gotchas). Issues filed this loop: #14 (report bodies unsanitized); #12 annotated (clean nimVersion "" remains the one silent discard caller). Remaining open issues: #1, #4, #7, #10–#14.** Then → final full suite → push (11 commits since 01446f0 unpushed) → notify amoxtli → memory. Next: triage round 3 → fix → floor → push (all 7 commits since 01446f0 unpushed) → notify amoxtli session with sha. L2 wontfix (keep). Mandate (Corey 2026-08-22): fix ALL in-range incl. Lows; separate issues filed not fixed. D2 — usage says singular <entrypoint>, parser accepts N; D3 LOW — `--all` doc omit "gates still apply"; D4 LOW — closure path convention (relative vs absolute dep-root) undocumented in type/schema.
  - T2 LOW — tests: help text asserts closure? no; JSON warnings + schemaRevision==ClosureV1Revision unasserted; two-group entry case; unknown-flag exit; error-branch exits; renderClosure content; test_cli_closure leaks temp dirs (no removeDir); `p == normBody` equality arm unexercised; non-dot stateDir prune untested; nonexistent depRoot untested.
  - L2 LOW — `@n` practically dead in supported layouts (only nimblePath wins) — harmless; keep.
  - Security: clean (allowlist lookup; under-root filter kept; walk terminates; std/json escaping).
- **Resume:** FORK-2 resolved (a) 2026-09-03. `/tdd` in the round-3 order (B3a ✅ 93fe3d1, B3b ✅ 4cda363, B3c ✅ 58a9419, A0 ✅ f34c1ac, A1 ✅ 657cbc6, A2a ✅ 79f7c22, A2b ✅ ea96cc1, B1a ✅ 99f7add, B1b ✅ 4df3cad, B1c ✅ 597b521, B2a ✅ 35550d0, B2b ✅ 3fe07ef): **A3a → A3b (E2E-A-trust) → A3c-i/ii (E2E-1) → A2c-i/ii/iii (FORK-2 (a)) → C-dep → C4 (E2E-2) → C5a/b/c → C1a → C1 → C1b-i/ii/iii → C2 → C3a/C3b (E2E-3) → C3c → C6**. Pre-flight before A1: `milpa fetch` (lock identity stale; `_deps/nkdl` dangling) + `./dev` mounts `$PROJECT_DIR` at `$PROJECT_DIR`. ~~Prereq issue #1 (`SlotState`) first, independently.~~ **Prereq CLEARED: #1 landed inside RFC-0007 A2b (96a0598).** Commit per slice after gates; push direct to main ([[feedback-push-direct-to-main]]).
- **Artifact:** this RFC = `docs/rfc/0005-*.md` + this handoff (per /rfc-flow convention; NO GitHub issue — #2 was an /architect-Step-6 leak, closed).  •  ~~**Prereq (do first, independent):** issue #1 (`SlotState` enum dispatch hardening)~~ — **struck at the A7-gate: issue #1 landed inside RFC-0007 A2b (`SlotState(ssIdle/ssLive)` + `ChildId` replaced the `pepIdx == -1` sentinel, commit 96a0598); no standalone prereq remains.**
- **Origin:** came from `/architect` feature-frontier exploration (codebase mode) after RFC-0004 shipped (commit `87994fc`, branch `rfc-0004-incremental-hermetic-execution`, pushed — PR not yet opened/merged).

## A7-gate re-baseline (2026-09-03) — 0007 Stage A landed; deltas applied to the RFC doc

RFC-0007 Stage A (A0–A7) is complete on main, all CI-green (see `docs/rfc/0007-execution-substrate.handoff.md` for per-slice evidence). The 0005 doc + this handoff are re-baselined on the shape that actually shipped; where a round-1/2/3 record below contradicts this list, **this list and the RFC doc win** (the round records are history, kept verbatim):

1. **`StoredEntry.result` payload = the landed observation.** `CachedResult = {run: ProcessResult, records, cachedAt, payloadChecksum}` where `ProcessResult = {exit: Exit, cause: Cause, evidence: Evidence, rusage: Option[Rusage], durationUs}` (`src/crisol/process/types.nim`); the payload's `run` node is `process/resultjson.toJson` verbatim (the ONE `ProcessResult`⇄JSON owner — the 0005 serializer wraps, never re-derives). NO outcome/exitCode/signal/durationMs is stored. `resultCacheFormatVersion` is **3** (v2 = real-`ProcessResult` payload, v3 = the `Limits` key re-home).
2. **Outcome is ADVISORY — a pure function of (observation, policy), recomputed at every trust boundary, never read from storage as truth.** Read side already enforced at HEAD: `lookupAtPlan` recomputes `outcome` over the replayed `pkCached` phase; a not-passed recompute is `cdmRecomputeMiss` ⇒ rerun (`cachedispatch.nim`). 0005's tiered lookup inherits: transport/integrity/trust gate *serving eligibility*, the recompute gates *serving*. Nothing in 0005 signs, hashes, compares, or gates publication on outcome-shaped data — observations only.
3. **Publish gate = `shouldStore` at HEAD:** `resolveCacheable` && recomputed `outcome(res) == oPassed` && `evidenceSatisfies(spec, runEvidence(res))` && attempt-1. `isFullyAchieved` no longer exists; `evidenceSatisfies` consumes NAMED guarantees, never enum ordinals: observed escapee (`escapees.len > 0`) ⇒ not stored; per-limit `lsFailed` ⇒ not stored; `lsUnsupported` ⇒ stored with the honest label; `tree == toUnobservable` ⇒ stored with the label (deliberately not consulted by the gate); requested `netIso` (`hlNetwork`) ⇒ never stored until RFC-0008 observes. The 0005 trust layer filters on the same named `Evidence` guarantees (incl. `tree`/`killDomain` as labels) — never `ord` comparisons. Locked calls carried: no network enforcement anywhere; escapee policy (observed ⇒ uncacheable, unobservable-by-tier ⇒ cacheable-with-label); kill authorship (`Cause` asserted only from the runner's recorded acts).
4. **`--verify-cache` compares observations, never verdicts:** fresh `Exit` (structural `==`) + parsed `records` vs stored `ProcessResult.exit` + `records`; `Cause`/`Evidence`/`rusage`/durations excluded from comparison, reported for diagnosis; outcome strings never compared (RFC §Stage B rewritten).
5. **Wire = `crisol/run/v2`** (rev 16 cutover, 17 `recomputeMiss`, 18 `substrate`; current `RunV2Revision = 18`). 0005's planned revs renumber **13/14/15 → 19/20/21** (cacheTier+cacheLookup+verifyFails / cacheStats / keyDiff). amoxtli's v2 consumer is exercised against real output in 0007's A7-gate (the other half of the gate), so the "additive fields safe" claim stands on v2.
6. **Issue #1 (`SlotState`) prereq struck** — landed inside 0007 A2b (96a0598). No standalone prereq before B3a.
7. **Runner/API deltas that touch 0005 slice text:** `CrisolInterrupted` + `pendingSignal()` retired (B0's prefetch loop now checks `signals.shutdownRequested()` and abandons the prefetch; `execute()` returns a partial report); `spawn.nim` deleted (secrets-scrub wording re-anchored on `filterEnv`/`ChildSpec.env`); lock is `flock` (A4), not `F_SETLK`; `EntrypointResult` lost `outcome`/`exitCode`/`signal`/`achieved`/`peakRssBytes`/`cached`/`flaky` and carries `compile`/`run: Phase` — FORK-2 (a)'s synthesis is `compile = Phase(pkRan, …)` + `run = Phase(pkCached, stored)`.
8. **`KeyInputs` re-home:** `rlimitConfig: RlimitConfig` → `limits: Limits` (still 9 components); `KeyComponent`'s `kcRlimit` → `kcLimits`; `explainMiss` renders `limits` per `LimitKind`.
9. **Line anchors:** round-3 anchors pre-date the 0007 rewrite. Anchors inside re-baselined RFC sections are refreshed at HEAD 2026-09-03; anchors inside round-record prose (this handoff, §Key portability env internals, verify-cache re-entrancy guards) are historical — re-grep at slice time.

## DECIDED DESIGN (Corey's selections — locked)
Architect ran 4 competing interface designs (Minimal / Flexible / Ergonomic / Ports-and-Adapters). Corey chose, with full over-engineering risk disclosed:
1. **Architecture = FULL FLEXIBLE** (not the hybrid I recommended) — pluggable registry + multi-tier + pluggable serializer/telemetry, BUT executed at the bar = built on the Ports design's clean boundary contract + **phased green migration**, with the Flexible agent's self-corrections folded in.
2. **Trust = ed25519 signed attestation NOW** (not just HMAC) — ports for none/HMAC/ed25519; verify-on-read; sign-on-publish.
3. **Scope = A + core B together** (matched my rec) — remote cache + miss-explanation + hit-rate + `--verify-cache`; defer dashboards/attestation-dashboards.

### Architecture (ports-and-adapters; "dispatch UNCHANGED" was reversed by FORK-2 (a) — round 3 found the cold host never reaches the lookup under that constraint, and the post-compile consult is the fix)
- **`CacheBackend` port** — closure-field object (zero-cost, matches existing `CacheSeams` idiom, NOT vtable/method): `get(key)→Option[StoredEntry]`, `put(StoredEntry)→bool`, optional `list`/`delete`. NEVER raises; `none`/`false` on miss/corrupt/version-mismatch/timeout/offline.
- **`StoredEntry` wire type** — `{key: SoundnessKey, keyInputs: KeyInputs (for explain; adapters MAY omit on disk), result: CachedResult, payloadChecksum, formatVersion, attestation: Option[Attestation]}`. `Attestation = {sigAlg, signer, signature, signedAt}`.
- **`TrustPolicy` port** — `verify(entry)→bool` (on-read), `sign(var entry)` (on-put). Adapters: `nonePolicy` / `tokenPolicy`(HMAC-SHA256) / `signedPolicy`(**ed25519**, sign-on-put, verify-against-pinned-pubkeys). Sigstore/Rekor adapter STUBBED behind `when defined(crisolSigstore)`.
- **`TieredCache`** — composes `seq[TierConfig{backend, populateOnHit, requireTrust}]` L1→L2→L3. `get`: waterfall, verify-trust on requireTrust tiers (fail→try next, never serve bad entry), populate-on-hit backfill earlier tiers **— backfill must respect the STRICTEST trust policy of any tier that will serve the entry** (correctness subtlety). `put`: sign once, write all tiers best-effort. Remote tiers wrap their own deadline; L1 sync no-timeout.
- **`BackendRegistry`** — `registerBackend(name, factory)`; adapters = one file + one registration. Ship: `memory`(test), `local-fs`, `http`, `s3`; `crisol-server` future.
- **`CacheSerializer` port** — JSON only ships (msgpack deferred but port exists). **`TelemetrySink` port** — NilSink default; LogSink/InMemorySink(tests).
- **Wiring:** `realSeams(tc: TieredCache, ...)` builds `CacheSeams.load`=`tc.get` / `store`=`tc.put`. **`lookupAtPlan`/`shouldStore`/`CacheContext`/`inactiveDecision` byte-for-byte UNCHANGED.** Publish gate unchanged (`isFullyAchieved && attempt-1 pass` in `shouldStore`, before trust layer ever sees a write).

### Core B (observability/trust UX)
- **Miss-explanation:** `explainMiss(prev, curr: KeyInputs)→seq[MissDiff]` (PURE; diffs the 9 components). Mechanism: write a `KeyInputs` SIDECAR keyed by the **identity** key (stable locator; soundness key changed = the miss) so a miss can name which of {closureContentHash, flagHash, nimVersion, ccVersion, fixtureHash, argv, rlimitConfig, hermeticEnvHash, protocolMajor} changed. `--explain-miss` flag + degrades gracefully if no sidecar (older writer).
- **Hit-rate telemetry:** summary line (L1 hits / remote hits / misses / total / % / wall-saved / published) + `run/v1` field. Aggregate existing `CacheDecision` (already per-result; RFC-0004 made it 8 honest variants incl `cdmStored`/`cdmGroupOptOut`/`cdmHit`).
- **`--verify-cache`:** re-run sampled cache hits, compare to cached result, flag nondeterminism the hermeticity gate can't catch. **Runs as a POST-RUN BACKGROUND re-check, NOT in the hot path** (self-review correction). Never evicts (human decision); emits a verify-fail telemetry event.
- Config (KDL `cache { remote "name" { backend / trust { ... } / scope-group } telemetry { hit-rate / explain-miss / verify-cache-pct } }`) + flags `--no-remote-cache`, `--explain-miss`, `--verify-cache`. CLI/env override config. **Secure-by-default**: read tokened; publish requires write-scoped credential (never a `--publish` flag).

### Hard constraints (every slice respects)
Soundness key = SOLE content address; only `isFullyAchieved && attempt-1 pass` may PUBLISH; local L1 stays, run NEVER blocks on remote (timeout/offline → miss, proceed); ports-and-adapters so tests use in-memory backend (NO network in suite); entrypoint-granularity / binary-opaque identity preserved (see [[boundary-granularity-discriminator]]).

### Deferred (explicit non-goals for 0005)
History dashboards / OTEL export (follow-on); full Sigstore/Rekor (ed25519 ships, Sigstore stub); msgpack serializer; distributed *execution* (still `--shard`); any per-test/sub-binary feature.

### Self-review corrections to BAKE IN (from the Flexible agent's own critique)
1. `CacheSerializer` port present but JSON-only ships (don't build msgpack speculatively).
2. `SigstorePolicy` type locked but impl behind a build guard (no shipping empty stub).
3. `--verify-cache` = post-run background re-check, NOT hot-path latency.
4. Per-tier trust backfill: enforce backfill only when source trust ≥ destination tier's policy (strictest wins).
5. Reconcile the remote-fetch "future" with the runner's ACTUAL concurrency model — do NOT hand-roll a competing async primitive (the runner is fork/poll-loop based; remote I/O likely a bounded background thread or sync-with-deadline).

## Implementation Path / candidate stages (refine into slices in the RFC doc)
- **Prereq:** issue #1 `SlotState` (separate, first).
- **Stage A — port skeleton (green throughout):** A1 port types (`cacheport.nim`: CacheBackend/StoredEntry/TrustPolicy/TieredCache) + in-memory adapter, roundtrip test. A2 wrap local-fs as first adapter; `realSeams` → single-tier TieredCache; ALL existing tests green. A3 point `gcResultCache`/`cleanOrphans` at `backend.list/delete`.
- **Stage B — observability:** B1 `KeyInputs` sidecar + `explainMiss` (pure) + `--explain-miss`. B2 hit-rate telemetry (TelemetrySink, summary line, run/v1 field). B3 `--verify-cache` (background re-exec compare).
- **Stage C — remote + trust:** C1 HTTP/S3 adapter + BackendRegistry (in-process fake-server test). C2 TieredCache L1+L2 wiring in api.nim, config (KDL `cache { remote }`), populate-on-hit + strictest-trust backfill. C3 TrustPolicy port: none/HMAC. C4 ed25519 signedPolicy (sign/verify/tamper-reject/pinned-keys). C5 secure-by-default token scopes + offline-tolerance hardening.
(Sequence A→B→C so each leaves the suite green; B can interleave; C is the network-touching tail.)

## Testing strategy
Boundary tests at the port via in-memory adapter (tier waterfall / populate-on-hit / trust verify+reject / miss→next-tier / offline→miss / publish gate). `explainMiss` pure → exhaustive KeyInputs-diff. ed25519 sign/verify roundtrip + tamper-reject + strictest-trust backfill. `--verify-cache` divergence via nondeterministic stub. Fold old `loadCached`/`storeCached` direct tests into the local-fs adapter boundary tests. **No network/disk-hot-path in the suite.**

## Process reminders (carried from RFC-0004)
- Build/test ONLY via `./dev` (podman); `./dev test` EXIT code UNRELIABLE — grep for `[FAILED]`/nimble-exception excluding `fail_always`/`fail_compile`; report `[OK]` count. `./dev check` for type-check. See [[dev-test-verification-gotchas]].
- Serial sonnet subagents for slices, ONE `./dev` builder at a time (concurrent-./dev race bit us in RFC-0004). Don't run my verification concurrently with a building agent.
- Commit/push ONLY when Corey asks; commit msg ends `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` (global hook strips it; verify `git log -1 --format=%B | grep -i Co-Authored-By` empty). No worktree isolation ([[git-hooks-worktree-isolation]]). No milpa.
- [[no-fake-forks-soundness]]: don't dress first-principles-correct (esp. soundness/security) decisions as forks.

## Architect round 1 — DONE (applied to RFC doc)
4 lenses (depth/breadth/design/feasibility) ran. ~25 clear-best fixes applied. Convergent central fix: `TieredCache.get`→`getWithProvenance` returning `TierHit{result,tierName,tierIndex,verified}` (3 lenses independently); sink moved off TieredCache onto the realSeams adapter; `CacheDecision += cdmTrustFail`; `EntrypointResult += cacheTier`. Trust: signer-in-signed-envelope + recompute-checksum-on-read + signedAt-never-signed; multi-signer/rotation/HMAC-caveat documented. verify-cache: cacheDisabled+retries0+synthetic-plan+user-visible+strict-flag. Backfill: real `verified` bit, A3 tested via injected mock policy (nonePolicy can't exercise the security case). Naming: KeyComponent/kc, backfill-on-hit/verify-trust. Config: URL-scheme selects adapter (dropped redundant `backend`). Added: remote-error telemetry, operator/deployment section, Alternatives-considered, secrets-in-records non-goal, path-keyed sidecar (so flag-change misses are explained). Re-slice: A2→A2a/A2b(atomic, updates test_cachedispatch+test_api callers), C5→C5a/b/c, +C-dep +C1a, HttpFetcher seam.

## FORK-1 (crypto deps) — RESOLVED 2026-08-21
- **Trigger:** sello 0.5.0 (`~/projects/nim/libs/sello`, RFC-006 closed 2026-08-21) = pure-Nim ed25519 sign+verify, zero runtime deps, no FFI, `{.raises: [], gcsafe.}`; API `keypair(seed)`/`keypair(seed, expectedPublic)`/`kp.sign(msg)`/`pk.verify(msg, sig)`/`toBytes`/`toPublicKey`/`toSignature`/`wipe`. This was the "nim-ed25519 yak project" from the original fork notes.
- **Decision (Corey):** (b) ed25519 = **sello**; (a) HMAC-SHA256 = **nimcrypto** (pure Nim, in CAS) — both policies ship; Corey may drop in an in-house HMAC later (NOT via sello: sello's SHA-512 is internal, hash export is a closed non-goal, and it has no SHA-256 at all). (c) S3 = **unsigned/MinIO path-style first**, SigV4 follow-on (my call per the RFC's original recommended default; nothing near-term needs SigV4).
- **sello publish state:** Corey handles push + `v0.5.0` tag himself. Until then crisol pins **locally**: milpa supports `"sello" local="../sello"` (source_spec `local=` form; `milpa add` CLI has no `--local` flag — write it in `milpa.kdl` by hand). **Caveat for C-dep:** `./dev` mounts only the project dir + the milpa CAS into podman; a local-path `_deps/sello` symlink to `~/projects/nim/libs/sello` won't resolve in-container — add the mount (or confirm milpa copies local deps into the CAS) in slice C-dep.
- **Historical notes (kept for context):** at round 1, no pure-Nim ed25519 existed in the ecosystem (only FFI wrappers around orlp's C); the sign/verify split + CT-in-Nim analysis that motivated sello is recorded in sello's own RFC-001..004.

## Architect round 2 — DONE (applied to RFC doc, 2026-08-21)
5 lenses (depth / breadth / design / feasibility / load-bearing liveness), ~54 raw findings, heavy overlap. All resolved as clear-best (the two lens-flagged "forks" — ledger-on-hit, CacheRuntime bundle — had confident recommendations and were made). Applied:
- **Security/correctness:** signed envelope now binds **SHA-256** of the recomputed canonical payload (FNV-1a-64 is second-preimage-able; it stays for corruption detection only); envelope is a pinned NUL-delimited byte layout (`key\0hex(sha256)\0$formatVersion\0signer`, keys.nim convention); idempotency claim corrected (timing fields make payload bytes differ per publisher — "any valid entry is sound", not "equal bytes"); `--verify-cache` synthetic plan must reset `edecision` to `edRunFresh` (else full recompile via `spawnCompileStable`); unsigned-S3/MinIO has no transport write-auth ⇒ `verify-trust` + non-none policy REQUIRED for `s3://` in 0005 (IAM story labelled SigV4-follow-on); `std/httpclient` has NO connect-phase timeout ⇒ B0 pins a raw-`std/net` HTTP/1.1 fetcher with `connect(timeout)` (C1b), default deadline 2000 ms, DNS residual documented.
- **Types/API:** `TrustPolicy.verify` returns granular `CacheError` (`ceTrustNoAttestation/UnknownAlg/UnpinnedSigner/SignerMismatch/BadSignature`) + `dispose` closure; `SigAlg` enum; `signer` = base64(pubkey) == pinned-key string (ed25519) / operator `key-id` (HMAC); `signedPolicy(signSeed: Option[array[32,byte]], pinned: seq[PublicKey])` — sello's move-only `Keypair` built+captured INSIDE (compile-spike in C-dep); `tokenPolicy(secret, keyId)`; `CacheGetResult` object; `LoadProc` WIDENED to `LoadOutcome{result,tier,err}` (resolves the round-1 contradiction — only path for `cacheTier`/`cdmTrustFail`); `CacheRuntime{tiers,sink,dispose}` bundle replaces 2 new realSeams params; `CacheSecrets` resolved once in api.nim (cacheport never reads env); `HttpFetcher` returns `Option[HttpResponse{status,headers,body}]` (none = transport failure); `BackendConfig.settings: Table` + `settingGet/settingInt`; `TelemetryEventKind/TelemetryEvent` declared (+tekPublish, tekVerifyFail); `productionRegistry()` vs `testRegistry()` (memory:// is a config error in prod); `RunOptions.cacheRuntime` library escape hatch; trust is CACHE-GLOBAL (KDL `trust{}` moved to `cache` level — per-tier would break backfill-with-attestation).
- **Config/ops:** env names `$CRISOL_CACHE_HMAC_KEY`, `$CRISOL_CACHE_TOKEN[_<TIER>]`; s3 `endpoint`/`path-style`; config-time rejection of unverifiable trust config; format-bump mixed-fleet transparency + lifecycle note; multi-project prefix hygiene; MinIO local-dev; privacy guarantee for cacheStats/cacheTier; POSIX-only note; explainMiss cold-host honesty + keyInputs on wire seeds sidecar on backfill; verify-cache seed semantics + min-1 sample; flags renamed to parser grammar (`--verify-cache`, `--verify-cache-pct N`, `--verify-cache-seed N`, strict requires verify).
- **Slices/liveness:** A3 split → A3a (pure engine) / A3b (provenance wiring: types+cachedispatch+runner+jsonout) / **A3c (file:// two-tier END-TO-END spine through runTests/CLI — E2E-1)**; C0 → **B0** (precedes B3); B2 → B2a/B2b; C1 → C1 + **C1b** (production fetcher, the single sanctioned loopback socket test); C3 → C3a (config) / C3b (wiring) / **C3c (prefetch — was an unowned "mitigation")**; C4 carries **E2E-2** (HMAC tamper over file:// tiers); A2b corrected (test_api.nim has no direct realSeams caller); C-dep: generic `./dev` local-dep mount (no hardcoded path), move-only-capture spike; ledger staleness on hits ACCEPTED + documented; "every mechanism has a producer slice" added to hard constraints.

## Architect round 3 — DONE (applied to RFC doc, 2026-08-21, fable)
5 lenses, ~85 raw findings; the doc was rewritten wholesale because the fixes touch the type inventory, seam shape, config grammar, slice order and DoD. Applied (clear-best):
- **Liveness / honesty:** headline restated — a remote tier removes *execution*; the key is a compile byproduct (closure hash from the nimcache after `nim c`), so a cold host still compiles and, as of HEAD, never consults the cache at all (`lookupAtPlan` needs `edRunFresh` ⇒ binary + depgraph). ⇒ **FORK-2** (post-compile consult A2c vs honest rescope), with the cold-host E2E-1 (second stateDir/project copy) specified under (a). New §Key portability: `HOME/PATH/USER/TERM/TZ/LANG/LOGNAME` VALUES are in the key (RFC-0004 design; no passthrough knob ever shipped) ⇒ **slice A0 `env-pin`** (KDL/CLI/RunOptions; defaults unchanged) + key-invariance test; `dep-roots` absolute paths documented. Stage B gets its own DoD (E2E-B); mock-policy trust E2E in A3b (E2E-A-trust) so the reject path is live from Stage A; trust slices (C4/C5) moved BEFORE http/s3 (zero network dep; unsigned s3 unusable without trust). **Build order = value-arrival: B3 → A0 → A1 → A2a → A2b → B1 → B2 → A3 → C** (separate observability RFC rejected).
- **Correctness:** `cdmTrustFail` DROPPED (the live stamp `runner.nim:1326-1356` overwrites plan-time decisions with `shouldStore`'s verdict — a trust-rejected entry that runs live is correctly re-published as `cdmStored`) ⇒ `EntrypointResult += cacheLookup: CacheVerdict` (+ `cacheTier`); E2E-2 asserts `cacheLookup == trustBadSignature && cdmStored` and must recompute `payloadChecksum` after the byte flip (bare flip = `cvCorrupt`, the negative control). **Put rule** mirrors backfill (`attested OR not verifyTrust`) — a no-secret consumer must never overwrite CI's signed object. `verify-trust` default = `policy != "none"` (E2E-1 config was a parse error otherwise). Version coupling: payload-format bump ⇒ storage-version bump (static assert). Domain-separation tag in the envelope. `--verify-cache`: `retries` is per-pep (set `pep.retries=0`), `edecision` reset was a no-op (never written back), `execute(recordLedger=false)`, no-op `onResult`, results not merged, placement after `persistLastRun`/before `releaseLock`. Secrets: `delEnv` after resolve + `CRISOL_CACHE_*` stripped at every hermeticity level (`--hermetic none` leaked them). Sidecars under `<root>/v<N>/inputs/` (else counted by the cap and evicted FIRST by LRU); tier-0 only; `envDigest` names so `kcHermeticEnv` is legible; multi-line `nimVersion` render rule.
- **Seam & types (design):** seam derives INPUTS — `KeyOfProc → KeyInputs`, `LoadProc/StoreProc(pep, KeyDerivation[, res])`, `LoadProc → CacheLookup{hit, verdicts}` (round-2 `keyInputsFor(...)` had no source; `StoreProc` "unchanged" was a contradiction); `KeyContext` replaces realSeams's 8 params; ONE `CacheVerdict` enum (`cv*`, `cvOk`) + `Fetched[T]` replace `CacheError`/`BackendGet`/`BackendPut`/`CacheGetResult`/`LoadOutcome`; `list`/`delete` + A4 DROPPED (can't carry `cachedAt`; remote GC is a non-goal) ⇒ optional `probe(keys)` (s3 ListObjectsV2) for C3c; `BackendConfig.settings` Table DROPPED ⇒ typed `RemoteTier` factories; `dispose` DROPPED (sello `=destroy` wipes; HMAC string unwipeable); `Tier` (was TierConfig), `scheme` vs tier `name` ("l1" pinned), `hmacPolicy`/`ed25519Policy`, `lookup`, `TelemetryEvent` variant, `HttpRequest`/`HttpReply` variant (timeout vs unreachable), `VerifyCache` options object, `noRemoteCache`. **Module split:** `cacheport`/`cachetier`/`cachewire`/`cachememory`/`cachelocalfs`/`cachehttp`/`caches3`/`cachetrust`(only sello/nimcrypto importer)/`cachetelemetry`/`cacheregistry`; `explainMiss` in `keys.nim`; `CacheConfig` types in `types.nim` (config→cacheport→resultcache→depgraph→config would cycle). `RunOptions.cacheRuntime` REMOVED (facade leak) ⇒ internal `runTestsWith(opts, CacheDeps{registry, secrets, sink})`; registry parameterized by `HttpFetcher` so E2E-3 drives the real KDL path.
- **One format everywhere:** local-fs no longer bypasses the serializer / strips attestation (a `file://` verifying L2 had nowhere to put one ⇒ E2E-2 impossible); the L1 file = RFC-0004 file + optional `keyInputs`/`attestation`/`storage` keys, backward-readable; serializer + `memoryBytes` double land in A1. **Local-fs root** (RFC-0006 collision): `resultcache` helpers take a root (`<root>/v<N>/`), L1 root = `<stateDir>/cache` (nimcache slug dirs are siblings, not the port's business), configured `file://<dir>` ⇒ `<dir>/v<N>/`, `autoCreate`/`maxEntries=0`/`ENOTDIR` offline semantics; E2E-1 deletes `v<N>/` only.
- **B0 rewrite:** `std/net` TLS has no deadline (blocking `SSL_connect`) ⇒ `SO_RCVTIMEO/SNDTIMEO`; the select-drained concurrent pass is a ROUND not a slice (second HTTP state machine + non-blocking TLS) ⇒ deferred; replaced by per-tier **circuit breaker** + `probe` + **deferred remote puts** (flush at end-of-run join; puts were inside the poll loop); http status table, body cap 8 MiB, no redirects, no HEAD, EINTR, `pendingSignal()` in prefetch. KDL: **flat top-level grammar** (`remote-cache "n" {}`, `cache-trust {}`, `cache-stats`, `explain-miss`, `verify-cache-pct`, `env-pin`) matching landed config + 0006 D6; key==flag naming. `--json` purity: human lines to stderr. schemaRevision 13/14/15 named per slice. A2b atomic set corrected (4 test files, 9 `CacheSeams` literals, helper). C-dep: sello `v0.4.0` is tagged with the full surface ⇒ git pin, no local pin / `./dev` mount; pre-flight `milpa fetch` + `./dev` same-path mount.

## Resume
Round 3 is APPLIED. **0007 Stage A is complete (2026-09-03) and the A7-gate re-baseline is applied** (§A7-gate re-baseline above; RFC doc updated in the same pass). FORK-2 is resolved (a) and recorded here + in the RFC status line; `/tdd` from slice **B3a** (no remaining prereq — issue #1 landed in 0007 A2b). If Corey wants another architect pass: `/architect docs/rfc/0005-distributed-cache-and-trust.md round 4` (fable).
