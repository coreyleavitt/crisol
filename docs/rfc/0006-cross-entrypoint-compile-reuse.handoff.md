# RFC-0006 — Cross-entrypoint compile reuse — handoff

## ✅✅ RFC-0006 SHIPPED & COMMITTED (2026-07-29). Review + lows complete; committed on `main`.
- Commits on `main`: **`bfda648`** (RFC-0006 implementation, 75 files) + **`5dff9ad`** (low-severity review fixes, 10 files). Both clean (verified: no Co-Authored-By/claude trailer).
- ALL review findings resolved: 1 Crit + 2 High + 16 Medium (R1–R16 + RS1/RC1/RS2/RD1) + 6 Lows (L1–L6). icbaseline.nim kept (empirical IC-broken-under-ORC record); TOCTOU S3 = confirmed non-issue.
- objcache DEFAULT-ON (Corey's call); `--no-objcache` opts out; measurement takes precedence over caching (inverts RFC's old "objCache precedence" wording — deliberate consequence of default-on, flagged to Corey).
- Nothing left pending. RFC-0004/0005 design docs also committed in bfda648.

## ✅ STAGE 4 CODE-REVIEW COMPLETE (2026-07-29) — 0 Critical/High/Medium; SHIPPABLE
3 review rounds. Round-1: 1 Critical + 2 High + 12 Medium (R1–R16). Round-2: objcache DEFAULT-ON flip (Corey's request) + 2 flip-fallout regressions. Round-3 re-review: 4 more Medium (RS1/RC1/RS2/RD1) + Lows. ALL Crit/High/Medium fixed & verified. **Final ship-gate: 127 files, 118 pass + 9 known-benign, test_mem_throttle passed, `./dev check`+`./dev build` clean, RunV1Revision=11.** Only LOWS remain (deferred by mandate — listed below). Refactors landed: shardedledger.nim (−35% ledger dup), measureworker split (workerplan/measureworker/cacheworker). NEXT = Corey's commit decision (all uncommitted on `main`).


- **Stage:** ✅ **RFC-0006 FULLY IMPLEMENTED (Stage M + Stage R) — 2026-07-29.** /loop stopped (stop condition: every slice implemented). Full acceptance: 125 test files exit 0, **1241 [OK]**, zero unexpected failures; `./dev check` clean; binary builds; RunV1Revision=10.
- **NEXT = Stage 4 `/code-review`** of the RFC-0006 scope (ship at 0 critical/high). Then Corey's call on: default-on flip for objcache (currently opt-in), commit/branch (see git flag), and the Stage-M measurement re-run against amoxtli's 373-ep suite if desired.
- **⚑ GIT FLAG:** working branch is now **`main`** (session started on `rfc-0004-incremental-hermetic-execution`); I ran NO checkout — switched externally. ALL RFC-0004/0005/0006 work is uncommitted on `main`. Awaiting Corey before any git action.
- (historical) DECISION GATE EVALUATED (2026-07-29) → STAGE R GREENLIT; FORK-R → native objcache; Stage S closed (r_time high). Stage-3 tdd COMPLETE.
- **⚑ MEASUREMENT RESULT (crisol dogfooded on its own 117-entrypoint suite, `--measure-compile-reuse`, ambientCcache=false, fresh baseline):**
  - Whole-run cost split (cc-time-weighted over 117 compiles): **codegen 54.5% · cc 43.6% · link 1.9%**.
  - Reuse (global, keyed on configHash — the cross-group objcache opportunity): **r_time 0.759, r_size 0.693** (1208s of 1591s per-unit cc-work is redundant across entrypoints). Per-segment (in the `compile` block): integration r_time 0.836/cc 48.4%, unit r_time 0.585/cc 39.5%. `reproducible=true` in BOTH segments (soundness precondition empirically holds).
  - Concrete targets (topUnits): `@psystem.nim.c` (~5.9s cc each) and `@ptables.nim.c` (~4.2s) recompiled near-identically across dozens of entrypoints.
  - **Committed rule: cc% ≥ 40 AND r_time ≥ 0.50 → Stage R.** Satisfied (43.6% & 0.76 global; both segments r_time ≥ 0.5). Honest caveat: codegen (54.5%) > cc (43.6%), so Stage R's savings ceiling is bounded by the cc share — R touches cc only, not codegen/link. IC ruled out (broken under ORC). Bin-packing (codegen lever) is a locked Non-Goal.
- **FORK-R RESOLVED (Corey, 2026-07-29): NATIVE `objcache.nim`.** Rationale accepted: dep-free, composes with stateDir/GC/telemetry, ZERO consumer-side change; the compile-worker child is already built (Stage M) so R2 extends it. Correctness surface (#include-closure hashing + collision-resistant content-addressing + preimage confirmation) is already designed in the RFC. → **R0 DONE.** Now grinding R1–R5.
- **STAGE S CLOSED (not triggered):** decision rule routes to S only when r_time < 0.20; measured r_time = 0.76 (HIGH). S slices will NOT be built. Only Stage R remains.
- **Resume (Stage-R grind):** `/loop implement the next unimplemented RFC slice with /tdd ...` — remaining slices R1 → R2 → R3 → R4 → R5 (Stage R section of RFC). R1 = objcache.nim core (IN PROGRESS next).
- **Final verify:** comprehensive pass (all 232 unit+integration suites, minus known-flaky `test_mem_throttle`) → every file exit 0, **1185 [OK]**, zero real failures; `./dev check` clean. RunV1Revision=9.
- **NEXT (human-in-loop, NOT a code slice):** (1) run Stage-M measurement (`measureCompileReuse` ON) against a REPRESENTATIVE multi-entrypoint suite (crisol's own / amoxtli's) → read `compile.segments` cc%/r_time → apply committed decision rule (cc%≥40 & r_time≥0.5 → Stage R; 0.20≤r_time<0.50 → marginal; r_time<0.20 → S-if-lever-else-close; cc%<40 → close). (2) Stage 4 `/code-review` of the RFC-0006 scope. Stages R (R0 resolves FORK-R) & S remain pre-designed but CONDITIONAL/unbuilt. IC empirically ruled out (broken under ORC here). All RFC-0006 files still UNTRACKED/uncommitted — commit only when Corey asks.
- **Resume RIGHT NOW:** re-dispatch **M-report pass (b1)** to a sonnet subagent (reuse-check alerting default-off/gated + ambient-ccache detection + per-basename top-N — full design in the M-report (b) bullet below). **BLOCKED ON EXTERNAL OUTAGE:** FOUR consecutive (b1) dispatches (~14:00–14:45 on 2026-07-29) DIED on TRANSIENT Anthropic API errors (one server mid-response error, then three 529 Overloaded) — NOT logic failures; every one died BEFORE editing (verified NO partial edits: `grep -rn ReuseCheckConfig\|reuseAlerts\|ambientCcacheDetected\|topUnits src/ tests/` → empty; `./dev check` green). Tree is in clean post-pass-(a) state. Backed off ~50min; (b1) RE-DISPATCHED ~15:41 and currently IN FLIGHT (heartbeat ~16:06). If it dies on another 529, back off again; when API recovers just re-dispatch (b1) fresh (nothing to undo). After (b1): **(b2)** compile-wall-time regression guard → then Stage M DONE → evaluate decision gate against a representative MULTI-entrypoint run.
- **Full /loop resume command:** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; after each slice report one progress line (e.g. "slice 4/8 done, 4 remaining"); stop when every slice is implemented`. RFC + handoff + all new src/test files currently UNTRACKED/uncommitted — commit only if/when Corey asks.
- **Tracking issue:** #4
- **Nature:** research-gated. Stage M (measurement) unconditional + is the decision gate; Stages R/S conditional on M's `r_time` + cost split. Do NOT build R/S before M's findings are recorded. **Committed decision rule** (cc% ≥ 40 & r_time ≥ 0.50 → R; r_time < 0.20 → S-if-lever-else-close; cc% < 40 → close). `r_time` is now **cc-time-weighted** (round-2), not size-weighted.

## Stages / slices (POST-ROUND-2; M unconditional; R/S conditional)
- **Stage M — measure (unconditional):**
  - [x] **M0** artifact telemetry substrate — SEPARATE shard stream `<stateDir>/ledger/artifacts/` (RunLedger has no row-kind field) **+ its own compaction/GC pass** (round-2 F4) — prerequisite, lands first. DONE: new `src/crisol/artifactledger.nim` (`ArtifactRow{entrypointIdentity,groupId,configHash,artifactBasename,keyHash,sizeBytes,ccTimeUs,timestamp,rowVersion}` + open/append/scan + `compactArtifactLedger` = combined compaction+GC), wired into `cleanOrphans` step 7 + `crisol clean` summary; `ledger.nim` provably untouched; full `./dev test` green. groupId/configHash added because IdentityKey is opaque (can't decode group/flags for segmentation).
  - [x] **M-driver** split-compile MEASUREMENT driver (round-2 NEW; resolves F2 M→R coupling) — `nim c --compileOnly` + per-file cc replay matching Nim's `execCmdsInParallel` concurrency + link; overlap-aware codegen/cc/link spans + per-unit cc wall-time; NO caching; measurement-gated (default path stays monolithic). Mechanism-agnostic — needed on every branch. Stage R extends THIS, doesn't reinvent it. DONE: new `src/crisol/compiledriver.nim` (`CompileDriver{compileOnly,runCc,link}` closure-seam like ccprobe.RunProc; `CompileSpans` overlap-aware via monotonic clock; `realCompileOnly`/`defaultRunCc`(=`std/osproc.execProcesses`, the exact primitive Nim's `extccomp.execCmdsInParallel` uses, default `countProcessors()`)/`realLink`/`newMeasureDriver`/`runMeasured` abort-on-first-fail). `parseCompileManifest*` factored out of `closure.nim` (raw unfiltered compile[]+linkcmd); `extractClosure` now a filter over it, byte-identical output (8 existing closure tests green). Full `./dev test` green (1069 [OK], 0 unexpected [FAILED]). runner.nim production path untouched (measurement-gated).
  - [x] **M-golden-fixture** (round-2 NEW; B3/F6) — DONE via FORK-GOLDEN=B (controlled real-Nim, dependency-free). Committed `tests/fixtures/golden_reuse/` (116K): ep_a/ep_b + shared substrate + companion-C-header FFI (`{.compile./.header.}` + real `-I`), real `nim c --mm:orc` output (manifests + curated reusable `.c`). Empirically confirmed: shared units (`fixture.c`, `@pstd@sprivate@sdigitsutils`, `@psystem@sdollars`) byte-identical after 4-line header strip; `@mfixture_substrate.nim.c` REALLY ORC-tailored (ep_a=`substrateA__*` only, ep_b=`substrateB__*` only). Closed-form `r_size=18274/22202≈0.823`, `r_time=400/800=0.5` (weights deliberately non-proportional → proves r_time≠r_size trap). `tests/unit/test_golden_reuse.nim` 12 checks green. Fixture-local `stripHeader` is scaffolding the NEXT slice's production `normalize()` supersedes + re-validates.
  - NOTE: **M-artifact-identity implemented in sequenced passes** (impl sequencing, one RFC slice): (a) DONE — pure golden-validated core `src/crisol/artifactid.nim`: `normalize()` (header-marker-gated 4-line strip + `@response-file` inlining + known cacheDir/binDir erasure), `ccIncludeClosure()` (real `cc -M` via `ccprobe.RunProc` seam), `artifactKeyHash()` (mirrors `keys.chainComponent`/`depgraph.closureContentHash` FNV folds), `reuseRatios()` (segmented by group/config; shared iff keyHash carried by ≥2 entrypoint identities). Golden test rewritten onto production pipeline, re-validates r_size≈0.823/r_time=0.5; real `cc -M` integration test green. **DECISION (design, RFC-aligned):** measurement mode drives the slot's compile via a **measurement worker child** (`--internal-measure-compile <plan.json>`) — the RFC's R2 explicitly presupposes an "already-built M-driver worker," so the worker IS a Stage-M deliverable that R2 extends with cache mode. The WORKER writes ArtifactRows (it holds the per-unit cc times + .c content); parent's :642 role is just spawn+reap. Preserves slot single-pid/killpg/RSS/timeout. Default (measurement OFF) path stays monolithic, untouched. Split for runner.nim-risk isolation: **(b1)** standalone worker binary `<-- DONE`; **(b2)** gated slot wiring in runner.nim `<-- DONE (landed)`; **(b3)** worker-binary seam hardening `<-- IN PROGRESS` (closes the self-reexec fork-bomb finding below before M-artifact-identity is marked clean).
  - **(b2)-FINDING (soundness, RESOLVED-BY-b3, not a fork):** b2 wired the worker via `getAppFilename()` in `spawnCompileStable`. That re-execs *the running process*, not necessarily the crisol binary. For a **library consumer** (amoxtli → `api.runTests(measureCompileReuse=true)`, or any unittest host) the reexec'd `[selfBinary, --internal-measure-compile, plan]` ignores the token and RE-RUNS → spawnCompile → forks again → unbounded fork bomb → watchdog SIGKILL. Sound ONLY when the host process's main() dispatches the token (i.e. the crisol CLI's `runMain`, crisol.nim). crisol IS a library (RFC-0003 facade; amoxtli is a library consumer) → this is a real defect on the library path, NOT hypothetical. Per [[no-fake-forks-soundness]] a fork bomb is never acceptable → one correct hardening (made the call, not escalated): **injectable `workerBinary` seam** — CLI sets it to `getAppFilename()` (provably sound, it handles the token); library default empty → `spawnCompileStable` degrades to monolithic + one-time warn, NEVER bombs. Same seam Stage R's `--internal-compile-worker` reuses. **RFC note owed:** self-reexec worker has an unstated soundness precondition (host main() must dispatch the internal token); record it in the mechanism section.
  - **(b1) DONE:** new `src/crisol/measureworker.nim` (`MeasurePlan{entrypointPath,entrypointAbsPath,flags,nimcacheDir,outputBinPath,groupId,configHash,stateDir}` + `toJson`/`parseMeasurePlan`; `runMeasureCompileWorker`: parse→force `CCACHE_DISABLE=1` (putEnv, child compilers inherit)→`newMeasureDriver`+`runMeasured`→`recordArtifactRows` (parse manifest, skip entry unit `@m<basename>.nim.c`, per reusable unit normalize()+ccIncludeClosure()+artifactKeyHash()+sizeBytes+ccTimeUs → append ArtifactRow) wrapped in try/except so a measurement-layer failure NEVER flips a successful compile's exit code). CLI: `src/crisol.nim` gained `import measureworker` + dispatch of const `InternalMeasureCompileToken` = `--internal-measure-compile <plan.json>`, routed in runMain after -h/--version, before unknown-subcommand check, NOT in usage() (internal). Two ep-path fields deliberate: `entrypointPath`=ep.path (identity input, must match runner.appendAttemptRow's LedgerRow string) vs `entrypointAbsPath`=resolved path actually compiled. normalize()'s known cacheDir/binDir strings DERIVED (nimcacheDir + outputBinPath.parentDir) not carried, single source of truth. Tests: `tests/unit/test_measureworker.nim` (plan round-trip, malformed→clear error, CCACHE_DISABLE injection) + `tests/integration/test_measureworker_real.nim` (real ep→runnable binary + N rows entry-unit-excluded [N=4 for pass_always]; row fields keyHash/sizeBytes/ccTimeUs/group/config match independent recompute; unwritable stateDir doesn't fail compile). Full suite 1121 [OK]/0 unexpected [FAILED]. `runner.nim` provably untouched (git diff empty).
  - [ ] M-artifact-identity — hash at `spCompiling→spRunning` (runner.nim:642, NOT finalize); `normalize()` = EXACT known-cacheDir-string erasure (crisol constructed the paths) NOT regex + `@response-file` content inlining (round-2 F6/depth-7,8); hash Stage-R key material (normalized `.c` + FULL `cc -M` #include closure); record `(ep, basename, keyHash, sizeBytes, ccTimeUs)`; r_time (cc-time-weighted, PRIMARY) + r_size (secondary); segment by group/config; **measurement-mode-gated** (round-2 F4 — else hashes whole substrate ×373/run forever)
  - [x] **M-artifact-identity — DONE** (all passes: a pure core + b1 worker + b2 runner wiring + b3 worker-binary-seam hardening). Fork-bomb finding closed; `Config.workerBinary`/`RunOptions.workerBinary` seam (CLI sets `getAppFilename()`, library default empty → monolithic + one-shot warn). Full suite 1130 [OK]/0 unexpected [FAILED]; `./dev check` clean. RFC note on the self-reexec soundness precondition added.
  - [x] **M-cost-split — DONE.** New `src/crisol/compilecost.nim`: pure `costSplit(spans): CostSplit{codegenPct,ccPct,linkPct}` (total 0 → all 0.0) + RAW per-compile telemetry `CompileCostRow{entrypointIdentity,groupId,configHash,codegenUs,ccUs,linkUs,timestamp,rowVersion=1}` in a lean sharded stream `<stateDir>/ledger/compilecost/<pid>-<bootId>-<seq>.ndjson` (sibling of exec `ledger/` + `ledger/artifacts/`, never mixed) + scan + `compactCompileCostLedger` GC wired into `cleanOrphans` step 8 (+ `crisol clean` summary). Worker (`measureworker.recordCompileCostRow`) records ONE row per compile in its own try/except (never fails compile), identity `identityKey(plan.entrypointPath, plan.configHash)` == the ArtifactRows' identity (verified in integration test). Pcts derived at report time from raw. bootId/seq idiom duplicated a 3rd time (documented; future extraction trigger). Suites green. NOTE: 3 shard-stream consumers now duplicate bootId/seq — earmarked tech-debt, out of scope.
  - [x] **M-IC-baseline — DONE.** Standalone probe `src/crisol/icbaseline.nim` (`IcProbeResult{supported,orcCompatible,firstUs,secondUs,speedupPct,errorMsg}` + `probeIncremental` over injectable `IcRunProc` seam; compiles TWICE with `--incremental:on --mm:orc` sharing one nimcache, CCACHE_DISABLE=1; classifies flag-unsupported / ORC-error / success). NOT wired into production path. 6 unit tests + 1 real-toolchain integration test, suite 1138 [OK]. **Brief-bug caught + fixed (clear-best, not a fork):** bare `--incremental` is invalid Nim — needs a value (`on`/`off`/`v2`/…) per `compiler/commands.nim:987`; corrected to `--incremental:on`.
    - **⚑ EMPIRICAL FINDING (decision-gate context):** in THIS toolchain (Nim 2.2.10-patched), `--incremental:on` IS a recognized option (`supported=true`) but **CRASHES under `--mm:orc` with an internal compiler error `'=copy' operator not found for type string`** (ARC/ORC hook-lookup failure during rod-file serialization) — fails even the COLD compile → `orcCompatible=false`, no speedup measurable. Reproduced 3×. **IC is broken under ORC here → not a viable repeat-compile lever; it does not and cannot close this RFC.** (Also independently reinforces that cross-entrypoint reuse is the only lever in play.)
  - M-report — SPLIT (Opus call, like M-artifact-identity) for focus:
    - [x] **(a) core segmented `compile` run/v1 block — DONE.** New pure `src/crisol/compilereport.nim`: `buildCompileBlock(artifactRows, costRows): JsonNode` (nil when no telemetry) + `readCompileBlock(stateDir)`; reuses `artifactid.reuseRatios` for rTime/rSize/bytes, own pass for artifact counts + cost pcts + `reproducible` (no (identity,basename) pair with ≥2 distinct keyHashes). Additive into `jsonout.toJson`/`toJsonString`/`persistLastRun` via optional `compileBlock: JsonNode=nil` (byte-identical when nil, verified); **`RunV1Revision` bumped 6→7** (also updated `test_c6_jsonout.nim`'s hardcoded ==6). `api.nim` builds+passes the block gated on `cfg.measureCompileReuse`. Suite 1152 [OK]. **First real sample (pass_always.nim, 1 ep):** `ccPct≈0.488, codegenPct≈0.460, linkPct≈0.052, artifactsTotal=4, bytesTotal=208978, rTime=0/rSize=0` (0 sharing expected w/ 1 ep). cc% ~49% ≥40 threshold on a trivial fixture is already suggestive; **the real gate needs a representative MULTI-entrypoint run (crisol's own suite or amoxtli's 373-ep) — that measurement is the decision-gate step AFTER Stage M code lands.**
    - [x] **(b1) durable-report surfaces — DONE (verified by control loop).** New `ReuseCheckConfig{enabled,alertBelow}` on Config (KDL `reuse-check { alert-below 0.5 }`, default-OFF/gated on Stage-R adoption, mirrors perf-check); top-level `reuseAlerts` array (empty when disabled, mirrors `regressions`); run-wide `ambientCcacheDetected` bool (parent-side env/PATH inspection, pure `buildCompileBlock` gets it as param); `topUnits` top-10 by ccTimeUs (tie-break sizeBytes desc, basename asc). `RunV1Revision` 7→8 (updated test_c6_jsonout). Files: compilereport/jsonout/config/types/api + test_config/test_jsonout/test_compilereport/test_measure_compile_gate. NOTE: the (b1) subagent survived 4 transient-API deaths then completed but didn't finalize its report (stopped waiting on a bg `./dev test`); control loop VERIFIED directly — all 6 b1-affected test files exit 0 (167 [OK]), `./dev check` clean. Only failure = `test_mem_throttle` (known-flaky mem-aware scheduler test, ORTHOGONAL to b1's report/config changes; retrying to confirm flake).
    - [x] **(b2) compile-wall-time regression guard — DONE (impl complete; final comprehensive verify running).** `compilereport.computeCompileRegressions` (pure) — per-entrypoint total compile wall (codegenUs+ccUs+linkUs) vs baseline `median + k·MAD` over PRIOR compilecost rows (current run excluded from own baseline); mirrors perf-check's current-vs-history split via `currentRunStartUs` passed from api.nim's `runStartUs`. Emits additive `compile.compileRegressions` array `[{entrypointIdentity,groupId,configHash,currentUs,baselineUs,thresholdUs}]` (always-present-possibly-empty like `regressions`/`reuseAlerts`). RunV1Revision 8→9. **Design call (soundness, not escalated):** did NOT reuse `PerfCheckConfig.k` — perf-check-off zero-values (k=0,floor=0) would degenerate the guard whenever perf-check is off (independent of `measureCompileReuse`); instead independent consts `CompileRegressionK=3.0`/`SampleFloor=10`/`AbsFloorMs=5` numerically matching perf-check's "moderate", documented inline. Agent verified affected files green; control loop running a full unit+integration (minus mem_throttle) acceptance pass to confirm.
    - **⇒ ALL RFC-0006 Stage-M slices implemented.** On green comprehensive verify → /loop STOP condition met (Stage M complete). NEXT (separate, human-in-loop): run the Stage-M measurement against a representative MULTI-entrypoint suite → evaluate the committed decision rule (cc%≥40 & r_time≥0.5 → R; etc.) → Stage 4 /code-review. Stages R/S remain CONDITIONAL/unbuilt.
    - **mem_throttle status (NOT a blocker, orthogonal):** `test_mem_throttle` "mem-aware false … probes overlap" (`anyOverlap` ~line170) FAILS 3/3 even in isolation right now (WSL2 host resource-constrained → probes don't achieve wall-clock overlap). Confirmed NOT RFC-0006-caused (runner.nim diff touches only compile-spawn + `measureworker` import; nothing in `admission`/`memprobe`/overlap scheduling). Documented env-flake per [[dev-test-verification-gotchas]] (memory refined 2026-07-29). All b1 own-tests pass 3/3 in isolation.
  - [ ] M-IC-baseline — IC = repeat-compile-of-same-program; a DIFFERENT question; favorable IC does NOT close this RFC
  - [ ] M-report — measurement (unconditional `run/v1` `compile` block, **SEGMENTED** per group/config: `segments:[{groupId,rTime,rSize,ccPct,codegenPct,linkPct,reproducible,artifacts*,bytes*}]`) SPLIT FROM alerting policy `reuse-check { alert-below 0.5 }` (gated on R adoption — round-2 D5/D8). Force `CCACHE_DISABLE=1` during M (round-2 B5). Per-artifact-basename top-N breakdown feeds S1 (round-2 B6). Compile-wall-time regression guard, median+k·MAD (round-2 B4).
  - all effectful M steps behind injectable `CompileDriver` seam (ONE seam, two modes — measure vs cache; M and R are configs of it, not two abstractions — round-2 D1)
- **Stage R — content-keyed object cache (conditional: cc-bound & r_time≥0.5):**
  - **R-slice progress (grind resumed post-decision-gate):**
    - [x] **R0** FORK-R resolved → native objcache (Corey).
    - [x] **R1 DONE** — `ioutils.atomicPutFile*` (shared, factored out of resultcache which now reuses it, tests green) + new `objcache.nim`: `ObjCacheDecision` enum (mutually-exclusive terminal), `ObjCacheSeams{lookup(keyHash,preimage);store(...)}`, `realObjCacheSeams`, `storeObject`/`lookupObject`, store at `<stateDir>/objcache/v1/<keyHash>.o`+`.meta`, TWO-artifact commit (.o before .meta; FNV payloadChecksum + keyPreimage in .meta), lookup miss on: missing .o / missing-or-malformed .meta (torn commit) / formatVersion mismatch / preimage mismatch (ocdCollisionReject + forensic log) / checksum mismatch. Soft cap (DefaultMaxObjCacheEntries=10000). 5 unit blocks + resultcache regression green; `./dev check` clean.
    - [x] **R2a DONE** — new `objkey.nim` `stageRKey(cContent,ccCmd,knownStrings,nimVersion,ccVersion,ccMRun,readFile): (keyHash,preimage)` (full cc input set; reuses artifactid.normalize×2 + ccIncludeClosure; preimage = injective netstring-framed concat; keyHash=toHex16(fnv1a64)). `compiledriver`: `newCacheDriver(seams,keyOf,concurrency,ccRunner)` (runCc = hit→cp-.o / miss→compile-gate-store; failed cc → no decision, no store), additive `RunCcResult.decisions`, `parseCcOutputObj`, `ObjKeyOfProc`. Measure mode unaffected; `./dev check` clean.
    - [x] **R2b1 DONE** — `InternalCompileWorkerToken="--internal-compile-worker"` + `runCompileCacheWorker` in measureworker (reuses MeasurePlan; captures nim/cc versions once; CCACHE_DISABLE=1; keyOf → entry unit non-cacheable via empty keyHash [new compiledriver `ocdDisabled` branch], reusable → stageRKey; `runMeasured(cacheDriver,...)`). ESCAPE HATCH: any exception / spans-not-ok → monolithic `nim c` fallback, exit as spawnCompileStable. Dispatch added in crisol.nim runMain. **Soundness fix (first-principles, not escalated):** `.c` path derived from ccCmd's LAST token (matches artifactid.deriveCcMInvocation), NOT `nimcacheDir/basename` — vendor `{.compile.}` units (fixture.c) live in source dir, never copied to nimcache. Cross-entrypoint HIT proven by object-count arithmetic (golden ep_a→6 objs, ep_b→7, +1 only = shared 5 reused); entry-unit-private proven (exact reusable count); escape hatch proven via `--run` (fails under `--compileOnly`, monolithic fallback links+runs). Also fixed R2a's stray `std/strutils` unused-import in objkey.nim (control loop). All affected files green; `./dev check` clean.
    - [x] **R2b2 DONE — OPT-IN v1 (Config.objCache default false).** `types.objCache` + KDL `objcache #true/#false`; `RunOptions.objCache`/`noObjCache` + pure `api.resolveObjCache(config,optIn,optOut) = (optIn or config) and not optOut`; CLI `--objcache`/`--no-objcache` (run-only). runner.spawnCompileStable restructured: precedence objCache(cache worker, `InternalCompileWorkerToken`) > measureCompileReuse(measure worker) > monolithic; empty workerBinary+objCache → `warnObjCacheNoWorkerOnce` + monolithic (never getAppFilename → no fork bomb); shared `buildCompileWorkerPlan` helper + `monolithicCompArgs` template dedupe both worker branches. Default-OFF path byte-identical (test: no objcache/ dir created). Real-binary `--objcache` end-to-end populates objcache/v1/; `--no-objcache` overrides. `test_objcache_gate.nim` (8 cases) + `test_api.nim` (7-case precedence matrix) + `test_config.nim` green; binary builds; `./dev check` clean. **⇒ R2 COMPLETE (cache mode wired end-to-end).** Default-on flip = one-liner, deferred to Corey at ship (R5).
    - [x] **R3 DONE** — objcache.nim module-doc: host-local v1 (ambient cc env not in key since raw forkExec not hermetic scrub; CCACHE_DISABLE forced; cross-host = RFC-0005 door) + object-reuse-outside-result-soundness-key rationale. Guards: `test_soundness_key_objcache_independence.nim` (unit — soundnessKey pure + `fieldPairs` runtime enumeration asserts KeyInputs has exactly the 9 RFC-0004 fields, NONE objcache-shaped — structural tripwire); `test_objcache_soundness_independence.nim` (integration — real-binary: per-ep `inputHash` in lastrun.json BYTE-IDENTICAL across `--objcache` / `--no-objcache` / default; field = jsonout.nim:257 `epNode["inputHash"]`, stamped runner ~1311 from `$soundnessKey`). Green; `./dev check` clean.
    - [x] **R4 DONE** — `objcache.gcObjCache(stateDir,maxEntries,maxAgeSecs,nowSecs): GcObjCacheReport{evicted,tmpSwept}`: current-version-dir-only (sibling v<N> preserved), .tmp sweep, orphan half-pair hygiene (unconditional), age-then-size LRU by `.o` mtime (store-time proxy, .meta schema untouched), evicts BOTH `.o`+`.meta`, never raises. Config `objcacheMaxEntries`/`objcacheMaxAgeDays` (KDL objcache-max-entries/objcache-max-age-days, default 0). cleanOrphans step 9 + tuple `objCacheEvicted`/`objCacheTmpSwept` (callers use named fields, no positional breakage) + `crisol clean` summary. 6 unit tests + clean-integration green; binary builds; `./dev check` clean.
    - [ ] **R5 (split a/b like M-report)** — hit-rate/reused-bytes/net-wall-clock reporting, LAST Stage-R slice.
      - [x] **R5a DONE** — `CompileSpans.decisions` additive; `runMeasured` copies `ccResult.decisions` (both paths). New `objcachestats.nim` stream (mirror compilecost) `ObjCacheStatsRow{entrypointIdentity,groupId,configHash,hits,misses,stored,disabled,reusedBytes,timestamp,rowVersion}` at `<stateDir>/ledger/objcachestats/`. `recordObjCacheStatsRow` in cache worker only, own try/except (never fails compile); reusedBytes=Σ ocdHit .o sizes; identity==recordArtifactRows. Convention: hits=ocdHit, misses=ocdMissCompiled+ocdStored, stored=ocdStored⊆misses, disabled=ocdDisabled. GC step in cleanOrphans + tuple `objCacheStatsShardsRemoved`/`Kept` + summary. **Verified invariant (golden ep_a→ep_b same stateDir):** ep_a hits=0/misses=6/stored=6/disabled=1; ep_b hits=5/misses=1/stored=1/disabled=1/reusedBytes>0; hits+misses+disabled==7 both. Green; build+check clean.
      - [x] **R5b DONE (FINAL SLICE — impl complete; full acceptance pass running).** `compile.objcache {hits,misses,stored,disabled,hitRate,reusedBytes,cacheSizeBytes,segments[per group/config w/ hitRate],note}`; ABSENT when no objcachestats rows (back-compat). `note` = CI-shuttle net-impact caveat. Drift visible: `compile.segments[].rTime` (potential) vs `compile.objcache.segments[].hitRate` (realized), same (groupId,configHash) key. RunV1Revision 9→10. Real sample (2 real --objcache runs): hits=4,misses=4,stored=4,disabled=2,hitRate=0.5,reusedBytes=108896,cacheSizeBytes=333860. **Fork resolved (necessary):** `api.readCompileBlock` gate widened `measureCompileReuse` → `measureCompileReuse or objCache` (else a plain `--objcache` run writes stats but never reports them) + `RunReport.compileBlock` field + minimal `crisol: objcache: …` summary line. RunV1Revision bump also touched test_c6_jsonout + test_measure_compile_gate (hardcoded 9→10). Green; build+check clean.
      - **⇒ ALL RFC-0006 SLICES IMPLEMENTED (Stage M + Stage R R0–R5b). /loop stop condition met on green acceptance.** — extend compilereport.buildCompileBlock(+objCacheStatsRows,+cacheSizeBytes)/readCompileBlock(+scanObjCacheStatsLedger+objcache-dir-size) → emit `compile.objcache {hits,misses,stored,disabled,hitRate,reusedBytes,cacheSizeBytes,segments[per group/config],note}`; ABSENT when no rows (back-compat). `note` = CI-shuttle net-impact caveat (reusedBytes benefit vs cacheSizeBytes shuttle cost; crisol can't measure real CI save/restore). Drift = realized per-seg hitRate vs potential per-seg rTime (already in segments), keyed same (groupId,configHash). RunV1Revision 9→10 (+update test_c6_jsonout). Summary line. **⇒ completing this = ALL RFC-0006 slices done.**
      - **R2a design:** new `objkey.nim` `stageRKey(cContent,ccCmd,knownStrings,nimVersion,ccVersion, ccMRun, readFile): (keyHash, preimage)` — full cc input set (normalize(ccCmd) ⊕ normalize(cContent) ⊕ cc-M closure ⊕ nimVersion ⊕ ccVersion), reusing artifactid.normalize/ccIncludeClosure; preimage = framed canonical concat (stored in .meta, confirmed on hit). **NOTE: this is RICHER than Stage-M's `artifactKeyHash`** (which keyed only normalized-.c + closure for reuse DETECTION); the object CACHE key must also bind cc flags + toolchain versions for soundness. `compiledriver.newCacheDriver(seams, keyOf, cc-run)` — runCc = lookup→cp-.o-to-objOut (ocdHit) / miss→compile-gate-on-exit→store (ocdStored/ocdMissCompiled); additive `RunCcResult.decisions` (measure mode leaves empty). `parseCcOutputObj(ccCmd)` extracts `-o <obj>`.
      - **⚑ DEFERRED DECISION (R2b, will surface to Corey or make-and-note): is cache mode DEFAULT-ON (opt-out `--no-objcache`) or OPT-IN v1 (gated like measureCompileReuse, `--no-objcache` reserved)?** Lean: opt-in v1 (new correctness-critical compile path + known first-wave dedup gap + big blast radius on amoxtli), default flip is a one-liner once Stage R proven. RFC's `--no-objcache` phrasing implies eventual default-on. Not blocking R2a.
  - [ ] (superseded R1 bullet) `objcache.nim`+`ObjCacheSeams{lookup(keyHash,preimage);store(...)}`+`ObjCacheDecision{ocdHit,ocdMissCompiled,ocdStored,ocdDisabled,ocdSoftCapSkipped,ocdCollisionReject}` (mutually-exclusive terminal states, RFC-0004 M8 discipline — round-2 D3)+shared `ioutils.atomicPutFile`+**TWO-artifact commit** `<keyHash>.o`+`<keyHash>.meta`(checksum+**key preimage**), .o before .meta, missing .meta⇒miss (round-2 F5)+read-time checksum AND **preimage confirmation** (collision defense — round-2 depth-1 HIGH) · **R2** wire objcache layer into the M-driver worker → cache mode + monolithic-`nim c` fallback escape hatch on ANY objcache error (round-2 B1/D2/F1) · R3 host-local scope + compile-env constraint · R4 dedicated `gcObjCache` from `cleanOrphans` (objcache/ is a 3rd sibling pruneDir NEVER walks — risk is unbounded growth not deletion, round-2 F7; preserve all v<N>, sweep orphaned .tmp — round-2 B7/B9) · R5 hit-rate + reused-bytes + **net wall-clock incl CI cache save/restore** (round-2 depth-6)
  - **MECHANISM (round-2 REWRITE):** NOT a generated shell script. A **crisol compile-worker child** `crisol --internal-compile-worker <plan.json>` spawned via existing argv `forkExec` = slot's ONE pid (killpg/RSS/timeout unchanged). Worker (is Nim, has objcache/closure/ioutils in-proc): compileOnly→parseCompileManifest(raw)→per-cc key+confirmed-hit-cp/miss-compile-gate-on-exit-store→link→fallback-to-monolithic on any objcache error. NO shell text. Resolves D1+D2+F1+B1+depth-2 simultaneously.
  - key = normalize(ccCmd) ⊕ cContent ⊕ **full `cc -M` #include closure** ⊕ nimVersion ⊕ ccVersion; digest→filename, FULL preimage stored in .meta + confirmed on hit
  - config: FLAT scalars `objcacheMaxEntries`/`objcacheMaxAgeDays` (match LANDED resultcache, NOT unlanded 0005 block — round-2 D6); `--no-objcache` reverts fully to monolithic, INDEPENDENT of `--no-cache` (round-2 D7)
- **Stage S — DCE-relaxation (conditional: r_time low AND S0 finds a lever):**
  - [ ] **S0** capability check — no `--dce` in `nim --fullhelp` 2.2.10 → S may be infeasible → "close" branch · S1 config (global-only v1; populated data-driven from M's per-module breakdown) · S2 emit-all threading · S3 soundness guard (the deep part) · S4 tradeoff report
  - adjacent option: precompiled substrate static-lib SEED

## Open forks (awaiting Corey / architect)
- **FORK-GOLDEN — RESOLVED (Corey, this session): option (B) controlled real-Nim fixture, dependency-free.** RFC's "chronos" read as illustrative; essential requirement = companion-.h + `-I`-variance + real ORC DCE tailoring across 2 entrypoints, closed-form-verifiable. Below kept for provenance.
- **FORK-GOLDEN (M-golden-fixture substrate) — was BLOCKING; now resolved → B.** RFC says "chronos-importing 2-entrypoint real fixture." Reality: chronos is NOT a crisol dep and NOT installed (`crisol.nimble` requires only `nim >= 2.0`); scratch `tests/fixtures/tmp` holds only stdlib-only real artifacts (RFC rules those out — no companion `.h`, no `-I` variance). The fixture is the ORACLE all later M slices trust. Options: (A) temp-install chronos, generate+commit real chronos `.c`/`.json`, no permanent dep — max fidelity to amoxtli target but heavy/fragile install, big fixture, closed-form `r_time` infeasible by hand; (B) **[RECOMMENDED]** controlled real-Nim fixture: 2 entrypoints over a shared local substrate module using a companion C header (`{.compile./.header.}` + `-I`) engineered so the two entrypoints reach different proc subsets → REAL ORC DCE tailoring, small enough for closed-form `r_time`/`r_size`; dependency-free (matches crisol's discipline that FORK-R leans on), hermetic, fast, still real Nim 2.2.10 ORC codegen — sacrifices only "literally chronos" (RFC's own words generalize the requirement to companion-.h + `-I`-variance); (C) light real nimble pkg w/ C header — middle ground, still an external dep, none installed. Resolve before M-golden-fixture; M-artifact-identity depends on it.

### (originally: none blocking Stage M)
- **FORK-R (Stage R mechanism)** — genuine, informed fork, tension both ways (rounds 1–2):
  - **native** `objcache.nim`: dep-free, composes with stateDir/GC/telemetry, ZERO consumer-side change — BUT new correctness-critical surface + must reimplement BOTH ccache's header-manifest hashing AND collision-resistant content-addressing/confirmation (round-2 depth-1 sharpened this). Split-compile worker is a NATIVE-arm cost.
  - **ccache**: battle-tested, direct mode ALREADY solves #include-closure AND collision problems, wraps cc so works with MONOLITHIC nim c (no worker needed) — BUT must be in every CONSUMER's image, its dir/GC/env fight stateDir.
  - **sccache**: same + native S3/redis → RFC-0005 distributed-object door + mature content-addressing; same consumer-image cost.
  - Reached ONLY if Stage M greenlights R. Resolve at R0. NOT blocking.

## Key decisions (cumulative)
- Adopted issue #4's problem, rejected its solution shape (Direction 1 unsound under ORC whole-program DCE — empirically confirmed). Key on CONTENT + full cc input set, never module identity.
- Research-gated: measure `r_time` (cc-time-weighted, round-2) first; committed decision rule pins mechanism selection.
- Round-2 architectural pivot: **compile-worker re-exec** supersedes the generated shell script as R2's mechanism (linchpin — 4 lenses converged; resolves the single-pid/seam/shell-injection/escape-hatch cluster at once).
- Round-2 soundness hardening: read-time checksum guards torn writes NOT digest collisions → store+confirm key PREIMAGE (two-artifact .o+.meta commit).
- Round-2 metric fix: decision is about TIME → r_time is cc-time-weighted; r_size is secondary (storage planning).
- Round-2 key completeness: #include closure = full `cc -M` (incl system/libc headers), not nimcache-local; response-file content inlined.
- Round-2 M→R honesty: M-driver (measurement split-compile, no cache) is a genuine Stage-M prerequisite AND mechanism-agnostic; Stage R extends it. "No fork blocks Stage M" survives.
- Soundness reframed honestly: content-keying sound ONLY if key captures FULL cc input set AND hit is preimage-confirmed; an unsound `.o` CAN taint the RFC-0004 result cache indefinitely → Stage R correctness is a PRECONDITION for 0004, not severable.
- objcache HOST-LOCAL in v1; cross-PROJECT sharing excluded (whole-run F_SETLK lock scoped to stateDir → shared stateDir serializes unrelated runs — round-2 B2; future `CRISOL_OBJCACHE_DIR` is the door). Cross-HOST deferred to RFC-0005 door.
- Boundary: bin-packed unified binary excluded ([[boundary-granularity-discriminator]] backtrack). Persistent Nim compiler-server researched-and-closed (no such mode in mainline — round-2 B10).

## Round-2 review ledger (architect, 4 lenses; ALL findings applied to RFC unless noted)
| finding | lens(es) | status |
|---|---|---|
| shell-script R2 = injection surface + secretly 2 children + shell-reimpl of store → **compile-worker re-exec** | design+feasibility(+breadth escape-hatch) | applied (R2 rewrite) |
| read-time checksum ≠ collision defense; FNV over near-identical substrate collides → store+confirm key preimage | depth (HIGH) | applied (two-artifact .o+.meta) |
| size-weighted r drives a TIME decision → r_time cc-time-weighted primary, r_size secondary | depth | applied (decision rule + M) |
| #include closure scoped nimcache-local → misses system-header backport → full `cc -M` closure | depth | applied |
| M-cost-split needs split driver → hidden M→R coupling → M-driver as mechanism-agnostic M prerequisite | feasibility (HIGH) | applied |
| CompileDriver seam & R2 don't compose → ONE seam, two modes (measure/cache) | design | applied |
| ObjCacheDecision miss/stored conflation (RFC-0004 M8 déjà vu) → mutually-exclusive terminal enum | design | applied |
| M-report flat but segmentation demanded → segmented `compile` block | design | applied |
| alert surface ships down "close" branch → split measurement (unconditional) from alerting policy | design | applied |
| objcache checksum can't embed in raw .o → two-artifact commit + ordering | feasibility | applied |
| extractClosure filters out substrate → factor raw `parseCompileManifest` | feasibility | applied |
| M-artifact-identity ungated hot-path cost + no artifact-stream GC | feasibility | applied (measurement-gate + GC) |
| no golden real-compiler fixture (gitignored scratch only) → M-golden-fixture slice | breadth+feasibility | applied |
| no compile-wall-time regression guard | breadth | applied |
| ambient ccache pollutes cc% → CCACHE_DISABLE=1 during M | breadth | applied |
| S1 needs per-module breakdown to author list | breadth | applied |
| objcache multi-version GC + tmp-orphan sweep contracts | breadth | applied |
| compiler-server not researched (asymmetry vs S0) | breadth | applied (Non-Goal researched-closed) |
| config block vs landed resultcache flat scalars → flat scalars | design | applied |
| --no-cache/--no-objcache independence; --no-objcache reverts to monolithic | design+breadth | applied |
| cross-project shared-stateDir serializes via F_SETLK lock | breadth | applied (Non-Goal + future OBJCACHE_DIR door) |
| config `compile-check` name collision → `reuse-check` | design | applied |
| normalize = exact known-string erasure not regex; response-file inlining | depth+feasibility | applied |
| R4 pruneDir rationale factually wrong (3rd sibling dir, growth not deletion) | feasibility | applied (rationale fixed) |
| runner.nim:376 cite 3 lines off (→:379) | feasibility | applied |
| overlap-aware span timing (not summed durations) | depth | applied (M-driver) |
| debug-info DWARF path on cross-slot hit — harmless caveat | depth | applied (one-line note) |
| EXDEV safe (hit=cp, populate-rename within objcache/) | breadth | noted in RFC |

## Round-1 review ledger (architect, 4 lenses)
All 4 completed; ~25 clear-best fixes applied; FORK-R surfaced. Highlights:
| finding | lens(es) | status |
|---|---|---|
| `.c` header path-poisoning → r≈0 (verified) | breadth | fixed (normalize) |
| Stage R wrong files (`@m` vs `@p`; chronos `@p`) (verified) | depth | fixed (reusable set = all but entry unit) |
| ledger row-kind conflation (vs parseRow) | design+feasibility+depth | fixed (M0 separate stream) |
| soundness oversold (key incomplete) | depth | fixed (full cc input set + host-local v1) |
| unsound `.o` taints result cache | depth | fixed (precondition reword) |
| `pruneDir` deletes objcache | breadth+feasibility | fixed (gcObjCache) |
| `--dce:off` may not exist | depth+design | fixed (S0 gate + close branch) |
| IC solves a different problem | breadth+depth | fixed (reframed) |
| missing seam/checksum/enum/thresholds/CI-persist/config/report-shape | design+breadth+feasibility | all fixed |

## Review ledger (stage 4 — round 1, 2026-07-29; 5 reviewers: correctness/security/design/quality/test-coverage)
User directive alongside review: **flip objcache to default-ON** ("only one user right now"). Gate: the default-on flip MUST land only AFTER R1(C1)+R2(Q1)+R4(C2) soundness fixes + R14 soundness tests, else every run inherits the wrong-hit surface.
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R1 | **CRIT** | `objkey.stageRKey` (objkey.nim:102) drops `ccIncludeClosure.ok`; failed `cc -M` → empty `inc` component → normal non-empty key → wrong-header `.o` served as preimage-CONFIRMED hit. `keyOf` (measureworker.nim:620) doesn't check either. Asymmetric w/ recordArtifactRows:262 which DOES. Trigger: cc -M probe failure under load, or `deriveCcMInvocation` splitWhitespace mis-tokenizing a shell path w/ a space. | open (CONFIRMED — correctness+security independently; verified at source) | fix: propagate `.ok` → empty keyHash (non-cacheable) like the other degrade branches; make deriveCcMInvocation fail-safe on odd tokenization |
| R2 | **HIGH** | objcache key omits libc fingerprint: `ccVersion=captureFirstVersionLine("cc")` (measureworker.nim:577) is cc-only; RFC-0004 `ccprobe.ccVersion` folds `cc\|ldd` (ccprobe.nim:112). libc-only upgrade → stale-ABI `.o` confirm-hit. | open (CONFIRMED at source) | fix: reuse `ccprobe.ccVersion(realRun)` in runCompileCacheWorker |
| R3 | HIGH(design) | sharded-NDJSON ledger idiom duplicated 4× (ledger + artifactledger + compilecost + objcachestats, ~1700 lines); each module's own doc names the "extract when a 3rd/4th consumer appears" trigger — now fired. | open | extract `shardedledger.nim` generic + typed codec per stream |
| R4 | MED | `deriveCcMInvocation` doAssert (artifactid.nim:196) raises `Defect`, NOT caught by worker `except CatchableError` (measureworker.nim:645) → worker dies, monolithic escape hatch bypassed. | open (CONFIRMED at source) | graceful degrade (ok=false) instead of doAssert |
| R5 | MED | Stage-M `artifactKeyHash` omits `normalize(ccCmd)` that Stage-R `stageRKey` includes → M over-counts sharing → measured r_time=0.76 gate # is a slight overestimate. Benign within-segment (flags constant) today but unproven; RFC claims "same key material" (contradiction). | open (CONFIRMED at source) | fold normalize(ccCmd) into artifactKeyHash (make M-key a true prefix of R-key) |
| R6 | MED | objcache has entry-count cap only (default 10k), NO aggregate-byte cap; `.meta` stores full `.c` preimage; age default 0 (off); GC only on explicit `crisol clean` → multi-GB unbounded growth on large suites. | open | add byte-size cap to gcObjCache + soft-cap check |
| R7 | MED | RFC's promised low-confidence gate for narrow (`--changed`) runs NOT implemented; `compile.segments` aggregates ALL historical ledger rows, no sample-size/as-of field → rTime misleading on thin runs. | open | implement low-confidence gate + emit sample-size/as-of |
| R8 | MED | `measureworker.nim` (652 lines) fuses measure+cache workers + plan schema. | open | split → workerplan.nim / measureworker.nim / cacheworker.nim |
| R9 | MED | R5b drift tie-in (potential rTime vs realized hitRate) can't populate in ONE run (objCache/measure mutually exclusive per slot; cache worker emits no artifact rows) → "works" only via R7 stale-history blend. | open | document limitation OR have cache worker also emit ArtifactRows |
| R10 | MED | `atomicPutFile` returns bool only; `resultcache.storeCached` lost its specific OSError msg (perm/disk-full/EXDEV) → harder prod diagnosis. | open | return `(ok, error)`; restore specific log at call sites |
| R11 | MED | stale docstrings in compiledriver/artifactid/measureworker call Stage R / pass(b2) "later/unbuilt" though implemented in same tree. | open | update docstrings |
| R12 | MED | `readXShard`/`readAllXShardRows` byte-identical pairs in all 3 new streams (ledger.nim's differ by identity filter — justified there, not here). | open | collapse each pair (subsumed by R3 extraction) |
| R13 | MED(design) | explicit `--objcache`/`--measure-compile-reuse` with no workerBinary degrades silently to monolithic via one-shot stderr only, not structured `warnings[]` → CI consumer gets silent no-op. | open | route through ConfigWarning/warnings[] |
| R14 | MED(tests) | soundness/robustness test gaps: both-flags-together (T1), objcache-max-* KDL parse (T2), formatVersion-mismatch + malformed-.meta miss via lookupObject (T3), reuse-check range (T4), readCompileBlock adversarial (T5), api or-gate (T6), normalize false-positive (T7). | open | add tests (T1/T3 required before default-on flip) |
| R15 | LOW | usage() missing `--measure-compile-reuse` (D7); `icbaseline.nim` unreachable prod surface (D8); workerBinary doc omits --objcache (Q5); spawnCompileStable plan-write dup (Q6); TOCTOU key-vs-compile in trust boundary — non-issue (S3); minor test gaps T8–T13. | open (batch/defer) | Lows — address opportunistically |
| R16 | MED | (NEW, introduced by R9 fix) cache worker emits ArtifactRows with `ccTimeUs=0` for HITS → skews cc-time-weighted potential-rTime AND pollutes the append-only measurement ledger permanently. | fixed via R9-revert (round 2a) | resolution: REVERT R9 row-emission; resolve D5 drift by honest docstring (comparison spans a measure-run + cache-run, now transparent via R7 provenance fields) |

## Round 1 fix outcome (2026-07-29) — 3 file-disjoint sonnet agents, all green
Agent 1's final FULL `./dev test` (combined tree, all 3 agents' changes on disk): **1306 [OK], 0 [FAILED]; `./dev check` + `./dev build` clean.**
- **Agent 1 (gate):** R1/R1b/R4 (stageRKey→(keyHash,preimage,ok); deriveCcMInvocation shell-aware `shellSplit` + ok flag, no doAssert; `buildCacheKeyOf` seam checks ok→non-cacheable). R2 (`captureToolchainCcVersion`→ccprobe.ccVersion, folds cc|ldd). R5 (artifactKeyHash +normalizedCcCmd trailing param, M-key = prefix of R-key; golden r_size/r_time unchanged). R9 (cache worker emits artifact rows — **SUPERSEDED, see R16**). R11 docstrings. **R14-T7: real bug fixed** — normalize() blind-substring erasure corrupted superstring paths → boundary-guarded `eraseKnownString`.
- **Agent 2:** R6 (objcacheMaxBytes + byte-cap gcObjCache, both-artifact atomic eviction, threaded from cleanOrphans). R10 (atomicPutFile→(ok,error), specific OS error restored at resultcache+objcache call sites). R14-T3 (formatVersion-mismatch + malformed-.meta miss via lookupObject). R14-T4 (reuse-check range).
- **Agent 3:** R7 (LowConfidenceMinEntrypoints=5; segments carry currentRunEntrypoints/sampleEntrypoints/lowConfidence; reuseAlerts suppressed on low-confidence; **RunV1Revision 10→11**). R13 (ConfigWarning on objcache/measure-requested-but-no-workerBinary, in api.planImpl, reuses existing shape). R14-T1 (both-flags precedence via compilecost-empty discriminator). R14-T5 (readCompileBlock adversarial). R14-T6 (shouldReportCompileBlock predicate + tests).
- **NOTE (pre-existing, out of RFC-0006 scope):** test helper `buildCrisolBinary()` compiles into an UNSCOPED nimcache → transient collision under parallel test builds (Agent 3 hit + re-ran clean). Latent test-harness flake, not an RFC-0006 defect.

## Round 2 plan
- **2a (parallel, disjoint):** (1) objcache DEFAULT-ON flip (gate R1/R2/R4 now green) — Config.objCache default true, --no-objcache opt-out preserved, update default-off tests. (2) R16/R9-final: revert cache-worker artifact-row emission + honest drift docstring.
- **2b (parallel, disjoint, after 2a green):** R3 extract `shardedledger.nim` (+ R12 collapse pairs), preserving public append/scan/compact signatures. R8 split measureworker.nim → workerplan/measureworker/cacheworker.
- **Round 3:** final re-review (Security + Design + Correctness) over full diff; verify; ship at 0 C/H/M.

## Round 2a outcome + acceptance (2026-07-29)
- **R16/R9-final DONE** (agent a76e): cache worker no longer writes ArtifactRows (grep-verified: only measure worker does); drift docstring corrected to honest CROSS-RUN framing (measure-run rTime vs cache-run hitRate, keyed by group/config, transparent via R7 provenance). Tests inverted. `./dev check` clean.
- **DEFAULT-ON FLIP DONE** (agent accf): default flipped in config.nim (docToConfig var default true + conventionConfig literal), NOT types.nim bare field (low-level Config(...) stays off → test helpers unaffected). resolveObjCache formula unchanged. Library-safety regression test added (default-on + empty workerBinary → monolithic + R13 warning, no fork-bomb). Fixed latent bug: measure-only tests needed explicit noObjCache:true isolation. usage() updated.
- **FULL ACCEPTANCE (agent a84e, 126 files direct-run): 1314 [OK]**, `./dev check`+`./dev build` clean — BUT surfaced TWO flip-fallout issues (now being fixed by agent ac53):
  - **ISSUE 1 (confirmed regression):** `--measure-compile-reuse` alone STARVED — default-on objCache wins spawnCompileStable precedence → cache worker runs instead of measure worker → no ArtifactRows/segments/topUnits. `test_measure_compile_gate.nim` fails 2/2. FIX: measure > objCache precedence (measurement suppresses default-on objcache; warn if both explicit). **⚑ CONSEQUENCE FLAGGED TO COREY:** inverts RFC's old "objCache takes precedence" rule — that rule assumed opt-in objcache; under default-on it's wrong to let a default swallow explicit measurement.
  - **ISSUE 2 (flip-exposed self-reexec unsoundness):** `runMain` unconditionally sets workerBinary=getAppFilename(); in-process runMain (tests) → foreign binary self-reexec'd as worker → unsound (flaked test_jsonout, masked by cached state). FIX: move getAppFilename() injection OUT of runMain into crisol's real top-level entrypoint, thread as `runMain(selfWorkerBinary="")` param → real CLI sound, in-process callers safe monolithic. Kills the whole class.
- **Flip-fallout fixes DONE (agent ac53):** Issue 1 — measurement suppresses default-on objcache (`api.planImpl`: effective objCache = resolveObjCache(...) and not measureCompileReuse; contradictory-both-explicit → ConfigWarning); runner branch order untouched. Issue 2 — `runMain(selfWorkerBinary="")`; only real-CLI isMainModule passes getAppFilename(); in-process callers safe monolithic. Token dispatch + R16 intact. 4 scoped tests + adjacents + check/build green.

## Round 2b outcome (refactors under green, 2026-07-29)
- **R3 + R12 DONE (agent a1f3):** new generic `shardedledger.nim` (466 lines); artifactledger/compilecost/objcachestats reimplemented as thin instantiations; **552-line (35%) reduction**; R12 duplicate shard-readers collapsed; ALL public signatures preserved (zero test edits); per-stream independent boot-id/seq via `{.global.}` in generic procs (per-type monomorphization). `ledger.nim` left out of scope (identity-filter difference). 6 scoped + 7 caller tests + check/build green. No behavior change.
- **R8 DONE (agent afa7):** `measureworker.nim` (745 lines) split → `workerplan.nim` (MeasurePlan/toJson/parseMeasurePlan/forceMeasurementCcEnv/entryUnitBasename/tokens) + `measureworker.nim` (~230, measure-only: runMeasureCompileWorker/recordArtifactRows/recordCompileCostRow) + `cacheworker.nim` (runCompileCacheWorker/runMonolithicCompile/buildCacheKeyOf/captureToolchainCcVersion/recordObjCacheStatsRow). No renames/signature changes; callers' imports updated (crisol.nim/runner.nim/3 tests). selfWorkerBinary/dispatch/R16 intact. 24 scoped + 4 caller suites + check/build green.

## Round 3 verdict (2026-07-29) — full acceptance GREEN; re-reviews found 4 new Mediums + Lows
- **FULL ACCEPTANCE: ACCEPT** (agent afa7, 126/126 files, 117 clean + 9 benign, mem_throttle passed, check+build clean).
- **Re-reviews confirm all round-1/2 fixes HOLD** (R1/R1b/R4/R2/R5/R16/R14-T7/flip/precedence/R3/R8 all verified sound at source). New findings (all MEDIUM, in-mandate; being fixed by agent a6139 [correctness batch] + a follow-up test-org agent):
  - **RS1 (sec, soundness):** `deriveCcMInvocation` allow-lists only -I/-D/-std → drops -isystem/-iquote/-include/-nostdinc (via --passC) → cc -M probe searches different dirs → in-place header change = wrong-hit confirmed. FIX: preserve all flags, denylist only -c/-o<obj>.
  - **RC1 (correctness, feature no-op):** `shellSplit` not exported; `compiledriver.parseCcOutputObj` + `cacheworker.buildCacheKeyOf` cPath still use `splitWhitespace` → objcache SILENT no-op under whitespace paths (WSL2 mounts). NOT unsound (safe degrade). FIX: export shellSplit, reuse both.
  - **RS2 (sec, resource):** cacheworker calls realObjCacheSeams WITHOUT configured objcacheMaxEntries → write-time soft cap ignores user config (10_000 hardcoded); no auto byte-bound on default run path (only crisol clean). FIX: thread caps into MeasurePlan→worker; storeObject soft cap honors entry+byte config.
  - **RD1 (design, test-org):** R8 split production but not tests — test_measureworker.nim tests workerplan+cacheworker; no test_cacheworker.nim/test_workerplan.nim. FIX: split test files to follow modules.
  - **Lows (fold cheap / defer):** types.nim vs api.nim contradictory precedence docstrings (fold into a6139); ledger.nim R3-exclusion docstring rationale imprecise (real reason = field-shape not identity-filter); cleanOrphans 14-field tuple could nest CompactReport; eraseKnownString leading-boundary caveat (not triggerable today).
- **ROUND-3 FIXES DONE + VERIFIED:**
  - **RS1 DONE (agent a6139):** deriveCcMInvocation rewritten allow-list→denylist (keep all flags verbatim, drop only -c / -o<obj> both forms; +-M, +source; shell-aware). Control loop VERIFIED at source (artifactid.nim:327-379): -isystem/-include now preserved. Soundness hole closed.
  - **RC1 DONE (agent a6139):** shellSplit exported; parseCcOutputObj + buildCacheKeyOf cPath now shell-aware. Whitespace-stateDir end-to-end store proof added.
  - **RS2 DONE (agent a6139):** MeasurePlan carries objcacheMaxEntries/MaxBytes; runner sets from config; storeObject/realObjCacheSeams take maxBytes; write-time soft cap honors entry+byte config (one dir scan, skips new keys over EITHER, replacements proceed, 0=unbounded). types.nim doc corrected. Control loop VERIFIED at source (objcache.nim:296-346).
  - **RD1 DONE (agent aa82):** test_measureworker.nim split → test_workerplan.nim (12) + test_cacheworker.nim (7); old file DELETED (measure-mode coverage lives in integration test_measureworker_real). Green.
  - **Doc Low DONE:** types.nim objCache precedence comment corrected (mutually-exclusive-by-construction via planImpl).
- **FINAL SHIP-GATE (agent afa6, IN FLIGHT):** full 126-file acceptance re-run over final tree (unit set now ~83 files: test_measureworker deleted, test_workerplan+test_cacheworker added). On green → REVIEW COMPLETE at 0 C/H/M.
- **Remaining = LOWS ONLY (deferred by mandate; noted for Corey):** ledger.nim R3-exclusion docstring rationale imprecise (field-shape not identity-filter); cleanOrphans 14-field tuple could nest CompactReport; eraseKnownString leading-boundary docstring caveat; R15 batch (usage() --measure-compile-reuse [likely now fixed via flip agent's usage edit — reverify], icbaseline unreachable, workerBinary doc, spawnCompileStable plan-write dup, TOCTOU non-issue).
- **THEN: commit decision (Corey).** All RFC-0004/0005/0006 work still uncommitted on `main` (⚑ git flag). Nothing committed this session.

## Round 3 (superseded — was IN FLIGHT) — final gate
Launched in parallel over stable tree: full acceptance suite run + correctness/security/design re-reviews (verify fixes hold + hunt newly-introduced C/H/M). Ship at 0 C/H/M. **ALL Crit+High+Medium findings (R1–R16) fixed; flip done + fallout fixed; refactors done.** Remaining: round-3 verdict, then commit decision (Corey — all still uncommitted on `main`). Lows (R15) deferred by mandate.
- **NEW-modules-since-round-1** (in round-3 review scope): shardedledger.nim, workerplan.nim, cacheworker.nim.
- RunV1Revision now **11** (R7).
