# RFC-0004 — Incremental hermetic execution — handoff

- **Stage:** 🏁 **CLOSED** — RFC-0004 implemented (27 slices), reviewed (3 rounds to floor, 0 critical/high), MERGED to `main`, PR resolved, amoxtli repinned. All follow-on issues (#3) closed and the `rfc-0016-c1b-state-dir` divergence reconciled. Nothing blocking remains.
- **Landed on `main` (linear):** `87994fc` (RFC-0004) → `e36b7ea`/`b8ccb13` (issue #3 fixes) → `891058b` (CRISOL_STATE_DIR) → `1c0b475` (import cleanup). Full suite **1052 [OK]**, `./dev check` clean, all commit messages trailer-clean.
- **Deferred, non-blocking (future work, NOT RFC-0004):**
  - **Remaining Lows** — the R1/R3 Low batch documented in the review ledger below, left intentionally per the fix-through-Medium mandate.
  - **Issue #1** — explicit `SlotState` enum to replace the `pepIdx == -1` idle-sentinel (R3-1 deferred; pre-existing, core-dispatch-wide, out of review scope). A future `/architect` candidate.
- **Verification method (IMPORTANT, still applies repo-wide):** `./dev test` EXIT code is unreliable — grep the captured log for `\[FAILED\]`/nimble-exception, excluding expected `fail_always`/`fail_compile` fixtures. test_mem_throttle is load-flaky (passes in isolation). See [[dev-test-verification-gotchas]].
- **Everything below is the historical implementation + review record, retained for reference.**

## Slice progress (26 active slices; A1c deferred to after C5)
Order: A2-pre → A3 → A5 → A2 → A1a → A1b → A4a → A4b → A4c → A4d → **A6 ✓ → A7 ✓** → A8 → A9 → B0 → B1 → B2 → B3 → B4 → C0 → C1 → C2 → C3 → C4 → C5 → C6.
- [x] **A2-pre** — `ccprobe.nim` (cc/libc version probe, injectable RunProc seam, sentinels for missing cc/ldd). 13 tests. `tests/unit/test_ccprobe.nim`.
- [x] **A3** — `sandbox.nim` + types (`HermeticLevel`, `RlimitConfig`, `SandboxSpec`, `SandboxAchieved`, `isFullyAchieved`, `resolveSandbox`, published `DefaultEnvAllowlist`+`DefaultEnvAllowlistPrefixes`). 18 tests. `tests/unit/test_sandbox.nim`. Pure resolution only — spawn/config/CLI wiring deferred to A5/A4/A9.
- [x] **A5** — `sandbox.filterEnv` + `hermeticEnvHash` (FNV, excludes TMPDIR value); new `forkExecEnv(...,spec)` overload in spawn.nim; fixture `env_probe.nim`. 12 tests (`test_envscrub.nim` + `test_env_scrub_integration.nim`). NOTE: spec-driven overload exists but NOT yet threaded into the live run path — wiring lands in A4a/A6.
- [x] **A2** — `keys.nim`: `identityKey(path,flagHash)` + `soundnessKey(KeyInputs)` chained-FNV (9 components, NUL-sep, per-component fnv1a64-last). `IdentityKey`/`SoundnessKey` distinct strings in types.nim. XOR-swap + NUL-alias negative cases PASS. Fixed stale depgraph.nim XOR docstrings (lines 21, 73). `test_keys.nim`.
- [x] **A1a** — `resultcache.nim`: `ExecutionCache` per-key files `<stateDir>/cache/v<fmt>/<key>.json`, `CachedResult` (reuses Outcome/TestRecord from types), atomic write (O_CREAT|O_EXCL→rename), FNV `payloadChecksum`, version/checksum→miss, interim soft-cap (`DefaultMaxCacheEntries=10_000`). `loadCached`/`storeCached`. `test_resultcache.nim`.
- [x] **A1b** — `ledger.nim`: `RunLedger` per-process shards `<stateDir>/ledger/<pid>-<bootId>-<seq>.ndjson`, raw posix.write append, `LedgerRow` (identity/timestamp/inputHash/outcome/attempt/durationUs/rssBytes/rowVersion), `openLedger`/`append`/`closeLedger`/`scanLedger`. bootId from /proc/sys/kernel/random/boot_id (degrades to epoch). Corruption-resilient (torn-row skip, bad rowVersion skip, header-mismatch whole-shard discard). `test_ledger.nim`.
- [x] **A4a** — `Slot.testScratchDir`; `forkExecEnvScratch` creates scratch dir, injects `TMPDIR`, async-signal-safe child `chdir` (opt-in). Cleanup wired on normal/signal/exception paths. `env_probe.nim` extended (TMPDIR/CWD). `test_scratch_tmpdir.nim`.
- [x] **A4b** — safe rlimits (`RLIMIT_CORE=0`/`RLIMIT_NOFILE`/`RLIMIT_FSIZE`) folded into child window of `forkExecEnvScratch` (now the **single spec-driven spawn entry**; legacy `forkExec`/`forkExecEnv` kept for compile path). `RLIMIT_CORE`+`RLIMIT_FSIZE` importc'd (missing from Nim 2.2 posix). Fixtures `rlimit_fsize.nim`/`rlimit_nofile.nim`. `test_rlimits_safe.nim`.
- [x] **A4c** — `RLIMIT_CPU`/`RLIMIT_AS` (importc'd) in same child window; `sandbox.MinSafeRlimitAs=3 GiB` (documented ORC hazard); fixtures `rlimit_cpu.nim`/`rlimit_as.nim`; self-gated `test_rlimits_timing.nim` (quits 0 unless `CRISOL_TIMING_TESTS` set). NOTE: `./dev run` does NOT forward host env; use `./dev run env VAR=val nim r ...` to forward into container.
- [x] **A4d** — `SandboxAchieved` IPC: pre-fork status pipe; child writes `uint8` bitfield (AchEnvScrubbed/AchTmpdirIso/AchRlimitsApplied/AchNetIso) async-signal-safe before execvpe; parent decodes → `SandboxAchieved`. `forkExecEnvScratch` now returns `tuple[pid: Pid, achieved: SandboxAchieved]` (all callers updated to `(pid,_)`). getrlimit read-back gates AchRlimitsApplied. Forced netIso degradation (hlNetwork, no unshare wired) → isFullyAchieved=false. Child-dies-before-write → no hang. `test_sandbox_achieved.nim`.
- [x] **A6** — `EntrypointDecision` (edNeverBuilt/edStale/edRunFresh/edCached) + `CacheDecision` (cdmNotEligible[ord0]/cdhHit/cdmKeyMiss/cdmHermeticityDeg/cdmPolicyDisabled/cdmFlaky) in types.nim; `EntrypointResult` gains `cached`/`cacheDecision`/`achieved`. New `cachedispatch.nim` (seams: KeyOfProc/LoadProc/StoreProc bundled in `CacheSeams`; `lookupAtPlan`, `shouldStore`, `toCachedResult`, `realSeams(ptr DepGraph)`; `CachePolicy`). planner maps cd→ed at the boundary (`toEntrypointDecision`; `edecision` augments `decision`, A8 migrates reporting). `execute()` gains `spec`/`policy`/`seams` params: plan-time lookup promotes edRunFresh→edCached, synthesizes result, fires callback at plan time, marks done (admission bypass); store gate after each live result (isFullyAchieved + attempt-1 pass). **Spawn debt resolved:** live run path (spawnRunDirect + spawnRun) now routes through `forkExecEnvScratch` (single spec-driven entry); legacy `forkExecEnv` overloads RETIRED (test_env_scrub_integration migrated to scratch). `sandbox.hermeticEnvHashForSpec`. api.nim builds realSeams + resolveSandbox(hlIsolated default). Tests: `test_cachedispatch.nim` (10) + `test_cache_dispatch_boundary.nim` (6, through execute() w/ mocked seams).
- [x] **A7** — store-on-attempt-1-pass + real run-twice→cached through `crisol/api` (REAL on-disk cache). `test_cache_roundtrip.nim` (4): run-twice→cached (cdhHit), changed-closure→miss→live re-run, failing entrypoint never cached, --no-cache full bypass. Degraded-hermeticity→not-cached covered in test_cache_dispatch_boundary (hlNetwork netIso-not-wired → store skipped). NOTE: a stored result's live CacheDecision is cdmKeyMiss (fresh run that produced the now-cached value), even for edNeverBuilt/edStale (which are cdmNotEligible at plan-time lookup).
- [x] **A8** — cache reporting (additive schema evolution). `EntrypointResult.inputHash` added, stamped on hit (cachedispatch.synthesize) + live store path (runner) + miss (lookupAtPlan). planview.nim: `PlanV1Revision=2`, `decisionStringEd`/`decisionLabelEd` (edCached→"cached", cyan), `renderPlan`+`planToJson` migrated CompileDecision→EntrypointDecision, `schemaRevision` emitted, **`loadLastPlan` added** (reads `<stateDir>/lastplan.json`; tolerant absent/old/current/over-rev; NO writer yet — RFC symmetry mandate, C-stage will persist). jsonout.nim: `RunV1Revision=2`, `cacheDecisionString` (notEligible/hit/keyMiss/hermeticityDegraded/policyDisabled/flaky), per-ep `cached`(default false)/`inputHash`/`cacheDecision`, top-level `schemaRevision`, `loadLastRun` treats `schemaRevision>max`→safe cold-start+warn. render.nim: `[CACHED]` cyan tag. Cached duration = historical (cr.durationMs, confirmed). Tests: +12 test_planview, +12 test_jsonout, +3 test_cachedispatch, +2 test_cache_roundtrip. `./dev test` exit 0 (660 OK); dogfood emits schemaRevision:2 on both schemas. Reporting fully migrated to EntrypointDecision; legacy decisionString/decisionLabel kept as public API.
- [x] **A9** — cache controls. **Stage A COMPLETE.** `CacheableState = enum {csDefault, csTrue, csFalse}` on Group + PlannedEntrypoint (explicit 3-valued > Option[bool]). config.parseGroup parses `cacheable #true/#false`/absent (non-bool→cekConfig err). planner threads groupCacheable→PlannedEntrypoint (like groupMaxJobs). cachedispatch: `resolveCacheable(policy, groupState): CacheableResolution` = SINGLE source of truth for precedence; both lookupAtPlan + shouldStore delegate (zero dup). runner passes per-ep cacheable to shouldStore. `src/crisol.nim` `--no-cache` flag → RunOptions.noCache → CachePolicy(enabled:false) → execute(). **Precedence:** csFalse→never cache (overrides global both directions, →cdmPolicyDisabled); csTrue→cache iff global enabled ∧ hermetic-gate (so --no-cache beats #true, per RFC L144); csDefault→inherit global. --no-cache(invocation bypass) vs cacheable#false(permanent config decl) both →cdmPolicyDisabled, differ in scope. `--force-compile` genuinely orthogonal (lives in plan()→edStale→cmdNotEligible; --no-cache lives in lookup/store). 37 tests `test_a9_cache_controls.nim`; full suite 697 OK. csTrue≡csDefault in v1 (both need global; distinction kept for round-2 intent). fixtureHash stays "" (RFC round-2).
- [x] **B0** — `CRISOL_ATTEMPT` (1-indexed) injected into child env via `forkExecEnvScratch` extraEnv `[("CRISOL_SINK",…),("CRISOL_ATTEMPT",$attempt)]` in spawnRun + spawnRunDirect (runner ~320/373); declared `hermeticSkipVars` in sandbox.nim (~222) so survives allowlist filter as injected-after. `attempt` sourced from per-index counter (not hardcoded). Fixture `attempt_probe.nim`; `test_B0_attempt_injection.nim` (2).
- [x] **B1** — entrypoint retry. **Dispatcher restructured:** `dispatched[]`(bool,terminal) → `attempts=newSeq[int](n)` (0=undispatched, k=on attempt k) + `finalized=newSeq[bool](n)`. Fill scan re-dispatches `attempts[j]>0 ∧ ¬finalized[j] ∧ not-in-slot` (inc attempts[j]). Reap: maxAttempts=pep.retries+1; on FAIL with slotAttempt<maxAttempts ∧ not compile-fail/spawn-error → re-queue (no inc done); on pass/exhausted → finalize, inc done, fill results[i], `onResult` fires ONCE with final outcome (intermediate failed attempts NOT streamed — final-only contract). Plan-order + failFast/admission/signal-cleanup preserved. `retries int` on Group+Config (default 0), parsed in config.nim (group+global), threaded planner→PlannedEntrypoint. `EntrypointResult.{attempts:int, flaky:bool}`; `Summary.flaky:int`. `--retries N`(-1 sentinel=use config) + `--fail-on-flaky` in src/crisol.nim. `exitCode(s, failOnFlaky=false)` (types.nim ~387): +1 if failOnFlaky ∧ s.flaky>0; called from api.nim ~501 + crisol.nim. **Cache safety:** edCached finalized pre-loop (never retried); shouldStore returns cdmFlaky for attempt≠1 (flaky-pass never cached). Fixtures `flaky_once.nim`; `test_B1_retry.nim` (5 integration). `./dev test` exit 0, `./dev check` clean.
- [x] **B2** — flake-ledger append + flake-rate query. `execute()` opens `RunLedger` from `absolutePath(config.projectRoot/config.stateDir)` (guard: stateDir non-empty), closeLedger first in `finally`. `appendAttemptRow(led, ep, attemptNum, res, inputHash)` fires on EVERY live completion (intermediate retries INCLUDED — the B1-deferred observability site), BEFORE the retryEligible branch. Row: identity=identityKey(ep.path, flagHash(ep.flags)), timestamp=epochTime()*1e6 (µs), outcome=outcomeString (jsonout wire form), attempt#, durationUs=durationMs*1000, rssBytes=0 (C5), rowVersion=currentRowVersion. **edCached appends NO row** (finalized pre-loop, never reaches reap). Flake query in ledger.nim: `computeFlakeRate(rows): float` PURE (group by inputHash; bucket flaky iff has ≥1 "passed" ∧ ≥1 non-passed; rate=flakyBuckets/distinctBuckets; ""-inputHash = single bucket, surfaces flakiness when build-id unknown) + `isFlaky`/`flakeRate` I/O wrappers. Tests: `test_flake_rate.nim` (8 pure) + `test_B2_ledger.nim` (4 integ: flaky_once+retries1→2 rows isFlaky true; pass_always→1 row not flaky; fail_always+retries2→3 rows not flaky; edCached→0 rows). `./dev test` exit 0, `./dev check` clean.
- [x] **B3** — config-declared ENTRYPOINT quarantine (reporting overlay, boundary-clean). `Config.quarantine: HashSet[string]` (raw-string match vs `ep.path`, project-root-relative `/`-sep as discover.nim sets it; no normalization). Parsed in config.nim `docToConfig` Pass1 `of "quarantine"` (top-level block, `collectStrArgs`). `EntrypointResult.quarantined: bool` set in execute() BOTH paths: synth/cached (~728, post-lookup) + live-finalize (~1004). `Summary.quarantined: int`; summarize() guard `r.quarantined ∧ r.outcome.isFailure` → inc quarantined, skip the failure buckets (excluded from failed/compileFailed/timedOut/signaled/spawnErrors → exitCode stays 0); quarantined PASS = normal pass; total still counts it. render.nim `[QUARANTINED]` yellow tag (mirrors [CACHED]). jsonout: added `quarantined`(bool) + **also `flaky`/`attempts` (B1 had left them unserialized)** to run/v1; `RunV1Revision` 2→**3**. Tests: test_B3_quarantine_{config(4),summary(11),render(6),jsonout(8)} = 29. Verified: quarantined-only fail→exit0; mixed quarantined+real fail→exit1. `./dev test` exit 0, `./dev check` clean.
  - **B3 verification note (RESOLVED):** an intermittent `[FAILED]` in `test_mem_throttle.nim` is **load-flaky** (failed 2 full-suite runs, CLEAN 2 runs + 3/3 isolation) — timing-sensitive mem-probe-overlap test, NOT a slice regression. Also: **`./dev test` returns EXIT=0 even on real inner failures** — grep output for `[FAILED]`/nimble-exception, don't trust the code. Both in memory [[dev-test-verification-gotchas]]. B3 confirmed green: clean run, 741 [OK].
- [x] **B4** — per-TEST quarantine via protocol records (reading side only — boundary-clean). Pure `isQuarantined*(ep, res, q: HashSet[string]): bool` in runner.nim: returns true if `ep.path ∈ q` (B3 path rule, unconditional) OR (`res.outcome.isFailure` ∧ ≥1 rsFail record ∧ ALL rsFail record names ∈ q). Zero rsFail records (opaque / timeout / signal / exit-nonzero-no-protocol) ⇒ per-test rule N/A. Both execute() marking sites (synth ~782, live ~1061) now call the helper (replacing B3 one-liners); summarize/exit machinery unchanged (B3 already excludes quarantined failures). Flat `config.quarantine` set matched against BOTH paths (B3) and record names (B4). Fixture `protocol_two_fails.nim` (2 named rsFail records, exit 1). Tests: `test_B4_quarantine_per_test.nim` unit (13: 4 path + 9 per-test) + integration (5). Verified via grep method: both-names-quarantined→exit0; one-name-only→exit1; opaque-fail+names-in-q→NOT quarantined. `./dev check` clean. **STAGE B COMPLETE (B0–B4).**
- [x] **C0** — `crisol clean` knows the new stores. **Fixed a real latent bug:** `cleanOrphans` walking `<stateDir>/cache/` was deleting the result-cache `v1/` subtree as a non-slug orphan. Added to resultcache.nim: `resultCacheDirName*(): string` (="v"&$fmt; `cacheVersionDir` now calls it — single source of truth) + `isResultCacheRootName*(name): bool` (matches `v<digits>`: v1/v12/v999 yes; v/v1x/slug/"" no). pruneDir retain predicate now `… or isResultCacheRootName(name)` (no hardcoded "v1" in clean.nim; recognizes ANY v<N> so a future fmt-bump's old dirs aren't mistaken for compile orphans). Ledger (`<stateDir>/ledger/`) is a sibling of cache/—never walked by cleanOrphans (locked w/ tests). cleanAll removes whole stateDir → both new stores gone. NO content GC (deferred A1c). `test_c0_clean_stores.nim` (13). Verified isolation 15/15 + full-suite grep clean, `./dev check` clean.
- [x] **C1** — JUnit XML emitter. New `src/crisol/junit.nim`: `escapeXml*(s): string` (SINGLE escape path: `& < > " '`→entities; XML-illegal control chars→U+FFFD; tab/LF/CR pass) + pure `toJunitXml*(results, summary): string`. Root `<testsuites>`/`<testsuite>` per ep/`<testcase>` per record (opaque→1 synthetic case). Mapping: oFailed→`<failure>`; compile-fail/timeout/signal/spawn-error→`<error>`; rsSkip→`<skipped/>`; output→`<system-out>`. `cached="true"` on testcase (opaque) / testsuite (protocol); `time`=durationMs (already historical for cached). CLI `of "junit":` (nextVal, run-only) writes file after stdout reporting (additional sink; composes w/ --json + human). Hand-rolled escaper (justified: xmltree=DOM, xmlparser=parser). Tests `test_junit.nim`: 23-input adversarial escape table + 5 parseXml well-formedness round-trips w/ hostile content + opaque/protocol/cached cases (~56). Verified isolation + full-suite grep clean (828 OK), `./dev check` clean. Deferred: --junit for list/dry-run (no results); filterTag not threaded (machine sink).
- [x] **C2** — `--shard k/n`. New `src/crisol/shard.nim`: pure `shardOf*(eps, k, n): seq[Entrypoint]` (ep∈shard k iff `fnv1a64(ep.path) mod n == k-1`, 1-indexed; reuses depgraph.fnv1a64 — STABLE across machines, not Nim hash()) + `parseShardSpec(raw): (k,n)` (split `/`, validate n≥1 ∧ 1≤k≤n, ValueError→exit3). Properties tested: complete (∪ k=1..n == set eps), disjoint, stable (shuffled input→same membership), order-preserving (filter not reorder), edge (n>len→empty shards, n=1→all). Wired in pipeline.buildRunPlan as LAST selection step (`if shardK>0: runnable = shardOf(runnable,k,n)` after narrowing, before plan); composes w/ --changed (diff narrows first, shard partitions the narrowed set). `RunOptions.{shardK=0,shardN=1}`, CLI `of "shard":`. Tests `test_shard.nim` (30) + `test_shard_pipeline.nim` (5). Verified isolation + full-suite grep clean, `./dev check` clean.
- [x] **C3** — duration-balanced sharding. `shard.nim`: pure `balancedShardOf*(eps, k, n, durationOf: Table[string,int64])` = LPT greedy (sort by dur DESC, tie-break path ASC; assign each to least-loaded bin, ties→lowest idx; return bin k-1 filtered back to ORIGINAL input order) + `shardWithHistory(eps, k, n, stateDir)` wrapper: per-ep representative dur = MEDIAN of non-compileFailed durationUs rows (scanLedger by identityKey); NO history at all→`shardOf` hash fallback (C2 parity); partial→history-less eps get median-of-known (not 0, so they spread). Pure bin-pack separated from ledger I/O. `buildRunPlan` shard step now calls `shardWithHistory` (still inside `if shardK>0`; cold-start = C2 behavior, existing tests pass). Cross-runner consistency (shared ledger) documented; hash fallback = safe cold-start. Tests `test_c3_balanced_shard.nim` (18) + 2 added to test_shard_pipeline. Verified isolation + full-suite grep clean, `./dev check` clean.
- [x] **C4** — `--order <recent-fail|duration|none>`. New `src/crisol/order.nim`: `OrderMode = enum omNone/omRecentFail/omDuration`; pure `orderBy*(eps, mode, lastFail, medianDur: Table[string,int64])` (recent-fail: failed eps first by desc last-fail ts, never-failed after, lex tie-break; duration: desc medianDur, no-history=0=last, lex tie-break; none=identity; cold-start non-none→lexicographic path) + `orderByHistory` wrapper (lastFail=max ts of non-passed-outcome rows; medianDur=median non-compileFailed durationUs; `isFailureOutcome`/`failureOutcomes` const). Output always a permutation (set+len invariant, tested). buildRunPlan gains `order=omNone` param; runs AFTER shard, before plan; omNone=byte-parity (block skipped). shard×order: membership preserved, sequence changed. `RunOptions.order`, CLI `of "order":` (parseOrderMode, bad→exit3). Tests `test_c4_order.nim` (36) + `test_c4_pipeline.nim` (5). Verified isolation + grep clean, `./dev check` clean. **NOTE (code-review nit):** `median` duplicated in order.nim + shard.nim to avoid import cycle — extract to shared util in Stage-4 review.
- [x] **C5** — telemetry persistence (peak RSS). `Slot.peakRssBytes` tracked: reset at slot-claim, sampled `max(peak, procGroupRssBytes(pid))` pre- AND post-poll for spRunning slots (post-poll catches compile→run transitions / fast exits), read at finalize. Admission RSS feed (onSlotFinish) unregressed. `appendAttemptRow` gains `peakRssBytes` param → `LedgerRow.rssBytes` real (was 0). `EntrypointResult.peakRssBytes` + run/v1 (`RunV1Revision` 3→**4**). edCached → no row, peakRss=0 (finalized pre-loop). Per-test durationUs already in records (no new per-test rows — boundary-clean). Fixture `rss_hog.nim` (alloc 8MiB + touch pages + sleep 150ms → ≥6 samples; loose `>1MiB` assert = non-flaky). Tests `test_c5_rss_jsonout.nim`(5)+`test_c5_rss_telemetry.nim`(3). **⚠️ SHARED-SUBSYSTEM FIX (flag for Stage-4 review):** `memprobe.parsePgrp` now reads `NSpgid:` (PID-ns pgid) in addition to `Pgrp:` — std `/proc/<pid>/status` has NO `Pgrp:` field, so `procGroupRssBytes` was returning `some(0)` (RSS admission feedback was silently INERT before C5). Fix makes RSS real → live admission behavior change. **VERIFIED by control loop:** full suite 926 [OK] zero real failures; test_admission(38)/test_memprobe(18)/test_scheduler(10) green; test_mem_throttle stable 4/4 ×3 in isolation with real RSS. `./dev check` clean.
- [x] **C6** — perf-regression detection. New `src/crisol/stats.nim` (`median` MOVED here from shard/order — **dedup nit RESOLVED**; new `mad(vals, med)`; `isRegression*(currentUs, historyUs, k, sampleFloor, absFloorMs): RegressionVerdict{regressed,baselineUs,thresholdUs}`). Algo: len<sampleFloor→not flagged; med=median; madUs=max(mad, absFloorMs*1000); threshold=med+int64(k*madUs); regressed = currentUs>threshold. Config: `PerfCheckConfig{enabled,k,sampleFloor,absFloorMs}` on Config; `perf-check { sensitivity … }` block parsed (presets: none=off; conservative=(4.0,20,10); moderate=(3.0,10,5) RFC-fixed; aggressive=(2.0,5,2)); per-component overrides k/sample-floor/abs-floor-ms; `requireFloatArg` added. `--perf-check` CLI forces moderate if no config block. Detection in api.nim runTests: per fresh (non-cached, non-compileFailed) result, history=scanLedger durationUs EXCLUDING current-run rows (timestamp>=runStartUs captured pre-execute); edCached skipped. Reporting: `EntrypointResult.{regressed,perfBaselineUs,perfThresholdUs}`; run/v1 `regressions` array + per-ep `regressed`; `RunV1Revision` 4→**5**; render `[SLOW: …]` yellow tag. Tests test_c6_{stats(11 doAssert),config(15),jsonout(9),regression(5 doAssert)}. **ALL 26 MAIN SLICES DONE.** Verified: C6 own tests green, shard/order green post-dedup.

### ⚠️⚠️ FLAKY-TEST FINDING (awaiting Corey — see below) — discovered during C5/C6 verification
- **`tests/integration/test_max_jobs_overlap.nim`** "uncapped: two probes DO overlap with jobs=2" is FLAKY: **2/3 pass, 1/3 fail** in isolation. Same family as the known-flaky `test_mem_throttle` (overlap-probe timing tests).
- **Plausible CAUSAL link to C5:** the test leaves `memAware` unset (`none`=AUTO). Before C5, `procGroupRssBytes` returned `some(0)` (the NSpgid bug), so the memory-admission gate's RSS feedback was INERT → it never throttled → both probes always overlapped (timing permitting). **C5's NSpgid fix makes RSS real → the mem gate can now intermittently throttle the 2nd slot → the asserted overlap doesn't happen.** So C5 likely AGGRAVATED this test's flakiness (and the agent's `git stash` baseline check is unreliable: nothing is committed, so stashing reverts ALL of RFC-0004 incl. C5).
- **Recommended fix (my read):** concurrency-cap tests (test_max_jobs_overlap) should set `memAware: some(false)` to ISOLATE the max-jobs-cap behavior from the now-active memory gate (they test the cap, not the gate). test_mem_throttle (which DOES test the gate) keeps mem-aware on but its assertions must tolerate real RSS. Residual pure-timing flakiness (overlap within the 150ms window) is separate pre-existing infra debt. This is **Stage-4-review / test-hardening** material, NOT an RFC code defect — C5's fix is correct; the mem gate working is arguably the intended (previously-dormant) behavior.
- Recorded in memory [[dev-test-verification-gotchas]].
- ~~C6 (FINAL slice before A1c)~~ — perf-regression detection. Flag identities whose current duration > robust baseline `median + k·MAD` (MAD over historical ledger durations). Config: ONE `perf-check { sensitivity "none|conservative|moderate|aggressive" }` block → preset (k, sample-floor, abs-floor-ms); power-user overrides `k`/`sample-floor`/`abs-floor-ms`. moderate=(k=3.0, sample-floor=10, abs-floor-ms=5). Rules: SUPPRESS below sample-floor; MAD-zero → `MAD ← max(MAD, absFloorMs)`. edCached EXCLUDED (no fresh measurement). Report: `regressions` array in run/v1 + render. `--perf-check` CLI. RFC §F6 line 164-174, slice line 241.
- [x] **FLAKY-FIX APPLIED (Corey: "fix tests now, keep gate"):** `test_max_jobs_overlap.nim` Config now sets `memAware: some(false)` to isolate the max-jobs-cap test from the C5-activated memory gate (with comment). Verified 5/5 green (was ~1/3 fail). C5's NSpgid fix KEPT. test_mem_throttle left on (exercises the gate). test_composition_s7 relies on the gate serialising (asserts non-overlap) — unaffected/supported. test_headofline has no overlap assertion.
- [x] **A1c** — cache+ledger GC (final slice). `resultcache.gcResultCache*(stateDir, maxEntries, maxAgeSecs, nowSecs): GcResultCacheReport` — LRU by cachedAt (age pass then size pass; malformed file→cachedAt 0→evicted-first, never crash; clock injected; soft-cap KEPT as write-time backstop, complementary). `ledger.compactLedger*(stateDir, maxAgeSecs, nowSecs): CompactLedgerReport` — merges all shards → one `compact-<pid>-<bootId>-<seq>.ndjson` (reuses rowToJsonLine/parseRow; write-then-remove crash-safe; age filter µs; torn-shard tolerant; scanLedger round-trips). cleanOrphans calls both (steps 5+6) UNDER the existing acquireLock(F_SETLK) exclusive lock (run/clean mutually exclusive → no evict-during-write race). Return tuple extended (+cacheEvicted, shardsRemoved, ledgerRowsKept); crisol.nim clean summary line extended. Config +`maxCacheEntries`(0=default 10k), `cacheMaxAgeDays`(0=off), `ledgerMaxAgeDays`(0=off), parsed from KDL. Removed pre-existing unused `times` import (./dev check now ZERO warnings). Tests `test_a1c_gc.nim` (13: 6 gc + 5 compact + 2 integration); test_c0_clean_stores updated (ledger-survive tests now assert dirExists + shardsRemoved≥1, since compaction removes the original shard by design — 14 pass).

## 🏁 ALL 27 SLICES IMPLEMENTED (A2-pre…C6 + A1c). Stage 3 COMPLETE.
- **Next: Stage 4 — `/code-review docs/rfc/0004-incremental-hermetic-execution.md`** (PhD-CS bar; 2-3 rounds; ship when a round finds 0 critical/high). Review ledger goes in this handoff.
- **Carry into Stage-4 review (known items to validate/clean):**
  1. **C5 `memprobe` NSpgid fix + admission behavior change** — validate the parsePgrp NSpgid logic (parts[1] = innermost ns pgid in our single-level container; could mismatch in nested ns), and that activating the mem gate by default is intended.
  2. **Overlap-timing test flakiness** — test_mem_throttle still load-flaky (Corey chose to leave it exercising the gate); consider hardening (serialize/self-gate). [[dev-test-verification-gotchas]].
  3. **median dedup** — RESOLVED in C6 (extracted to stats.nim); verify no stragglers.
  4. General PhD-CS pass over the new modules (cachedispatch, sandbox, keys, resultcache, ledger, shard, order, stats, junit, clean).

## Review ledger (Stage 4 — Round 1)
5 reviewers (correctness/quality/security/design/systems) + 4 adversarial verifiers. Scope = the RFC-0004 modules. Status: `open`/`fixed`/`deferred`/`wontfix`/`refuted`/`escalated`.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| H1 | High | Soundness key used `hermeticEnvHashForSpec` (allowlist **names only**) — unsound (PATH/HOME value change → stale cached pass). RESOLVED not as a fork: hash names+values (RFC-faithful), exclude only runner-injected vars (TMPDIR/CRISOL_*). | **fixed** | realSeams now `hermeticEnvHash(filterEnv(parentEnv,spec,@[]))`; names-only proc deleted; tests test_soundness_env_values.nim + test_cachedispatch.nim. 953 [OK]. |
| H2 | High | `nimVersion=""` everywhere → depgraph staleness + soundness-key blind to a Nim-compiler upgrade. | **fixed** | `const crisolNimVersion* = NimVersion` threaded into buildRunPlan/loadDepGraph/plan/execute/realSeams (api.nim). Verified 953 [OK] merged with H1. |
| H3 | High | `resultcache.storeCached` single `posix.write` (no partial-write/EINTR loop) → silent cache-population failure. | **fixed** | new `ioutils.writeAllFd` (dep-free) used by resultcache + ledger; test_ioutils.nim. 953 [OK]. |
| M1 | Med | shard keyed by `ep.path` alone → partition breaks for same-path/different-flags eps. | **fixed** | shard now keys by `$identityKey(path,flagHash(flags))` (shardOf/balancedShardOf/shardWithHistory); disjoint+complete tests. 963 [OK]. |
| M2 | Med | `stats.median` returns **upper** middle for even N; docstring says "lower middle". | **fixed** | true median = overflow-safe mean of two middles; docstring corrected; stats tests updated. 1002 [OK]. |
| M3 | Med | `PlannedEntrypoint` carries both `decision` (old) + `edecision` (new) — can disagree. | **fixed** | `decision` field removed; derived `compileView(pep)` accessor; all readers→edecision. 1002 [OK]. |
| M4 | Med | `execute()` cache triple unbundled; mis-wire silently disables caching. | **fixed** | `CacheContext` w/ `cacheDisabled`/`cacheEnabled` (asserts invariant) + `isActive`; execute() takes `cache:`. 970 [OK]. |
| M5 | Med | `LedgerRow.outcome` wire strings compared via literals in 3 modules, no single source. | **fixed** | new `outcomestrings.nim` single source; ledger/order/shard/jsonout use it; test_outcomestrings.nim. 963 [OK]. |
| M6 | Med | runner exception-path `finally` SIGKILLs immediately (no SIGTERM grace). | **fixed** | shared `teardownLiveSlots` (SIGTERM→drain→SIGKILL) on both interrupt + exception paths; test_m6_teardown.nim. 970 [OK]. |
| M7 | Med | `ledger.readShard`/`readAllShardRows` ~40 lines duplicated header-parse. | **fixed** | extracted `parseShardLines`; both callers use it (with M3 unit). 953 [OK]. |
| M8 | Med | CacheDecision conflation (cdmKeyMiss miss-stored/unstored; cdmPolicyDisabled --no-cache/cacheable#false; resolveCacheable cdhHit pre-lookup). | **fixed** | enum → 8 honest variants (+cdmStored,+cdmGroupOptOut); pre-lookup→cdmNotEligible; RunV1Revision 5→6; wire+render updated. 1002 [OK]. |
| M9 | Med | `ccprobe.realRun` shell-joins args into `execCmdEx` (injection-fragile) + needless copy. | **fixed** | `startProcess(cmd, args, {poUsePath})` argv-exec, no shell (poEvalCommand omitted); copy dropped. 1002 [OK]. |
| L-* | Low | batch: O_CLOEXEC defensive hardening on status pipe (H1-pipe **refuted** as High); memprobe `parts[1]`→`parts[^1]` (nested-ns; correct today, comment backwards) — **C5 carry-in #1**; order dead `omNone` branch; bare `except:` swallows Defect (ccprobe/ledger/memprobe/planner); keys.nim env docstring (part of H1); realSeams argv=binName not full path; `isInFlight` closure per-iter; `countCacheEntries` O(n)/store; compactLedger shardSeq side-effect + redundant createDir; cache `.tmp` never GC'd; junit escapeXml overlong/FFFE-FFFF UTF-8 passthrough (validity not breakout); resultcache tmp TOCTOU; ledger shard-name collision under bootId fallback; CacheableState double-negation comment; `resolveSandbox` 7 positional `Option[int64]` rlimits (transposition risk); `--hermetic` not wired into RunOptions facade. | open | cosmetic/narrow-trust/defensive — batch or defer per mandate. |
| —  | — | **REFUTED:** Security H1 status-pipe grandchild-hang — child `close(pipeWrite)` at spawn.nim:445 *before* execvpe:447; parent closes its copy before blocking read. Path structurally blocked. | refuted | downgraded to L (O_CLOEXEC defensive only). |

**Escalation (RESOLVED):** H1 env-hash was NOT a fork — fixed as a clear-best soundness fix (see [[no-fake-forks-soundness]]).

### Review ledger — Round 2 (re-review of the 9 fixes)
4 reviewers (correctness/security/design/systems) over the changed scope. All 9 R1 fixes re-confirmed correct/clean. NEW findings:
| id | sev | finding | status | proof/reason |
|----|-----|---------|--------|--------------|
| R2-1 | Med | `runner.nim:824` edRunFresh under `--no-cache` → `cdmNotEligible` (both if/else branches same); should be `cdmPolicyDisabled`. | fixing (Unit R2-A) | correctness R2. Defeats M8 distinction. |
| R2-2 | Med | `runner.nim:260-274` teardownLiveSlots Phase 2 WNOHANG-reaps but doesn't mark reaped → Phase 3 SIGKILL+reapBlocking on reaped pid (spurious signal to reused pgid, narrow interrupt/exception window). | fixing (Unit R2-A) | systems R2. Fix: track reaped slots, skip Phase 3 for them. |
| R2-3 | Med | `outcomestrings.nim` is a 2nd source of truth alongside `jsonout.outcomeString`; M5 didn't unify. New Outcome ⇒ two edits. | fixing (Unit R2-B) | design R2. Derive failure set from the single Outcome↔wire mapping. |
| R2-a | Low | `jsonout.persistLastRun:297` bare `posix.write` — H3 durability class, 3rd site missed. | folding in (R2-A) | security R2. Use writeAllFd. |
| R2-b | Low | `ccprobe.nim:73` no `defer p.close()` on raise (M9 residue). | folding in (R2-B) | correctness R2. |
| R2-c | Low | `planview.nim` dead `CompileDecision` `decisionLabel/decisionString` family (M3 residue). | folding in (R2-B) | design R2. |
| R2-d | Low | `cdhHit` breaks `cdm*` naming. | folding in (R2-B) | design R2 (cosmetic). |
| —   | refuted | `shard.nim:91` cross-version partition instability — intended M1 behavior change; disjoint+complete hold per-version; no consumers. | refuted | correctness R2 over-reach. |

### Review ledger — Round 3 (re-review of Round-2 fixes) → FLOOR HIT
4 reviewers over the R2 diff (runner/jsonout/types/outcomestrings/planview/ccprobe). Security CLEAN, Systems CLEAN, Correctness CLEAN (all R2 fixes confirmed correct). Design:
| id | sev | finding | status | reason |
|----|-----|---------|--------|--------|
| R3-1 | Med | `teardownLiveSlots.reapedInGrace` is a parallel `seq[bool]` vs tracking reaped on `Slot`. | **accepted (wontfix)** | Control-loop judgment: reaped-during-this-teardown is genuinely local to one call; a fresh local seq is the correct scope (enables clean idempotency, no stale-state). Putting it on long-lived `Slot` (meaningless 99% of the time, overloads the compile/run phase enum) is worse. Code confirmed CORRECT by all other lenses. Not a real issue per the review bar. |
| R3-2 | Low | extract `inactiveDecision(edecision): CacheDecision` to cachedispatch (the !isActive mapping is inline in execute). | open (leave) | navigability. |
| R3-3 | Low | `jsonout` imports `outcomestrings` only to re-export `FailureOutcomeStrings`; comment the import. | open (leave) | navigability. |

**Deferred design opportunity (future `/architect`, NOT this review):** replace the pervasive `pepIdx == -1` idle-sentinel with an explicit `SlotState` enum {idle,compiling,running}; teardown would then read slot state directly with no parallel array. Pre-existing; core-dispatch-wide; out of review scope.

### Remaining Lows (leave per mandate — fix-through-Medium)
R1 batch (L-* row above): O_CLOEXEC status-pipe hardening; memprobe `parts[1]`→`parts[^1]` nested-ns (C5 carry-in #1, correct today); bare `except:`→`CatchableError` at several sites; `realSeams` argv=binName not full path; `isInFlight` closure per-iter; `countCacheEntries` O(n)/store; compactLedger shardSeq side-effect; cache `.tmp` never GC'd; junit overlong-UTF-8 passthrough; resultcache tmp TOCTOU; ledger shard-name bootId-fallback collision; CacheableState double-negation comment; `resolveSandbox` 7 positional rlimit Options; `--hermetic` not in RunOptions facade. Plus R3-2, R3-3.

## ⭐ A6 detailed requirements (the lynchpin — read before resuming)
A6 must do BOTH the cache-dispatch feature AND resolve the accumulated spawn debt:
1. **Thread `SandboxSpec` into the LIVE run path.** Today `execute()`/the runner still spawn real test runs via the LEGACY `forkExec`/`forkExecEnv` — NOT the spec-driven `forkExecEnvScratch`. A6 must: resolve a `SandboxSpec` (default `hlIsolated`) once (from config+flags; config-KDL parse of `hermetic` level can be minimal/defaulted now, full CLI/config wiring is A9), and route real test-run spawns through `forkExecEnvScratch`. **Retire/delete any spawn variant left dead** (don't leave 3 forkExec* variants — keep legacy `forkExec` ONLY if the compile path still needs it; otherwise consolidate). The `TODO(A4d/A6)` marker in spawn.nim flags the spot.
2. **`EntrypointDecision` enum** (edNeverBuilt/edStale/edRunFresh/edCached) — single sealed sum (RFC F3). Map existing CompileDecision: edNeverBuilt←cdNeverBuilt, edStale←cdStale, edRunFresh|edCached←cdSkipFresh. The execute loop branches on this one field.
3. **Plan-time cache lookup:** for each runnable `edRunFresh` entrypoint, derive its `soundnessKey` (via keys.nim — needs closureContentHash from DepGraphEntry.closureHash, flagHash, nimVersion, ccVersion from ccprobe, fixtureHash[empty sentinel ok for now], argv, rlimitConfig, hermeticEnvHash, protocolMajor). On a cache hit (`resultcache.loadCached`) AND policy permits → set `edCached`, synthesize EntrypointResult from CachedResult, fire its ResultCallback at PLAN TIME before live results (deterministic ordering). `--changed` unaffected (changed closure ⇒ different key ⇒ miss).
4. **Gate + store:** only store on (a) `isFullyAchieved` AND (b) attempt-1 pass. `edCached` entrypoints BYPASS the admission controller entirely (spawn nothing, no liveCount slot).
5. **`CacheDecision` enum** on EntrypointResult, always populated (cdhHit/cdmKeyMiss/cdmHermeticityDeg/cdmPolicyDisabled/cdmFlaky/cdmNotEligible). (Reporting of it = A8; population = here.)
6. **Boundary-test via `crisol/api`** with store + key proc MOCKED (inject seams) — run-twice→cached is the A7 integration test; A6 is the wiring + dispatch, mock-tested.
NOTE: A7 (store-on-attempt-1-pass; real run-twice→cached) is the natural completion of A6 — they're tightly coupled; consider doing A6+A7 together in the fresh-context session.

### ⚠️ ARCHITECTURAL DEBT TO RESOLVE (A4d/A6): `forkExecEnvScratch` is the single spec-driven spawn entry but is NOT yet threaded into the live runner dispatch (`execute()` still uses the legacy spawn for real runs). A4d (SandboxAchieved IPC) + A6 (cache dispatch) must thread the resolved SandboxSpec from config→runner→this entry, and ensure the live path actually applies hermeticity. The TODO(A4d/A6) marker in spawn.nim flags the spot. Do NOT leave dead spawn variants — A6 should retire any unused ones.
### Both F1 stores complete (A1a+A1b). Safe /compact point.
- **Forks resolved:** default hermeticity=`isolated`, caching=on, cache-key=input-hash. Principle: no consumers yet → default to strongest *sound* config.

## Round 2 fixes applied (on top of round 1)
- **Ledger storage REPLACED:** round-1's `O_APPEND` single-file is UNSOUND on crisol's platform (overlayfs/WSL2 regular-file appends not atomic cross-process — depth+breadth both caught). New: **per-process shard files** `<stateDir>/ledger/<pid>-<bootId>.ndjson` merged on read, compacted by GC. Dependency-free; SQLite considered+rejected (toolchain dep, over-engineered). *Corey may override to SQLite if he wants indexed scans — I recommended shards.*
- **Single `EntrypointDecision` enum** (edNeverBuilt/edStale/edRunFresh/edCached) replaces round-1's two orthogonal enums (which allowed illegal `(cdNeverBuilt,edCached)`).
- **Key types in types.nim** (IdentityKey/SoundnessKey distinct string); derivation in `keys.nim`; ledger stores key as opaque string; wire name = `inputHash`.
- **SandboxAchieved concrete record + isFullyAchieved predicate**; populated via **child→parent pipe IPC** = new slice **A4d** (child can't write parent heap post-fork). getrlimit readback; cgroup-below-rlimit documented as undetectable residual.
- **CacheDecision enum on EntrypointResult** = structural cache observability (replaces a bolted-on --explain flag).
- **New/changed slices:** added **A2-pre** (cc/libc probe, effectful), **A4d** (SandboxAchieved IPC); A6 depends on A4d; B1 restructures the execute() dispatcher (1:1 plan-index → re-dispatch); C1 uses table-driven test NOT proptest (no dep); A4c self-gated skip; fixture inventory adds RLIMIT_AS safe-min + network-degradation fixture.
- **Ergonomics:** `--no-flaky-pass`→`--fail-on-flaky`; perf-check → one `sensitivity` policy block; interim cache soft-cap (maxCacheEntries=10k) since GC deferred; CI-deployment subsection (persist `<stateDir>/`, version-skew safe); cold-start fallback determinism (order×shard); CRISOL_* pinned after allowlist; nondeterminism-honesty + golden-mutation `cacheable #false` contract.
- **Kept schemaRevision int** (rejected design's `v1.<n>` string — int is more consumer-friendly).
- **Codebase nit for /tdd:** depgraph.nim:73 docstring still says "XOR" though code is chained-FNV — fix when A2 touches it.

## Round 1 fixes applied (summary)
- **Keys split:** identity key `(path,flagHash)` for ledger vs soundness key (cache); schema-version OUT of key (file header). Chained FNV not XOR (self-cancel bug). Added ccVersion/libc, testDataHash (fixtures), argvHash, rlimitHash to soundness key; TMPDIR value excluded from envHash.
- **rlimits = config constants only** (not admission estimate; RLIMIT_AS default unset — 512MiB SIGSEGVs ORC).
- **F1 split:** ExecutionCache (per-key files) + RunLedger (O_APPEND lines). Multi-invocation concurrency model + checksum + .tmp cleanup + lock-guarded GC.
- **ExecutionDecision enum** (edRun/edCached) — `cdCached` removed from CompileDecision; admission bypass for edCached.
- **Achieved-vs-requested hermeticity:** degraded run ⇒ no cache write. Never cache attempt>1 (flaky-pass freeze). Synthesized records: cachedAt + plan-time callback ordering.
- **F2:** `resource`→`isolated`; published default env allowlist; chdir default off.
- **Ergonomics:** `--no-cache`=no read+no write; `--retries-strict`→`--no-flaky-pass`; drop `--net none` (use `--hermetic`); `cacheable` tri-state; schemaRevision integer (resolves schema punt).
- **JUnit** escape proc + property test; sharding last-in-selection; perf-regression k=3/≥10-sample floor/MAD-zero floor; cdCached excluded from telemetry.
- **Slices resequenced/split:** A3→A5→A2→A1a→A1b→A4a→A4b→A4c→A6→A7→A8→A9; added B0 (CRISOL_ATTEMPT), C0 (clean ext), deferred A1c (GC); fixture inventory added.
- **Fork 3 RESOLVED:** input-hash (reuse closureHash + ccVersion), not binary-bytes.
- **Scope:** candidates #1 (caching), #2 (hermeticity), #3-clean (retry/quarantine), #5 (JUnit/shard/prioritize), #6 (telemetry/perf-regression). **#4 (per-test coverage selection) EXCLUDED** — layer-2-crossing, see [[boundary-granularity-discriminator]]. One staged RFC-0004 (Corey picked "one staged RFC" over split).

## Spine / key design decisions (this session)
- **The insight:** crisol already computes `closureHash` (FNV over imported-source *contents*) and spends it only on compile-avoidance. RFC-0004 spends it on execution → incremental engine (nextest/Bazel/go-test class), staying a layer-3 binary-opaque runner.
- **Soundness invariant:** F3 caching is GATED on F2 hermeticity ≥ `resource` — the env allowlist defines the bounded input set the fingerprint hashes. No hermeticity → no sound cache key.
- **Substrate-first:** F1 results-history store (content-addressed cache + append-only ledger) is the foundation; F3/F4/F5/F6 are tenants.
- **Everything entrypoint-granularity** (boundary-clean). Retry = whole-binary; quarantine = reporting-layer.

## Features
- F1 store · F2 hermetic exec (rlimits/tmpdir/env-allowlist; net-ns opt-in) · F3 result caching (pass-only v1, gated on F2) · F4 retry+flake-ledger+quarantine · F5 JUnit+shard+prioritize · F6 telemetry+perf-regression.

## Slices (19, staged A/B/C) — see RFC §Stages & slices
- Stage A (engine core, deepest scrutiny): A1–A9
- Stage B (reliability): B1–B4
- Stage C (CI + observability): C1–C6

## Open forks
- **NONE blocking.** One soft item for Corey's awareness (already resolved with a recommendation, not blocking): ledger storage = per-process shard files (my pick) vs SQLite (if you want indexed range-scans + a libsqlite3 toolchain dep). Going with shards unless you say otherwise.

## Non-goals (locked)
#4 per-test coverage selection + any sub-binary control; chroot/bind/tmpfs root isolation; non-Linux hermeticity (degrades); benchmarking; distributed execution; fail-caching in v1.

## Context
- RFC-0003 COMPLETE: committed `f7695cc`, pushed origin/main. amoxtli milpa.kdl crisol pin bump (`71e8719`→`f7695cc`) still pending (cross-repo, not now).
- CLAUDE.md Compact Instructions block present. Memory: [[boundary-granularity-discriminator]] governs feature scope; [[git-hooks-worktree-isolation]] (no worktree isolation; verify no Co-Authored-By on commits); [[toolchain-milpa-podman]] (./dev wrapper, no host nim).
