# RFC-0009 — path identity — handoff

- **RFC:** `docs/rfc/0009-path-identity.md` (DRAFT).
- **Resume:** **[2026-09-12 A3d-ii IN VERIFICATION] Quarantine fold (live soundness bug fixed). Implemented the dual-view design (control-loop call, RFC premise refined): B3 path rule FOLDS via TrackedPath, B4 test-name rule stays EXACT string (names aren't paths). types.Config gains quarantineTp: HashSet[TrackedPath] (B3) beside quarantine: HashSet[string] (B4). config.docToConfig builds quarantineTp AFTER trackedRoots (classify each user entry, keep pcTracked — user-origin reduction, never fromCanonical). runner.isQuarantined(ep,res,qNames,qPaths): B3 `ep.tp in qPaths`, B4 `rec.name in qNames`; 3 callers pass config.quarantine+config.quarantineTp. Load-bearing test (test_B4): quarantine "Foo.nim" downgrades failing "foo.nim" under forced fpAsciiLower (a raw match would miss it) + negative control. test_B4 fully retyped (qroots, ep.tp, qPathsOf mirrors production dual-view). Other B3 tests (config/summary/jsonout/render/summary_counts) unaffected & green; windows clean (types/config/runner). AWAITING full ./dev test (bi21j19s2) → commit A3d-ii → push → gate CI → A3d-iii. Slices DONE & CI-green (10): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i,A3c-ii,A3d-i. A3d-ii pending-commit. Remaining Stage A: A3d-iii,A3d-iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b.** **[2026-09-12 A3d-i IN VERIFICATION] Read-only result surfaces → fold-aware. Implemented in control loop (not delegated) after a discrepancy check: the RFC named jsonout+order+resultcache, but ONLY jsonout has a persisted-vs-live path comparison. VERIFIED (control loop): order.nim's lastFail/medianDur tables are built AND read with the SAME live ep.path (ledger join is via identityKey hash → values not keys); resultcache is purely SoundnessKey(content-hash)-keyed — NEITHER has a fold-sensitive path comparison, so per no-dormant-substrate they are UNCHANGED (documented finding, not a deferral; the only order/ledger fold risk lives in identityKey/keys.nim, out of A3d-i scope). REAL work (jsonout→pipeline): loadLastRun's failed set retyped HashSet[tuple[tp: TrackedPath, group]] (each persisted path reduced via fromCanonical(config.trackedRoots)); pipeline.nim:145 membership now (tp: it.tp, group) — folds via TrackedPath ==; api.nim failedKeys type flows through. Load-bearing test ADDED (test_jsonout): persisted Foo.nim matches live foo.nim under forced fpAsciiLower (§3 probe seam, volume-independent) — GREEN; a raw-string test would miss it. Test blast radius fixed: test_jsonout (makeCfg gets real trackedRoots + fk helper + fold test), test_shard_pipeline (makeConfig trackedRoots + failedKeys tp-keyed), test_changed (roots-before-eps, ep.tp set, failedKeys tp-keyed). Local verify GREEN: test_jsonout/shard_pipeline/changed/run_tests; windows check clean (jsonout/pipeline/api). A3d-i COMPLETE & CI-GREEN all 5 legs (run 34680209510, commit c8b9469). NEXT: A3d-ii (quarantine fold — see the design finding below). Slices DONE & CI-green (10): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i,A3c-ii,A3d-i. Remaining Stage A: A3d-ii,A3d-iii,A3d-iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b. **[2026-09-12 A3c-ii + timing-fix pushed, CI re-gating] A3c-ii = TWO commits: e1d5a42 (closure retype + closureHashInputs consistency fix) + e11b166 (test_headofline trackedRoots fix). The timing leg had failed on run 34676051660 (test_headofline elapsedMs ~2300ms vs 380ms) — NOT flake: local A/B proved it (baseline bad98af PASSES, e1d5a42 FAILS 3/3 on quiet host). Root cause: A3c-ii made config.trackedRoots load-bearing for the closure path (recordClosure/decideCompile classify+resolve each member); test_headofline's makeConfig left trackedRoots EMPTY → members classify pcOutside → dropped → warm entries look stale → all 4 recompile → timing blown. FIX: makeConfig sets initTrackedRoots(getCurrentDir(),@[],""). LESSON: any manually-built test Config needs REAL trackedRoots post-A3c-ii; tests/timing/ is EXCLUDED from ./dev test (only ./dev timing + CI timing leg cover it), which is why the agent's in-scope fixes missed it. Full ./dev timing GREEN (OK: 31 files) + ./dev test GREEN (206+21). A3c-ii COMPLETE & CI-GREEN all 5 legs (run 34677726157, commits e1d5a42 + e11b166). NEXT: A3d-i. Slices DONE & CI-green (9): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i,A3c-ii. A3c-ii DONE (CI-green 34677726157). Remaining Stage A: A3d-i/ii/iii/iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b. **[2026-09-12 A3c-ii COMMITTED e1d5a42, CI 4/5 GREEN — timing flake re-running] A3c-ii (closure retype to TrackedPath + closureHashInputs consistency fix) committed & pushed. CI run 34676051660: windows/macos/cgroup/test GREEN; `timing` leg RED on test_headofline.nim:172 (elapsedMs<380ms threshold) — the DOCUMENTED flake (380ms vs ~300ms nominal under CI load, [[dev-test-verification-gotchas]]), architecturally orthogonal to a closure-type change. Re-ran the failed leg via `gh run rerun 34676051660 --failed` (in progress). If it goes green → A3c-ii DONE, mark checkbox CI-green, proceed to A3d-i. If it fails AGAIN → local A/B (git stash vs main) on test_headofline to rule out an A3c-ii perf regression before proceeding. Slices DONE & CI-green (9): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i,A3c-ii. A3c-ii checkbox ticked, CI-pending on the timing re-run. NEXT (gated on A3c-ii timing-green): A3d-i. Remaining Stage A: A3d-i/ii/iii/iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b. **[2026-09-12 A3c-ii FIX-IN-VERIFICATION] Closure retype implemented + one real regression fixed. TWO issues surfaced during verification: (1) FALSE-RED — the slice agent (ae04380e781d8d6b3) STALLED (subagent-stall pattern) leaving detached `./dev test` runs that collided with the control-loop's own full-suite on the shared container nimcache (`Text file busy`/ETXTBSY across ~20 unrelated files that pass in isolation). Resolved: TaskStop'd the agent + killed leftover build trees + podman container; env now quiescent (banked to [[dev-test-verification-gotchas]]). (2) REAL REGRESSION — test_skipfresh went edStale on warm run: my D5 pin was WRONG. `closureContentHash` hashes the PATH STRING (not just content), and `recordClosure` hashed raw `inputs.files` while `planner.decideCompile` hashed the classify-FILTERED TrackedPath reconstruction (`if isProject: display else: toNative`) — divergent whenever a member is dropped as pcOutside. FIX: new shared `depgraph.closureHashInputs*(closure, roots)` helper (project→display, dep→toNative per RFC R3-17c); BOTH recordClosure and decideCompile now hash over it from the SAME filtered set. Verified GREEN after fix: test_skipfresh (run2 cdSkipFresh/compileSkipped=true), test_freshness, test_nimcache_persistence_real, test_changed, test_soundness_m10, mode-1 A3b-ii; windows check clean (depgraph/planner/narrow/api). Definitive clean full suite RUNNING (bqju1rvdx) with NO concurrency. NEXT: suite green → commit A3c-ii (closure retype + closureHashInputs fix) direct to main → push → gate CI all 5 legs → A3d-i. Slices DONE & CI-green (9): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i,A3c-ii. A3c-ii pending-commit. Remaining Stage A: A3d-i/ii/iii/iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b. A3c-ii files: depgraph.nim(closure field+toJson+fromJson+isEntryStale+recordClosure+closureHashInputs), narrow.nim(reduceClosure deleted), planner.nim(decideCompile), api.nim(closureReport), + ~11 test files + rfc9_narrow_support.nim(closureTp).** **[2026-09-12 A3c-ii IN VERIFICATION] Atomic retype `DepGraphEntry.closure` HashSet[string]→HashSet[TrackedPath] implemented by sonnet agent ae04380e781d8d6b3. Design pins (all held): D1 field+import; D2 toJson sorts by cmpKeyBytes + serializes `display` (on-disk format unchanged for tag-0); D3 classify-at-load converts+drops pcOutside — SUBSUMES the M10 closure filter (externals M10 filter kept; test_soundness_m10 GREEN as the guard); D4 isEntryStale gains roots, uses toNative (sole caller narrow.nim:115 threaded); D5 recordClosure converts inputs.files at the depgraph boundary, content-hash input UNCHANGED; D6 loadDepGraph/loadStoredDepGraph reuse config.trackedRoots — NO new param (supersedes RFC wording); D7 narrow reduceClosure DELETED, direct disjoint compare; D8 api closureReport sorts cmpKeyBytes→display. BONUS site the compiler surfaced (not in brief): planner.nim decideCompile — SECOND closure-existence+content-hash site; agent retyped existence via toNative and reconstructed hash input as `if isProject: display else: toNative` (soundness-sensitive; guarded by freshness/persistence tests). Blast radius: 4 prod modules (depgraph/narrow/planner/api) + ~11 test files + tests/support/rfc9_narrow_support.nim (closureTp helper). Control-loop verification DONE: test_soundness_m10/freshness/narrow/depgraph/depgraph_guard/issue16_unit/soundness_r4/fallback all GREEN; mode-1 A3b-ii selection E2E GREEN; windows check clean (depgraph/narrow/planner/api); all four prod diffs faithful to pins; fold-probe seam intact; fixed a stale module-doc line in the A3b-ii test. AWAITING full `./dev test` (bwnpa7bae) for the integration tests, THEN commit A3c-ii → push → gate CI all 5 legs → A3d-i. Slices DONE (8): A0-spike,A1,A0,A2,A3a-i,A3b-i,A3b-ii,A3c-i. A3c-ii pending-commit. Remaining Stage A: A3d-i/ii/iii/iv,A4a,A4b,A5a,A5b-i,A5b-ii,A5c,A-degraded,A-final-i,A-final-ii. Stage B: B-inventory,B1,B2,B3a,B3b,B3c,B4a,B4b. **[2026-09-12 CURRENT] A3c-i landing in TWO fix commits after a RED CI. (1) 7a97a21 header root descriptor + validation [format 5→6]; (2) 90379e0 completed the format-bump discipline (test pin→6, v5-discard test, CHANGELOG format-6). CI run 34668500591 then went RED on the `test` leg ONLY (macos/cgroup/windows/timing green): the forced-fpAsciiLower A3b-ii E2E discarded its own persisted graph with dgdFoldMismatch — RUN 1 (real `runTests` facade) persisted under the REAL probe (fpNone on ext4) while RUN 2 forced fpAsciiLower; A3c-i's new header fold-validation correctly caught the inconsistency. FIX (uncommitted, tree quiescent): made the RFC-0009 §3 injectable fold-probe reachable through the real facade — new `paths.FoldProbe` alias; `config.loadConfig`/`conventionConfig`/`parseConfigFile`/`docToConfig` gained `probe: FoldProbe = probeFoldPolicy` threaded to both `initTrackedRoots` sites; `api.RunOptions.foldProbe: FoldProbe = nil` threaded via `planTests` → loadConfig (nil→real probe). Test now injects `foldProbe: forcedProbe` into RUN 1 and rebuilds RUN 2's cfg via `loadConfig(probe=forcedProbe)`, so mode 1 forces the policy across persist AND load; modes 2/3 unchanged (nil→real). Verified locally: mode 1 GREEN (exact CI cmd), mode 3 SKIP, test_paths/test_config/windows-check GREEN. A3c-i COMPLETE & CI-GREEN all 5 legs (run 34671186734, commits 7a97a21 + 90379e0 + bae34d9). NEXT: A3c-ii.** **BUILD IN PROGRESS (`/tdd` under `/loop`).** Stage A ladder, slice-by-slice, one commit per slice direct to main, each gated on CI-green all 5 legs (test/timing/cgroup ubuntu + windows + macos) before stacking the next — the dev container is case-sensitive ext4 so macOS/Windows fold+path behavior is CI-only (MEMORY [[rfc0009-macos-test-gotchas]]). **Slices DONE & CI-green (7): A0-spike, A1, A0, A2, A3a-i, A3b-i, A3b-ii.** **A3c-i COMMITTED (7a97a21) but its CI (run 34666689129) came back RED on 3 legs (test/cgroup/macos) — root cause: the format-bump discipline was incomplete.** The A3c-i commit bumped DepGraphFormatVersion 5→6 and added the depgraph.nim History entry, but LEFT test_depgraph.nim's `assert DepGraphFormatVersion == 5` pin AND omitted the CHANGELOG format-6 section (the pin's docstring requires both on any bump). windows+timing were green (windows runs per-file conformance; timing runs only tests/timing — neither hits the tests/unit pin). **FIX IN PROGRESS (uncommitted working tree):** test_depgraph.nim pin → 6 with updated rationale + a new v5-discard assertion backing the CHANGELOG migration promise; CHANGELOG.md gains the `BREAKING CHANGE — dependency graph format 6` section. Touched tests pass locally (test_depgraph, test_depgraph_guard, test_issue16_unit) + windows cross-check clean; full `./dev test` re-running (task bb1371qe8) against the quiescent fixed tree before re-push. Resume: once full suite green → commit the fix direct to main → re-gate on CI all 5 legs → THEN implement **A3c-ii**. LESSON: run the verification full-suite only against a quiescent tree (an intermediate-tree run gave a false exit-0 earlier) and always read the run-tests.sh log tail, never trust a bare exit code. Per CLAUDE.md compact rule: re-read this handoff + MEMORY.md before continuing; the body `- [ ]`/`- [x]` checkboxes are the working slice ledger. Round-3 review ledger (historical): `<scratchpad>/rfc0009-round3-ledger.md` (R3-1..R3-29). No open forks awaiting Corey. Bookkeeping debt (non-blocking): stale TOML fence — see below.
- **(superseded) Round-2 Resume:** Round-2 /architect review complete (5 fable lenses, 2026-09-10). Round 1 + round-2 rewrite already applied; round 2 found MORE (deeper): 5 lenses converged on (a) §4's core premise was false — closure members are manifest/import-origin, not walkDir → folded-dedup keyBytes was nondeterministic; (b) the load-bearing A3b test was STILL vacuous (3 independent traces — passed on current main); (c) 3 unowned modules (api/clean/cachedispatch), A5/A3d/A3c mis-sliced as rounds. **ONE genuine fork (R2-1: canonicalization) — I MADE the call: Option B (deterministic min-raw-bytes representative, purely textual, honors the locked textual non-goal), over Option A (FS on-disk-case query).** ~41 clear-best fixes + Option B all applied via round-2 rewrite → **RFC now 439 lines, verified coherent + type-consistent + hygienic.** Full ledger: `<scratchpad>/rfc0009-round2-ledger.md` (R2-1..R2-42). **Next action: Corey's call** — (a) commit RFC+handoff (uncommitted; standing rule = push direct to main), (b) round-3 fable review (RECOMMENDED — the rewrite is as large as round1→2; §4-premise reshape + re-slicing want re-scrutiny before build), or (c) `/tdd` from A0. Do NOT auto-commit or auto-start /tdd. Corey may override R2-1 to Option A.
- **BUILD STARTED 2026-09-11 (Corey: "front load the spike and lets go").** RFC + handoff committed to main (31918f6). Stage A gated on A0-spike, IN PROGRESS.
  - **A0-spike: DONE & RESOLVED (2026-09-11).** Commits 768783f (test) + 4797f69 (S1/S2/S3) + 2b9151e (verdict recorded, checkbox ticked, guard pinned). Test: `tests/conformance/test_spike_import_case.nim` (self-contained std-only; self-skips on case-sensitive volumes; live on macos APFS via conformance sweep + windows NTFS via explicit ci.yml step). **VERDICT (CI 34567534296 + 34570358383, both macos APFS + windows NTFS, all legs green):** Nim 2.2.10 canonicalizes module identity to the ON-DISK real case — two differently-cased importers of one on-disk dep unify to a SINGLE `@m` entry (`widget.nim`), independent of importer case (S3 upper-only still records widget.nim) and import order (S1==S2). => **outcome (ii), SIMPLER branch:** a dep has one deterministic spelling per committed tree; keyBytes deterministic by Nim's own canonicalization; canonicalMerge/CanonicalSet/Option B UNREACHABLE. Recorded verbatim in §4; A0-spike ticked; spike kept as permanent regression guard (asserts S1==S2==S3==[widget.nim]; fires if a future Nim records the import spelling → decision reopens). Guard-confirm CI run 34572703298 (backgrounded watch bdmv1uze9).
  - **canonicalMerge/CanonicalSet REMOVAL reshape: DONE (commit 18d29df).** Corey confirmed it's clear-best (no fork). §1 CanonicalSet/canonicalMerge deleted; §4 determinism argument rewritten to rest on Nim's on-disk canonicalization (guarded by test_spike_import_case.nim); slices A1/A3c-i/A3c-ii/A4a/A4b/A5c/A-final updated; closure seen-sets → plain HashSet[TrackedPath]; A4b determinism test reframed to on-disk-case determinism; A5c collision-member requirement dropped. KEPT: fold-on-comparison (still load-bearing), keyBytes-never-folds, dep:* escape, fnv pair-rework, all other round-3 fixes. Alternatives/Contract-impacts retain Option A/B as superseded history. Audit clean (canonicalMerge/CanonicalSet only in 4 permitted historical spots; hygiene clean; body 506 lines).
  - **A1 (paths.nim): DONE (pending CI confirm).**
  - **A1 macOS fix (commit a53cd29):** symlinked-dep-root test failed on macos (getTempDir under /var → /private/var symlink made the candidate a lexical/resolved hybrid that matched neither abs nor realAbs → UnpackDefect). Test-fixture bug; resolved base via expandFilename. Linux still green. macOS CI confirming (run 34618928618 / superseded by A0 push).
  - **A0 (golden-pin): DONE (commit pending this push).** tests/unit/test_rfc9_golden_pin.nim + tests/support/rfc9_vendored_reference.nim + fixtures. Literal pins for path-relative (simple/max-depth) vectors; depRoot vectors compute-and-compare vs vendored frozen reference through production extractClosure. Re-run in every subsequent Stage-A slice (Linux byte-identical net). Per R3-29, depRoot vectors flip to literal pins at A5a.
  - **A1 + A0: CI-GREEN all 5 legs (run 34619019735, sha 3f55db7, 2026-09-11)** — macOS /var fix confirmed on the real APFS runner. Slices A0-spike, A1, A0 DONE.
  - **A2 (config + discover + jsonout): CORE DONE, wiring the last liveness gap.** (agent a8e1017b6607c0520.) Landed green (204+20 files, golden-pin byte-identical, windows cross-compile clean): Config.trackedRoots via initTrackedRoots at 3 projectRoot origins + dep roots; dep-root naming per R3-8 (KDL `dep-roots "path" name="x"`, basename default, fold-aware uniqueness error, realAbs-alias error [dep-vs-dep], projectRoot relative base, no-/-no-: grammar); stateDirOf canonicalization (R3-25); jsonout RunSchemaRevision 24->25 + per-root {name,foldPolicy} `trackedRoots` array. Three judgment calls flagged; two accepted (effectiveProjectRoot no-op-today=ladder; dep-vs-dep alias scope=fine, dep-vs-project handled by project-first classify).
  - **THIRD flagged item = a real DORMANT-SUBSTRATE gap, being closed NOW (agent a82e2b65d6533a9d3, RED-first):** the FoldPolicy evidence was unit-tested vs jsonout.toJson directly but NOT wired to the real `crisol run --json` (RunReport didn't carry trackedRoots), so it was dark — and A3b-ii needs it live in CLI JSON. Wiring: RunReport gains trackedRoots (populated in runTestsWith from Config) -> CLI emit at crisol.nim:1241 -> proven by an END-TO-END RED test through runTests(). A2 commits only after this lands green (as one complete slice). NOT a silent defer — closed in-slice.
  - **A2: DONE.** Committed 43bf991 (slice) + fb54d64 (macOS fold-policy test fix). CI 34634496565 confirming (A2 core CI run 34631451890 was green on 4 legs, macOS flaked on a hardcoded fpNone assertion — fixed). Two macOS test-fixture flakes total this build (A1 /var symlink 03223ae→a53cd29; A2 fpNone→classify-wiring assertion) — BOTH captured in MEMORY [[rfc0009-macos-test-gotchas]]: dev container is case-sensitive + no /var symlink so neither reproduces locally; write path/fold tests volume-independently (inject probe or assert wiring) and GATE each slice on macOS CI green before stacking.
  - **A3a-i: DONE & CI-GREEN all 5 legs (commit fa7a81d, CI run 34643813132).** (sonnet agent a214c8b3bdb85fadb, RED-first.) `Entrypoint` gained `tp: TrackedPath` (additive; zero-defaults for hand-built fixtures — 74 test files compile untouched); `discover`'s `entrypointTp` producer sets it via `fromCanonical(relPath)` with a debug assert `tp.display==path` + total `classify` release fallback (never crashes). `matchGlob` gained `fold: FoldPolicy = fpNone` param (default = byte-exact, all 13 existing tests unchanged) + always-normalizes glob `\`→`/`; under fpAsciiLower folds both operands; 3 call sites pass `config.trackedRoots.project.foldPolicy`. `normalizeRootRelative` rewritten to route through `classify` (total, exception-free; pcOutside→raw fallback), dropping the now-dead `root` param from `resolveFilesGroups`. IN-SLICE FIX (defect, not fork): adding `tp` broke `test_shard`/`test_c3_balanced_shard` (`seq[Entrypoint].find` needs derived `==`/`hash` → `TrackedPath`/`RootTag` ops not visible where those files only import types) → added `export paths` to types.nim (matches the existing Outcome/HermeticLevel re-export direction). Gates: `./dev test` exit 0 (205 unit + 20 conformance), `nim check --os:windows` exit 0, hygiene clean, all new tests volume-independent (macOS caution honored). Commit + CI pending.
  - **A3b-i: DONE & CI-GREEN all 5 legs (commit 08aff12, CI run 34650257910).** (sonnet agent a467401c8ae2c6ddc, RED-first.) gitdiff `-z` NUL-sep BOTH invocations (fixes core.quotepath hazard, proven by a `café.nim` + `core.quotepath true` test) + `reduceChangedName` (fromCanonical primary, classify fallback for defensive rejects; documented unreachable residual = pcOutside drop, physically un-constructible as TrackedPath) → `changedFiles*(projectRoot; roots; base="")` returns `HashSet[TrackedPath]`; `splitNul` helper (no `.strip()`, preserves exact bytes). Carried through api:896 (passes cfg.trackedRoots) + buildRunPlan.changed:`HashSet[TrackedPath]` to narrow's door with a TEMPORARY `display()`→HashSet[string] adapter at pipeline.nim:~150 (comment-marked deleted-at-A3b-ii; narrow.nim UNCHANGED). Migrated test_soundness_m9/m14 + test_changed integration + test_shard_pipeline (unplanned, caught by full-suite gate) to the TrackedPath shape, intent preserved, no assertion weakened. Gates: `./dev test` exit 0 (205+20), integration test_changed 10/10, `nim check --os:windows` exit 0, hygiene clean. **BOUNDARY CALL MADE (control loop, honoring no-dormant-substrate):** the RFC text schedules the graph-side adapter (R3-17a) "in A3b-i", but in A3b-i narrow still compares string↔string so that adapter has ZERO call sites — creating it here = dead code. Its only consumer is A3b-ii's retyped narrow, so **A3b-ii creates it** (nothing deferred — A3b-ii still lands folded graph membership on schedule). A3b-ii brief MUST create the `TrackedRoots`-parameterized graph-side adapter (HashSet[string] closure → membership vs HashSet[TrackedPath]), handle the fnv.nim:68 ABSOLUTE depRoot member via `classify`/srStaleEntry (R3-17b, never bare fromCanonical), and delete A3b-i's temp door adapter.
  - **A3b-ii: IN PROGRESS (LOAD-BEARING E2E, Windows-runtime-only DoD=CI-green per R3-18.3).** Pre-sliced by control loop into TWO sequential agents, committed as ONE A3b-ii (verify locally between, single commit + CI run). **Part 1 (agent afc5b0ba6d8e4a744): DONE & VERIFIED (control-loop gates: ./dev test exit 0, nim check --os:windows exit 0; UNCOMMITTED, awaits Part 2 for single A3b-ii commit).** narrow.nim retyped — selectByDiff/narrowByDiff gained `roots: TrackedRoots` + `changed: HashSet[TrackedPath]` (5-arg shape); Rule 2 reduces `ep.path` via fromCanonical (folds); Rule 5 uses private `reduceClosure` graph-side adapter (fromCanonical→classify for ABSOLUTE members→srStaleEntry if unclassifiable, R3-17b, never bare fromCanonical); pipeline temp adapter deleted; 28 real call sites migrated across 4 files (test_narrow 10, test_fallback 14 — incl. 5 blocks re-anchored abs→relative-under-tmpRoot since fromCanonical rejects absolute, assertions/SelectionReasons preserved; test_changed 1, test_closure_searchpath 1) via new tests/support/rfc9_narrow_support.nim (mkRoots/changedTp); the other 4 briefed files had 0 direct calls (only comment mentions). Behavior-preserving on case-sensitive Linux by construction. **Part 2 (agent ac090ecaea45cc273): DONE & VERIFIED.** New `tests/conformance/test_rfc9_a3bii_fold_selection.nim` proves folded selection LIVE via real git repo + committed case-rename (`git mv widget→tmp→Widget`) + edit, RUN 1 persists graph, RUN 2 `--changed` selects dependent NOT independent; negative control confirmed non-vacuous (control-loop verified: graph member raw `tests/unit/widget.nim` vs diff `tests/unit/Widget.nim`, old spelling absent from changed set; agent also proved RED under fpNone). 3 env modes: CRISOL_FOLD_POLICY (Linux forced-inject via probe seam), CRISOL_EXPECT_FOLD (windows/macos hard-assert real probe + JSON evidence), else self-skip. ci.yml wired on all 3 jobs (ubuntu forced-policy step, windows per-file step, macos sweep env pin); `./dev run` gained CRISOL_FOLD_POLICY/CRISOL_EXPECT_FOLD passthrough. Diff base = post-rename commit vs working tree (so old spelling can't appear). Decoy: under forced mode only, recreates old spelling as gitignored file so depgraph.isEntryStale's raw fileExists (fold-unaware, correct on real case-insensitive vols) doesn't force srStaleEntry on ext4 — legitimate simulation, documented.
  - **A3b-ii SEAM FIX (control loop made the call, in-slice):** the agent's Part-2 report exposed a real production defect — `paths.memoizedProbe`'s per-process memo keyed ONLY on rootAbs, ignoring probe identity, so a prior default-probe call (e.g. runTests' internal loadConfig) POISONED the memo and silently defeated a later forced-probe injection on the same root (the RFC §3 injectable seam A3c-ii/A4b also rely on). Fixed in paths.nim: bypass memo (read+write) for any probe != probeFoldPolicy; production default-probe path unchanged (still memoized). Removed the test's pre-warm hack; added a test_paths.nim regression test (two injected policies, same root, second honored). Verified: forced-policy conformance run green WITHOUT pre-warm; test_paths green.
  - **A3b-ii committed 172c5ff (narrow+pipeline+reduceClosure + conformance + ci.yml + dev + paths memo fix + migrations) — then CI run 34659246648 RED on windows+macos (the real target legs).** ROOT CAUSE (diagnosed via CI-diag echoes; reproduced on Linux too): the conformance fixture gitignored `tests/unit/widget.nim` to hide the forced-mode isEntryStale decoy, which ALSO left the original un-added → committed `git mv` failed silently (`fatal: not under version control`) → no rename → changed set reached the new spelling only via gitdiff's UNTRACKED scan (M14/R3-24) on case-sensitive Linux (accidental pass), but on case-INSENSITIVE windows/macos the untracked path is masked as the tracked lowercase name → changed set EMPTY → load-bearing selection unproven. A THIRD cross-platform gotcha class (git case-rename on case-insensitive FS); dev container can't reproduce. **FIX committed 58fea25:** don't gitignore widget.nim (must be tracked to rename); keep the forced-mode decoy out of the untracked scan via `.git/info/exclude` (applied post-rename); assert each git mv/commit exit code (silent-fail → loud). Verified locally: raw `git diff` vs rename commit now emits `tests/unit/Widget.nim` (new spelling via the DIFF, not the untracked scan) — behaves identically on case-insensitive vols. Local gates green + non-vacuous; self-skip clean; hygiene clean.
  - **A3b-ii SECOND backtrack — CI run 34661284061 (58fea25): macOS now GREEN (rename fix worked), windows STILL RED, but a DIFFERENT + deeper (PRODUCTION) bug:** `gitdiff.runGit` used `poStdErrToStdOut`, merging git's stderr into the parsed stdout. On Windows with `core.autocrlf`, `git diff` emits a per-file "LF will be replaced by CRLF" warning to stderr → parsed as a changed-file NAME → changed set = {the warning text}, real Widget.nim never entered selection. A real correctness bug for any Windows user with autocrlf, flushed out by the E2E. **FIX committed 2dddb91 (PRODUCTION gitdiff.nim):** capture stderr separately, never parse it as data; stdout = NUL-sep names only; stderr only in the "git diff exited code N" error msg; stdout drained first (large-diff safe), stderr small (no deadlock). Windows-specific (no Linux repro); the windows A3b-ii E2E step (CRISOL_EXPECT_FOLD, real NTFS+autocrlf) is the permanent regression guard. Local gates green (E2E forced, M9/M14, test_changed, windows cross-compile).
  - **A3b-ii: DONE & CI-GREEN ALL 5 LEGS (commit 2dddb91, CI run 34663083005).** Windows NTFS negative control passed live: graph `tests/unit/widget.nim` vs diff `tests/unit/Widget.nim` differ raw, changed={Widget.nim}, dependent selected via fold. The load-bearing selection property is now PROVEN LIVE on both case-insensitive targets (windows NTFS + macos APFS). Commits: 172c5ff (slice) + 58fea25 (fixture rename fix) + 2dddb91 (gitdiff stderr-merge PRODUCTION fix). TWO real cross-platform bugs caught by this E2E (both invisible on ext4 dev container) — saved to [[rfc0009-macos-test-gotchas]] gotchas 3. Tick committed (this commit).
  - **A3c-i DONE & CI-pending** (depgraph header: persists tag→NAME table + per-name foldPolicy from config.trackedRoots via rootsDescriptor/saveDepGraph; loadDepGraph validates against CURRENT config.trackedRoots — unknown name→dgdRootUnknown, resolvable-but-fold-disagree→dgdFoldMismatch, both discard-as-absent never cross-policy; format bump 5→6; consumed live at pipeline.nim:114). Load-bearing test (round-trip + both discards) green via fixedProbe (volume-independent, R3-11). **NEXT: A3c-ii** (depgraph ENTRY table retypes graph.entries[].closure to TrackedPath; narrow.nim:70/:80 string-keyed lookups retype; delete narrow's reduceClosure-era string-typed comparison [the A3b-i 'adapter' was NEVER created — see A3b-i boundary call]; pipeline.nim:111 loadDepGraph caller gains TrackedRoots param; api.nim:977 (ep.path,fHash) lookup retypes; depgraph.nim:514/:527 sorts move to cmpKeyBytes order, R3-3; Windows producer-rule step DEFERRED to A4b, R3-11). Slices DONE (8): A0-spike, A1, A0, A2, A3a-i, A3b-i, A3b-ii, A3c-i. Remaining Stage A: A3c-ii, A3d-i/ii/iii/iv, A4a, A4b, A5a, A5b-i, A5b-ii, A5c, A-degraded, A-final-i, A-final-ii. Stage B: B-inventory, B1, B2, B3a, B3b, B3c, B4a, B4b.
  - **GOTCHA to save to MEMORY once A3b-ii is green:** git case-only rename fixtures — (1) never gitignore a file you intend to `git mv` (leaves it un-added → silent mv failure); (2) on case-insensitive FS the untracked-file scan masks an untracked new-case spelling as the tracked old-case name; (3) prove the changed spelling comes from the DIFF, not the untracked scan; (4) use `.git/info/exclude` (not `.gitignore`) for a decoy that must not block tracking. Extends [[rfc0009-macos-test-gotchas]]. the committed-rename conformance test (RFC line 540 recipe EXACTLY: real git repo, ≥2 entrypoints one transitively importing a to-be-renamed dep, run-1 without --changed to persist graph, COMMITTED `git mv foo→tmp→Foo` + edit, diff vs pre-rename commit, MANDATORY negative control asserting raw-string spelling divergence, assert dependent SELECTED + independent NOT) with TWO env modes: CRISOL_FOLD_POLICY=fpAsciiLower (Linux forced-injection via initTrackedRoots probe param → E2E body runs green on Linux leg, R3-18.2) and CRISOL_EXPECT_FOLD=fpAsciiLower (windows/macos: hard-assert real probe==fpAsciiLower AND run JSON evidence reports it; self-skip only when neither env set on case-sensitive vol, R3-18.4); + ci.yml windows per-file step + Linux `test`-leg forced-policy step + macos conformance-sweep CRISOL_EXPECT_FOLD pin. DoD: CI-green windows+macos (not local). gitdiff argv `git diff -z --no-renames --relative --name-only` NUL-sep + `fromCanonical`-reduce; ALSO pin the SECOND git invocation (untracked, `git ls-files -z --others --exclude-standard`, R3-24) through the identical path; api.nim:880 changedFiles seam + pipeline diff-ingest carry TrackedPath set to narrow's door (narrow NOT retyped this half — temporary fromCanonical adapter); CREATE the graph-side `TrackedRoots`-parameterized adapter (R3-17a, deleted at A3c-ii). Slices done: A0-spike, A1, A0, A2, A3a-i (5). Remaining Stage A: A3b-i, A3b-ii, A3c-i, A3c-ii, A3d-i/ii/iii/iv, A4a, A4b, A5a, A5b-i, A5b-ii, A5c, A-degraded, A-final-i, A-final-ii. Stage B: B-inventory, B1, B2, B3a, B3b, B3c, B4a, B4b.
  - **BOOKKEEPING DEBT (flag to Corey, not blocking):** rfc-flow commit hook warns RFC-0009's TOML fence has 15 stale round-1 [[item]] rows (superseded by round-2/3 design + slices; titles truncated/ungraded) + missing value/size grades. Reconcile in a dedicated pass (quipu set states / add grades), not mid-ladder. (sonnet agent aed3054b45c558d63, RED-first). Building src/crisol/paths.nim (all §1 types + procs, injectable probe, cmpKeyBytes-not-`<`, keyBytes(tp,roots), rel field, private TrackedRoots, fromCanonical→Option) + tests/unit/test_paths.nim (injected-policy unit conformance) + tests/conformance/test_fold_probe.nim (windows-leg runtime probe) + ci.yml step. Uncommitted until reviewed. NEXT after A1 lands: review interface fidelity to §1 + gates green, commit+push+watch CI, report progress line, then A2 (config.nim: dep-root names/uniqueness/aliasing/base + stateDirOf + jsonout schema-rev + ClosureV1 bump).
  - **KNOWN WRINKLE:** the RFC now carries an rfc-flow TOML front-matter fence (auto-prepended on commit) with 15 stale round-1 `[[item]]` rows (round-1 deferred-fixes language, e.g. i1 mentions `<`/identBytes which round-3 removed) and NO `[[slice]]` rows. Not reconciled — flag to Corey; the body `- [ ]`/`- [x]` slice checkboxes are the working ledger. Commit hook also warns other RFCs (0001-0004) have incomplete items while living/done (pre-existing, not this build).
  - **Origin:** RFC-0007 D2b escalation — path identity is the prerequisite to de-POSIX the test suite and take full-suite Windows green.

## Round-1 terrain survey (the "survey" the RFC cites)

Two mapping agents swept `src/` (path-identity hazards) and `tests/` (posix coupling).

### Path-identity hazard sites (src)
- **Canonical scheme already exists but unenforced:** `closure.toProjectRelative` (closure.nim:482-506) strips root prefix + forces forward slashes — the seed of `ProjectPath.display`. `Entrypoint.path` and dep-graph keys use the same scheme.
- **`projectRoot` un-normalized at origin, re-derived 15+ times:** `config.nim` (112/154/168/937) produces `projectRoot` via bare `absolutePath` (no `normalizedPath`); `closure`/`depgraph`/`runner`/`cacheregistry`/`fnv` each recompute `projectRoot.absolutePath.normalizedPath` independently; `discover.nim:306` uses it verbatim. Feasibility measured **52 canonicalization sites across 7 modules**.
- **Equality/membership/dedup (case-fold hazards):** `narrow.nim:62/79` (`in changed` / `disjoint`) — **highest stakes**, git `core.ignorecase` vs discovery case → silent UNDER-selection; `depgraph` entry `Table[(string,string),…]`; `discover` `(relPath,group)` dedup; `closure` byBasename/byReal/`underAnyRoot`/seen-sets; `cacheregistry` underRootNorm/rootInsideStateDir. **No `sameFile`/inode primitive anywhere** — identity is always string equality.
- **Path hashing into cache keys:** `keys.identityKey`/`soundnessKey`, `planner.slug` (on-disk dir name), `cachelocalfs.sidecarPath`, `fnv.chainedContentHash` (folds relPath). None re-canonicalize.
- **POSIX/`/`-anchored assumptions:** `artifactid.eraseKnownString` (`/`-anchored, breaks on `C:`), `compilereport`/`ccprobe` (gcc/Make-shaped), `process/windows.nim` sink/cwd opened without `\\?\` long-path prefix.
- **`Entrypoint.path` blast radius (feasibility):** read in **20 src modules (~106 sites)**; **74 test files construct `Entrypoint(...)` (221 sites)** + ~35 read `.path`. Retype is whole-program-atomic.
- **Ranked blast radius:** closure > depgraph > keys > narrow(+gitdiff) > config > discover > cacheregistry > planner/cachelocalfs.

### Test-suite posix coupling (74 files; grep=73 importers)
- **B (48):** process control (fork/waitpid/setsid/kill) — gate `when defined(posix)`, SKIP on windows (correct; tests the POSIX backend).
- **A (4):** signal handling (sigaction) — gate posix.
- **C (19):** file I/O + rlimit — rewrite to std/os where possible; gate rlimit (3).
- **E (3):** getpid → `os.getCurrentProcessId`.
- **src (8):** all already correctly platform-gated (process.nim, httpraw, ioutils, lock, process/*).
- **Hidden (breadth bF18):** ~24 files hard-code `/tmp`,`/bin/sh`,`/proc`,`chmod`,shebang WITHOUT importing posix → compile but fail at runtime on windows. Needs its own bucket (B3c).
- **`test_conformance.nim` itself imports posix — it IS the harness.** Windows CI leg = **native Nim** (setup-nim-action, Git Bash, per-file `nim r`), NO podman; B4 precedent is the macOS leg's `bash ci/run-tests.sh`, not `./dev`.

## Round-1 review outcome (per lens)
- **DESIGN:** core split validated (two-field object beats distinct-string & interned-handle). Blockers: public `display*` field defeats the invariant (counterfeitable) → private + accessor; missing constructor for already-canonical relative input → `fromCanonical`; depRoot contradiction (FORK A).
- **DEPTH:** 4 blockers — degraded default soundness-inverted; refuse-if-not-under-root contradicts closure (silent exclude, not refuse) + can't represent depRoot absolute members; A4a is a type error (`byReal` keyed by out-of-root realpaths — needs a distinct `NativeAbs`); native-side gates stay case-sensitive UPSTREAM of ProjectPath (missing native-canonicalizer slice). Plus chainedContentHash one-string-two-jobs, planner.slug half-fold, git quoting, `==` cross-policy.
- **BREADTH:** unowned surfaces (jsonout.loadLastRun, order.nim, depparse, cacheregistry.rootInsideStateDir); fold-policy instability across runs → persist policy in depgraph header; **macOS-latest already runs full suite on case-insensitive APFS → gate identity tests on fold-policy not `windows` for a free proof**; RFC-0008 boundary; cross-policy fleet split; 24 hidden posix-assuming tests; reconcile counts.
- **FEASIBILITY:** A3a is a ROUND not a slice (FORK B); B4 `./dev` wrong → split B4a/B4b native; add slice **A0 golden-pin** (capture Linux vectors vs main, run every Stage-A slice); `FoldPolicy` must be **injectable** past the probe (Linux-testable) + lazy/cached; A3b recipe traps (real git repo, rename-without-git-mv, ≥2 entrypoints, separator enters graph-side).
- **LIVENESS:** **A3b as drafted passes on current `main`** (fresh temp project → no graph → conservative inclusion). Fix: 2 entrypoints, prior warm run, diff names a transitively-imported DEP, assert dependent-selected + independent-NOT. A5 hit fakeable → pin `--cache-stats` l1Hits==1 + one stored entry + negative control. Dark fields: FoldPolicy report, persisted real-case `display`. B4 subset-honesty (expected-skip manifest + executed-floor).

## Deferred clear-best fixes (apply in the round-2 rewrite, once forks decided)
Full detail in the session review ledger: `<scratchpad>/rfc0009-review-ledger.md`. Summary:
1. §1 type: private fields + `display*()` accessor; add `<` (over ident); `toNative(pp,root)` (mirror); `keyBytes` (was identBytes); add `fromCanonical(rel, root)` constructor.
2. §3 probe: OS query first (Win `FileCaseSensitiveInformation`, mac `pathconf _PC_CASE_SENSITIVE`); create-and-stat fallback per-process-unique suffix under stateDir-iff-same-volume; lazy+cached; **injectable** for Linux tests; probe-failure ⇒ full-run + no-cache (both fail-safe).
3. Native-canonicalizer slice: steps 1-2 (abs/norm/drive-letter/separator + fold) applied to ALL native paths entering SourceIndex/underAnyRoot; distinct `NativeAbs` type for byReal/underAnyRoot inputs (fixes depth A4a type error + F4 upstream under-selection).
4. New slice **A0** golden-pin (run in every Stage-A slice; vectors include an absolute depRoot member + max-depth entrypoint).
5. A3b: real git repo; mixed-case via commit→warm-run→disk-rename-without-git-mv→edit→`git diff -z --name-only`; ≥2 entrypoints, diff names a transitively-imported dep; assert dependent-selected + independent-NOT; separator variance enters graph-side.
6. A5: observation via `--json --cache-stats` (l1Hits==1/misses==0) + exactly-one-stored-entry + negative control.
7. FoldPolicy evidence: emit in A2 (jsonout schema-rev note), assert in A3b twins.
8. Persist foldPolicy in depgraph header; mismatch ⇒ discard-as-absent. A3c byte-level assert real-case `display` persisted (windows leg).
9. Unowned surfaces get owners: jsonout.loadLastRun, order.nim, depparse (→A4b), cacheregistry.rootInsideStateDir, quarantine membership (config.nim:796/runner.nim:147), planner memo key, pipeline keep-sets, shard identity, workerplan.entrypointPath (=display). Completion grep-gate: allowlist absolutePath/normalizedPath/expandFilename to paths.nim.
10. B4 split → B4a (native `ci/run-tests.sh` over tests/unit) / B4b (widen conformance[+integration per scope] + RFC-0007 line-555 tick); name test_conformance.nim in B3a; subset-honesty (expected-skip manifest + executed-floor); D2b tick text = "compiles for windows; posix categories skip by design; identity proven by A3b/A5".
11. B3c bucket (24 import-clean posix-assuming tests). Pin auditable bucket inventory (meta-test bucket∪ == grep). Extend import-purity meta-test to forbid std/posix in tests/support/.
12. macOS: gate identity conformance tests on fold-policy (self-skip fpNone) not `windows`; name macOS-latest in Stage A acceptance.
13. §1/§2/§5: total boundary returning classification (inProject | inDepRoot | outside), throw only where attribution required (fixes refuse-contradiction).
14. `\\?\` : A1 conformance includes a deliberate >260-char `nativeOf` round-trip (scheduled RED decides prefix).
15. Empty/root path invariant (display non-empty, no lead/trail `/`); explicit `toJson` emitting display only (never persist ident).

## FORK DECISIONS (Corey, 2026-09-10 — "best-in-class PhD-level design"; make the call, no fake forks)
- **FORK A → ROOT-TAGGED.** Rename `ProjectPath`→`TrackedPath`. Identity = `(rootTag, rel)`; project root = tag 0, dep roots get stable tags (configured name/order). Portable+attestable by construction; kills the machine-local-abs-path-in-key wart; dissolves multi-volume. Boundary returns classification `inProject | inDepRoot(tag,rel) | outside`; distinct `NativeAbs` for byReal/underAnyRoot inputs.
- **FORK B → FULL ADDITIVE LADDER** (RFC-0007-A1 pattern). The field BECOMES the type; enforcement (un-canonicalized comparison = compile error) is the thesis. A3a-i additive (TrackedPath alongside path:string, discover produces both, invariant display==path) → migrate one surface/slice → A3a-final removes path:string via compiler-enumerated + scripted 221-site test sweep.
- **FORK C → FOLD COMPARISONS, NEVER FOLD KEYS.** Two axes of the type: (1) `==`/`hash`/membership/lookup/closure-gates FOLD under probed policy (selection soundness). (2) `keyBytes` (identityKey/soundnessKey/slug/sidecar/chainedContentHash) NEVER fold — derive from discovery's on-disk case → fleet-portable, probe-INDEPENDENT, immune to fold-induced wrong-hit. Diff case reaches ONLY membership, never a key → no inconsistency. Load-bearing property keeps its SELECTION half (mixed-case --changed selects once); the cache half becomes "same tree → same key on every host/run" (portability), NOT "warm hit across spellings". `A5` reframes from cross-spelling-hit to key-portability + no-wrong-hit.
- **FORK 3 → SCOPED OUT** (measurement/cc-probing Linux-only; RFC-0006 parked). Applied.

## (superseded) FORKS as presented for decision
- **FORK A — depRoot / multi-root identity representation.** closure legitimately tracks files under depRoots (not under projectRoot); today `toProjectRelative` falls through to a raw ABSOLUTE spelling for them (proven by `fnv.nim:68 if relPath.isAbsolute`). `ProjectPath`="project-relative" can't represent them. Options: (a) **root-tagged** `TrackedRoots{project; deps: seq[NativeRoot]}` + `ProjectPath` carries a `rootTag`, dep members key `dep:<name>/rel` (principled; fixes the machine-local-abs-path-in-attested-key wart; dissolves the multi-volume risk; renames→`TrackedPath`; NEW persisted shape for dep members → touches RFC-0005's no-reshape claim for depRoot-using projects — but those keys were never fleet-portable anyway; Linux single-root keys stay bit-identical). (b) **foreign-absolute carried forward** (compat; smaller; enshrines the abs-in-key wart, documented). **Reco: (a) root-tagged** — first-principles-correct, and depRoot abs-keys weren't meaningfully attestable. Corey owns the RFC-0005-wire-risk appetite.
- **FORK B — `Entrypoint.path` migration strategy.** Retype is whole-program-atomic (20 modules, 221 test constructions). Options: (a) **full additive ladder** (RFC-0007-A1 precedent: add ProjectPath alongside path:string, migrate surfaces one per slice, remove path:string via compiler-enumerated + scripted test sweep) — keeps the "un-canonicalized comparison = compile error" enforcement the RFC's motivation stakes on. (b) **surface-only** (keep path:string permanently as display; introduce ProjectPath only at membership/key surfaces) — much smaller blast radius, forfeits compile-time enforcement. **Reco: (a) ladder** — the enforcement IS the design's thesis (echoes ChildId/LimitKind); RFC-0007 paid exactly this cost. Corey owns the effort/risk-vs-rigor call.
- **FORK C — cache-key fold scope (reshapes the load-bearing property).** Options: (a) **fold keys per-volume** (current draft) — warm hits across manifest/discovery case drift on one volume; cross-policy fleet split; sound only with a correct probe. (b) **keys always `fpNone`, fold membership only** — keys derive from discovery's on-disk case (canonical by construction), so folding buys little; fleet-portable, probe-independent for keys, sound on case-sensitive volumes; cost: drops the "warm hit across spellings" half of the load-bearing property (cache goes cold, never wrong, on case drift). **Reco: (b)** — crisol's value is sound SELECTION not compile speed (MEMORY → crisol-value-is-selection); membership-fold delivers the soundness, keys-fpNone keeps portability+simplicity. Corey owns whether Windows cache-warmth-across-drift is worth the probe dependency + fleet split.
- **FORK 3 (recommended-resolved, not blocking):** artifactid/ccprobe/measurement Windows-correctness — **scoped OUT** via a new Non-goal (measurement is RFC-0006's concern, parked on negative benchmark). Applied to the draft; flag if Corey disagrees.
