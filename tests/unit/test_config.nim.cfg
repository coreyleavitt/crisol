# Compile this suite with TLS support, matching the produced binary (src/crisol.nim.cfg).
# The non-ssl side of the https capability guard is pinned by
# tests/integration/test_https_capability_guard.nim, which deliberately omits this.
--define:ssl
